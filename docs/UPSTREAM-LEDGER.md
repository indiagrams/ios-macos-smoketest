# Upstream learning ledger

The record of what this fork learned by shipping for real, what happened to each
learning, and — for the ones that will never go upstream — why not.

## Why this exists

This fork exists to walk the shipping path and find out where the template hurts. Every
generic shipping learning is proposed upstream to
[apple-shipkit](https://github.com/indiagrams/apple-shipkit) **when it is hit**, not
batched into a cleanup pass at the end of the project. Batching is how ledgers die: a row
written three phases after the fact is a reconstruction, not a record.

Every learning gets a row — **including the ones that will never go upstream**. A
learning that fails the [SCOPE.md](../SCOPE.md) test, or that is a fork concretion rather
than a generic capability, is still recorded, with the reason it was ruled out. That is
what makes the scope gate auditable instead of a matter of memory, and it is what makes
this project's close-out a mechanical check rather than an archaeology project.

**A row is added in the same change as the learning that produced it.** That rule, plus
the rule that documentation is updated in the same change, are standing exit criteria on
*every* phase of this project — not just the phase that created this file.

How a learning gets from here to upstream is [CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md).
Short version: there is no fork relationship and no cross-repo PR, so it travels by
`git format-patch` into a separate clone of apple-shipkit.

## Verdict vocabulary

Six values, closed set. Three are terminal. Adapted from the Yocto Project's
`Upstream-Status:` patch tag, which exists to answer exactly the question D-19 asks:
*why was this deliberately not sent upstream?*

- `Pending` — identified, not yet proposed. **Non-terminal.**
- `Submitted [#N]` — PR open upstream. **Non-terminal.** The PR number is required.
- `Merged [#N]` — landed in apple-shipkit. **Terminal.** The PR number is required.
- `Denied [reason]` — proposed, upstream declined. **Terminal.** Reason required in brackets.
- `Out-of-scope [reason]` — fails the SCOPE.md test. **Terminal.** Reason required in brackets.
- `Fork-only [reason]` — passes the SCOPE.md test but is a fork concretion; upstream gets
  the slot, never the value. **Terminal.** Reason required in brackets.

Do not invent a seventh value. A vocabulary that grows mid-project stops being greppable,
which is the only property this column has.

## Close-out contract

Closing this project out is defined as: **no row is left non-terminal.** That is one
command, not a review meeting:

```bash
grep -cE '^\| UL-[0-9]{3} \|.*\| (Pending|Submitted)' docs/UPSTREAM-LEDGER.md
```

It must read `0` at close-out. If it does not, every remaining non-terminal row must be
listed explicitly with the reason it is still open — an accepted, stated exception, never
a silent one.

## Row conventions

- **IDs are `UL-NNN`, monotonically increasing, and never reused.** A retired or mistaken
  row keeps its ID and gets a terminal verdict explaining what happened. Stable IDs mean a
  row can be cited from a commit message or an upstream PR body and still resolve later.
- **`SCOPE test`** holds the literal answer to SCOPE.md's question — `No - in scope` or
  `Yes - out of scope`. Cite the test's sentence, not its path literal; the path moves
  across template versions.
- **`PR`** is an em dash until a PR exists.
- **No fork concretion in any cell** — no Team ID, no contact address, no secrets. Values
  that belong to this fork live in this fork's own docs and are referenced from here.

## Hazard: this file can be deleted wholesale

`bin/refork-smoketest.sh` runs `gh repo delete` followed by `gh repo create --template`.
Running it **destroys this repository and recreates it from the template**, taking this
ledger with it — along with every other fork-owned file this project produced. Nothing in
this file survives that, because remotes, untracked state, and fork-owned docs alike are
all recreated from the template rather than preserved.

This is recorded as a risk, not fixed here; fixing it is outside this phase's scope. Two
consequences worth carrying forward:

- It is the real argument for upstreaming promptly. A fork-side tool that has been
  upstreamed survives a refork. One that has not, does not.
- "This ledger's content has been upstreamed or archived" is a close-out item, not an
  afterthought.

## Ledger

Rows UL-007 through UL-011 were added in the phase close-out rather than in the change that produced each learning, which is late by this project's own rule ([CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md) section 5). Each of them says so in its Notes cell and names the commit that carried the learning, rather than being back-dated to look timely. A late row that admits it is late is still a record; a back-dated one is not. From Phase 2 on, the row goes in the same change.

| ID | Date | Phase | Learning | SCOPE test | Verdict | PR | Notes |
|----|------|-------|----------|------------|---------|----|-------|
| UL-001 | 2026-08-31 | 1 | Review-notes generator plus its drift gate — `tools/gen-review-notes.rb`, `test/gen_review_notes_test.rb`, `test/docs_structure_test.rb`, `.github/workflows/review-notes.yml` | No - in scope | Pending | — | D-14. Tooling around the project, not app code. All four files now exist and the workflow's job name, `review notes`, is a required status check on `main`. The generator is dependency-free — zero `require` statements — so the upstream port needs no Gemfile change. Upstream shape: rename to `bin/gen-review-notes.rb`, add a `ci/check-review-notes.sh` wrapper matching the existing `ci/check-app-icon.sh` convention, and add a job to upstream's `pr.yml`. |
| UL-002 | 2026-08-31 | 1 | An upstream-learning-ledger template — this file's structure, vocabulary, and close-out contract | No - in scope | Pending | — | D-20. Upstream gets the structure; this fork's entries stay here. The schema is a generic capability, the rows are fork concretions. |
| UL-003 | 2026-08-31 | 1 | `bin/setup-github.sh` recomputes and PUTs the required-status-checks array from scratch, silently destroying any fork-added required check on the next run | No - in scope | Pending | — | It should preserve unknown checks, or read an extra-checks list from config, rather than assuming it is the only writer. Affects Phase 4's IDENT-11 gate as much as this phase's drift check. Now load-bearing rather than theoretical: 01-06 made `review notes` the ninth required check, so any `make setup-github` run silently returns protection to eight and the gate stops gating while still reporting. Recovery runbook, verified idempotent, is `CONTRIBUTING-UPSTREAM.md` section 6; verify by context count, not by looking at the Actions tab. |
| UL-004 | 2026-08-31 | 1 | `SCOPE.md`'s governing test hardcodes an app directory path that moves across template versions — `app/SmokeApp/` here, `app/HelloApp/` upstream — so the quoted test goes stale while the test itself has not changed | No - in scope | Pending | — | Would be durable phrased against "the app target". Interacts with DOC-03 in Phase 5. |
| UL-005 | 2026-08-31 | 1 | `AGENTS.md` documents a `git merge upstream/main` sync path that cannot work for a repository materialised by `gh repo create --template`, and its opening line links the words apple-shipkit to the smoketest URL | No - in scope | Pending | — | **The fork-side correction landed in this phase**, commit `c56bde9`: the opening paragraph now states the `gh repo create --template` relationship, and the sync section is a prohibition table with consequences plus the four fetch-and-compare uses of the remote. **Upstream still needs the same fix to its own copy** — upstream repaired the link but not the merge path, and a template that tells every forker to merge `upstream/main` is wrong for every template-materialised repo, not just this one. Interacts with DOC-03. |
| UL-006 | 2026-08-31 | 1 | The Shipkit Pipes product identity — App Store display name and both platform bundle IDs | No - in scope | Fork-only [fork concretion; upstream gets the slot, never the value] | — | Recorded in `docs/PRODUCT-IDENTITY.md`, which 01-04 created — the link was dangling when this row was written and now resolves. Upstream keeps its placeholders. This row exists to demonstrate the Fork-only verdict: the learning passes the SCOPE test, and is still deliberately not upstreamed. |
| UL-007 | 2026-08-31 | 1 | The template ships a single `fastlane/metadata/` tree while documenting dual-platform support, and one `review_information/` directory cannot hold two app records' metadata | No - in scope | Pending | — | Surfaced in 01-05, when the generated `id=macos` notes block turned out to have nowhere to be written. Blocks a per-platform notes generator, so splitting the tree is a prerequisite inside Phase 8 rather than optional cleanup; the generator already takes `--id` and `--dest`, only the directories are missing. **Late row** — the learning landed in commit `ac851da` and this row in the phase close-out. |
| UL-008 | 2026-08-31 | 1 | `read_review_field` resolves `ENV[APP_REVIEW_NOTES]` ahead of `notes.txt`, and `.bootstrap.env.example` actively recommends setting that variable — so the recommended path silently defeats any generated review-notes file | No - in scope | Pending | — | D-25. Upstream could invert the precedence, or warn at the recommendation that the variable overrides the tracked file that CI checks. Worked around fork-side by making `tools/gen-review-notes.rb --check` assert the variable is unset in both the process environment and `.bootstrap.env`, rather than merely diffing two files: a file-only comparison goes green while Apple receives text nobody reviewed. **Late row** — the learning landed in commit `ee3a86f` and this row in the phase close-out. |
| UL-009 | 2026-08-31 | 1 | GitHub replaces the entire `checks` array when `required_status_checks` is PATCHed with one, so the obvious command for adding a required check destroys every check already there | No - in scope | Pending | — | Caught in 01-06 before it ran; the shape as planned would have left `main` protected by a single context. The safe form reads the live array, appends the new entry, and PATCHes the result — additive by construction and idempotent. Distinct from UL-003: that row is `bin/setup-github.sh` dropping a fork-added check, this one is the naive repair doing worse damage in the other direction. Runbook in `CONTRIBUTING-UPSTREAM.md` section 6, including why the parent `/protection` object must never be PUT. **Late row** — the learning landed in commit `7286037` and this row in the phase close-out. |
| UL-010 | 2026-08-31 | 1 | A grep-based gate run over a repository that documents its own literals matches the prose explaining the gate; three separate near-misses in this phase alone | No - in scope | Pending | — | Occurrences: this file's close-out grep would have matched its own verdict vocabulary had that vocabulary been formatted as a table (01-03); a workflow comment naming the trigger it forbids defeated a grep for that trigger (01-06); and a note explaining a banned phrase would have tripped the ban on it (01-06). Upstream carries the same shape in `bin/verify-rename.sh`, which greps five literals repo-wide and maintains a hand-written seven-path exclusion list that has to grow every time a doc discusses one. Generic fix: extract the specific section or table, or strip comment regions, instead of growing an allowlist. **Late row** — the learnings landed in commits `48ee116`, `c31a7a7` and `2ca986e`, and this row in the phase close-out. |
| UL-011 | 2026-08-31 | 1 | A competitive-scan verdict was recorded from a store search result's name alone, and the wrong verdict stopped the phase before the listing was read | No - in scope | Fork-only [product-research learning about this fork's own App Review argument; the template has no slot for it] | — | 01-02 recorded `CyberChef Pro` as a CyberChef derivative on presence alone and escalated a blocker against D-24; commit `db936de` corrected both assumption verdicts to NO after the App Store listing showed an unrelated cooking app. The original error is preserved in `docs/REVIEW-ARGUMENTS.md`'s audit trail rather than rewritten away, because it is exactly the reasoning failure that document exists to guard against. Recorded here because D-19 requires a row for every learning, including the ones that will never go upstream. |
| UL-012 | 2026-08-31 | 1 | A tool that reads project files without pinning an encoding dies with a stack trace instead of a verdict whenever the locale is unset | No - in scope | Pending | — | `tools/gen-review-notes.rb` read `docs/REVIEW-ARGUMENTS.md`, `notes.txt`, and `.bootstrap.env` with `File.read`/`File.foreach`, inheriting `Encoding.default_external`. With `LANG` unset - cron, launchd, a bare container, `env -i` - that is US-ASCII, and any non-ASCII byte raises `ArgumentError` from `String#match?` (line 211) or `Encoding::CompatibilityError` from `String#rstrip` (line 132). Reachable input, not exotic: an App Review contact with a diacritic, or a curly apostrophe anywhere in the notes prose. CI never caught it because `.bootstrap.env` is gitignored and absent there, so the guard short-circuits. A drift gate that crashes cannot be distinguished by its caller from one that passed. Fixed fork-side by pinning UTF-8 at every read and write site plus a regression test that clears `LC_ALL`/`LANG`/`LC_CTYPE`; upstream needs the same once the generator lands there. |
| UL-013 | 2026-09-01 | 2 | `register_app_id` hardcodes `BundleIdPlatform::IOS`, and the comment above it defers macOS registration to a `match` code path that v1.6 deleted, so the lane cannot register a macOS bundle ID at all and nothing says so | No - in scope | Merged [#279] | [#279](https://github.com/indiagrams/apple-shipkit/pull/279) | D-33. The defect has four surfaces, and changing only the hardcoded constant would have left three of them teaching the wrong model: the comment deferred to match's per-platform profile path, which v1.6 removed along with match-based CI signing (apple-shipkit#158); it cited fastlane 2.230 against a 2.238.0 lockfile; and the success line interpolated a literal iOS platform regardless of what was sent, so the log would have become the next stale comment. The lane now takes a `platform:` option (`ios` default, `macos`), validated before authenticating so a typo costs no round trip. `BundleIdPlatform` exposes exactly `IOS` and `MAC_OS` - re-read 2026-09-01 in the locked 2.238.0 gem at `spaceship/lib/spaceship/connect_api.rb:126-130` - so `UNIVERSAL` is unreachable through spaceship even though Apple's own API defines it, and `BundleIdCreateRequest` requires the field, so a value must always be supplied. Delivered by `git format-patch` into a separate clone per D-34; nothing was pushed from this repository and the `upstream` remote's push URL is still the literal `no-push`. `docs/BOOTSTRAP.md`'s claim that the lane is idempotent because `BundleId.create` rescues `ALREADY_EXISTS` was corrected in the same pull request - no code does that, and the `BundleId.all(filter:)` lookup preceding it is what actually makes a re-run safe. Merged upstream 2026-09-01 as `4dbdaa8` with all eight required contexts green (`enforce_admins` is on, so none were bypassed); the verdict moved to its terminal form in the change that observed the merge rather than at close-out. D-35 gated every bundle ID registration in this phase on this row reaching a terminal verdict, and that gate is now open. |
| UL-014 | 2026-09-01 | 2 | The template documents `fastlane match` with a separate certs repo, where v1.6 replaced it with per-run mint-and-revoke, so a forker following the documented model supplies a `GH_CERTS_REPO` that no longer drives anything | No - in scope | Pending | — | C-01. `fastlane/Fastfile` records that match-based CI signing was dropped in v1.6 (apple-shipkit#158) because match needed a separate certs repo, two extra secrets, and broke for legacy WWDR-G3-issued certificates on macos-15-arm64; `.github/workflows/release.yml` states outright that there is no certs repo dependency. The configuration surface did not follow the code: `.bootstrap.env.example` and `bin/init-bootstrap-env.sh` still prompt for `GH_CERTS_REPO` and `MATCH_PASSWORD_FILE`, and `bin/refork-smoketest.sh` still reads them. That is the worst shape a dead config key can take - a forker supplies a value, nothing raises, and nothing happens - and it is how this project's own planning notes came to carry a signing model the code had already abandoned. Found by reading the signing path while preparing UL-013 rather than by being bitten, so it is recorded before it costs anyone a session. Upstream shape: either remove the dead keys, or annotate them at the point of prompting with the version that stopped reading them. |
| UL-015 | 2026-09-01 | 2 | The fix in UL-013 still misreports an App ID's platform, one indirection further along: `register_app_id`'s success line interpolates the **requested** platform rather than what Apple returned, and its new mismatch warning fires on every re-run against a `UNIVERSAL` record while advising a second, unreclaimable bundle identifier | No - in scope | Merged [#280] | [#280](https://github.com/indiagrams/apple-shipkit/pull/280) | R-10. Found by 02-06 actually registering the two App IDs rather than trusting the lane's output. Apple returns `UNIVERSAL` whatever platform was requested: all three App IDs on this fork's team read back that way from `GET /v1/bundleIds`, including the pre-existing smoketest App ID that predates this work (the identifiers themselves are recorded in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values live). Verified four ways before being written down — raw probe JSON, `BundleId.all` through spaceship's object mapping, an independent read, and the comparator discriminating (`identifier` passed while `platform` failed on the same record) — and `name` came back exactly as sent, so the request arrived intact. Two surfaces, both introduced by #279. The success line prints the caller's own input back at them, which is the same shape as the literal `(IOS)` that #279 removed and which #279's own commit message called out as "the next stale comment". Worse, the `already registered` branch now emits, verbatim on every re-run, *"is registered for UNIVERSAL, not the requested MAC_OS. An App ID's platform cannot be changed after creation; a separate bundle ID is needed for MAC_OS"* — a bundle identifier binds permanently to a team on first upload, so that advice spends something unreclaimable over a mismatch that does not exist. Fixed upstream by reporting Apple's answer next to the request and treating `UNIVERSAL` as covering the requested platform; the genuine `IOS`-when-`MAC_OS`-was-asked case keeps the old warning. **Deliberately unsettled and stated as such in the patch, the changelog and the PR body:** whether Apple ignores `platform` at create time or normalises the record afterwards. A read-back cannot tell them apart and settling it costs a third App ID that can never be reclaimed (T-02-32), so the code handles `UNIVERSAL` when Apple returns it and claims nothing about when Apple will. Delivered by `git format-patch` into the separate clone per D-34, from a throwaway branch that was deleted after the patch was produced — this fork's `fastlane/Fastfile` still carries only upstream's merged content, and the `upstream` remote's push URL is still the literal `no-push`. Fork-side read-back assertions were left failing rather than widened; that is UL-013's lesson applied to itself. Merged upstream 2026-09-01 as `212b4892` — `gh pr view 280` reports `state: MERGED` with `mergeCommit.oid` equal to that hash, seventeen checks green including all eight required contexts, and `enforce_admins` is on upstream so none were bypassed. The verdict moved to its terminal form in the change that observed the merge rather than at close-out, which is the same discipline UL-013 followed. The merged commit was then brought into this fork with `git cherry-pick -x` through the fetch-only remote, exactly as `4dbdaa8` was; the `register_app_id` lane body here diffs empty against `212b4892:fastlane/Fastfile`, and `git remote get-url --push upstream` still returns the literal `no-push`. The fork-side read-back assertions were still not widened — a lane that stops misreporting the platform does not make Apple stop storing `UNIVERSAL`, and those two are separate facts. |
| UL-016 | 2026-09-01 | 2 | `bootstrap_asc`'s not-found remediation walks the reader through creating a single App Store Connect record with both platforms ticked, which is the Universal Purchase shape, without naming it as such or saying that the shape is close to permanent — and in the same breath tells them the app name is free to choose, which it is not | No - in scope | Pending | — | C-10. The lane's `UI.user_error!` block prints a numbered recipe whose step 2 reads `Platforms: iOS + macOS`. Quoted here as the defective string, not as guidance: a forker who wants two independent records, one per platform, must not follow it. The text nowhere says that ticking both platforms selects Universal Purchase, that Universal Purchase makes both platforms share one bundle identifier, or that unwinding the choice later means deleting the record — which permanently burns its SKU, since a removed app's SKU cannot be reused in the same organization. A structural, near-irreversible choice is presented as a form-filling step. **Recorded honestly: this fork ended up in exactly the shape that text describes.** Universal Purchase was adopted mid-phase after Apple refused a second record, so the instruction is now accidentally right for this repository. That does not make it correct. It was never written against a decision — it names one option and gives the reader nothing to decide with — so it would still silently produce the wrong structure for the fork the next reader is building, and being coincidentally right for one fork is not a property worth preserving upstream. The second defect in the same block is the sharper one and was found by being bitten: step 2 annotates the name field `SmokeApp (or whatever; verify uses bundle_id, not name)`. The parenthetical is true about what the lane verifies and false about what Apple permits. App Store names are unique **per account**, not merely store-wide, so "or whatever" is not free — a name already used by another app in the same account is refused outright, and Apple's refusal is the only place that constraint surfaces because there is no availability-query endpoint to ask in advance. This phase hit that refusal creating its second record and had to reverse a Phase 1 decision to get past it; the record ids and identifiers involved are in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values live. Upstream shape: state which structure the recipe produces and what it costs to change, offer the per-platform alternative alongside it, and replace the "or whatever" with the account-uniqueness constraint plus the fact that it can only be discovered at creation. Distinct from UL-014, which is dead `match` configuration. It is also no longer paired with C-03 — that correction flagged `.bootstrap.env.example` and `fastlane/Appfile` for asserting a shared bundle identifier against a fork that had declined Universal Purchase, and adopting Universal Purchase dissolved the conflict rather than resolving it. The two were always separate defects: C-03 was a fork-versus-template disagreement that a fork decision could end, and this row is a template defect that no fork decision touches. Verdict `Pending`: no pull request was opened for it in this plan, so the row is filed at the point the learning landed rather than held back until there is a PR to point at. |
| UL-017 | 2026-09-01 | 2 | A shipping-guidance file states that EU Digital Services Act trader status is an account-level-only setting. Apple carries **two** independent surfaces — an account-level declaration under Business, and a per-app declaration under App Information, App Store Regulations and Permits — so a verification that reads only the account level reports green while an individual app record sits undeclared; and the same file names trader verification state labels that appear in no first-party Apple source | No - in scope | Pending | — | C-08 and R-06 as one row, because it is the same file wrong about the same subject in two ways. **The generic fix, in template terms:** a trader-status verification step must name *both* surfaces, and must not assert a UI label its author has not read off a screen. Both halves were tested against Apple on 2026-09-01 and both failed. The account-level status string is `Active` — neither of the two predicted state labels, and neither of those has a first-party source anywhere. The per-app surface is real, carries its own sentence, and is where an app's own declaration actually lives. A third instance of the same defect landed in the same session: Apple's own documentation describes the trader dialog's choice in wording that matches neither option label displayed, and the real one is a contraction — three documented-string-versus-reality failures inside one phase, which is what makes this a rule rather than an anecdote about one file. Two things a template should say outright rather than imply. First, **there is no App Store Connect API surface for trader status at any level** — an exhaustive scan of every path and every schema name in specification 4.4.1 found nothing — so this check cannot be automated and a template that leaves it looking automatable is worse than one that names it as human-only. Second, under Universal Purchase the per-app surface carries **no platform selector**, so a single declaration covers every platform version on a record; a template that assumed one declaration per platform would be wrong in the other direction, and that was observed rather than reasoned about. |
| UL-018 | 2026-09-01 | 2 | `POST /v1/reviewSubmissions` returns `201 Created` against an app record with no build and no version, and the created container can then be removed by **no API route at all** — the resource permits no `DELETE`, and the documented cancel is refused because a never-submitted submission is not in a cancellable state. Creating one is a one-way door whose only exit is submitting it to App Review | No - in scope | Pending | — | Found by 02-09 running the write it had planned as a permissions probe. The plan's premise — that a build-less, version-less record makes `POST /v1/reviewSubmissions` unable to succeed, so the probe creates no state — is simply wrong; Apple creates the container eagerly and defers every validation to the point where items are attached or the submission is submitted. Anyone reasoning "there is no build, so this write is safe" is reasoning from a model Apple does not implement. **The trap is the exit, not the entry.** `DELETE /v1/reviewSubmissions/{id}` returns `403 FORBIDDEN_ERROR` whose own `detail` reads *"The resource 'reviewSubmissions' does not allow 'DELETE'. Allowed operations are: CREATE, GET_COLLECTION, GET_INSTANCE, UPDATE"* — confirmed independently against Apple's OpenAPI specification 4.4.1, where `/v1/reviewSubmissions/{id}` carries exactly `GET` and `PATCH`. That leaves `UPDATE` as the only mutating verb, and in the locked fastlane 2.238.0 gem `UPDATE` is two different operations distinguished only by the attribute sent: `submit_for_review` sends `{ submitted: true }` (`spaceship/lib/spaceship/connect_api/models/review_submission.rb:55`) and `cancel_submission` sends `{ canceled: true }` (same file, line 62), both through `patch_review_submission` (`connect_api/tunes/tunes.rb:1181`). The cancel was issued with a body containing `canceled` and nothing else, and Apple refused it: `409 STATE_ERROR.ENTITY_STATE_INVALID`, *"Resource is not in cancellable state"*, against a container in `state: READY_FOR_REVIEW` with `submittedDate: null`. Cancellation is therefore only available to submissions that have already been **submitted** — so the sole route that would make an unwanted container removable is to send it to App Review first. Verified by read-back rather than by status code: the open-submission count on the affected record was `1` before the cancel and `1` after it. **Two distinct refusals both arrive as `403`/`409` on this endpoint family and mean unrelated things.** The `DELETE` `403` is a resource-capability refusal — the operation does not exist on the resource — and a Developer-role key, an Admin key and the Account Holder would all receive it identically. It says nothing whatever about the caller's privileges. Any probe that maps a bare status code to a permissions conclusion will read it as insufficiency and be wrong; that conflation is what makes this row worth filing rather than just the irreversibility. Note also the asymmetry that makes the shape easy to miss: the child `/v1/reviewSubmissionItems/{id}` **does** allow `DELETE`, so items can be detached from a container that itself cannot be removed. Upstream shape, in template terms: never use a `POST` against `reviewSubmissions` as a permissions or connectivity probe — Apple grants one open review submission per app and platform, so a speculative create strands that slot indefinitely — and document that `cancel_submission` is valid only after `submit_for_review`, because its name reads as a general undo and is not one. A tool offering `cancel` next to `submit` without that constraint invites exactly this dead end. Filed while the state it describes is still live on this fork's iOS record; the container remains open and this executor has no sanctioned route to retire it. |
| UL-019 | 2026-09-01 | 2 | Two files tell a reader that an at-capacity certificate mint with `--force` **revokes the oldest certificate by creation date**. No such code path exists on the mint path in the pinned fastlane, and the App Store Connect API exposes no creation date on `Certificate` to order by, so the stated behaviour is unimplementable as written as well as absent | No - in scope | Pending | — | C-07 / R-04. The defective strings are `.github/workflows/canary-local-mode.yml` (*"At-cap mint with --force revokes the OLDEST cert by creation date — could wipe out a production cert"*) and `docs/APPLE-PREREQS.md` under its per-team certificate quotas heading, which carries the same sentence. Read out of fastlane 2.238.0, the version this repository locks. **What `--force` actually does.** `cert/options.rb` defines `force` as *"Create a certificate even if an existing certificate exists"*, default `false`, env name `CERT_FORCE`. `Cert::Runner#run` uses it in exactly one place: `should_create = Cert.config[:force]`, and `unless should_create` it looks for a certificate already in the local login keychain and creates only when it finds none. So `--force` means *skip the local reuse check and always POST*, and nothing else. `create_certificate`, the only thing `run` calls afterwards, builds a signing request, calls `Certificate.create`, and writes the results to disk — there is no removal of anything anywhere on that path. **A correction to the correction, found by re-reading the source rather than trusting the note.** R-04 was originally written as *"no revocation code path exists in the cert action at all"*, and that is very slightly too strong. `Cert::Runner#revoke_expired_certs!` does exist in the same class and does call `certificate.delete!` — but it is reachable only through the `fastlane cert revoke_expired` sub-command declared in `cert/commands_generator.rb`, never from the `cert` action and never from `force`. It is worth stating precisely, because the precise version is the stronger refutation: the one revocation fastlane offers here selects certificates by `certificates.reject(&:valid?)`, which is **expired** ones, not oldest ones. So the repository is wrong twice over — about which flag revokes, and about the ordering the only real revocation uses. **The operational advice is unchanged and still correct: do not override the `--force` default.** Only the reason was wrong. `--force` still forces a create, and a create still consumes a slot against a shared certificate limit, so on a team shared with another shipping product the recommendation stands on its own feet. This distinction is the whole reason the row is worth filing rather than quietly editing: a reader who learns the false mechanism will reason from it later — they might conclude `--force` is *safe* once no certificate on the team is old, or that it is a legitimate way to recycle a slot, and both follow from the false premise while contradicting the true advice. The claim most plausibly migrated from `fastlane match --force`, which genuinely does regenerate certificates; match-based CI signing was removed from this template in v1.6, and the sentence outlived the tool it described. Not the whole comment block is wrong, and the row says so rather than inviting a blanket rewrite: `.github/workflows/release.yml` carries the same neighbouring comment but states only the safe half — at-cap without `--force` is non-destructive, and the default is no `--force`. That text is correct and should be left alone. Separately and not part of this learning: the quota figures printed beside the defective sentence in both workflow files were measured against a different Apple team than the one this fork uses, and nothing in either file except the parenthetical says so. That is filed as this fork's own re-measurement problem rather than an upstream row, because the numbers are concretions and the slot is what upstream would get. Upstream shape: delete the revocation sentence from both files, replace it with what the flag does — skips the local-keychain reuse check, always creates, therefore consumes a slot — and, if a revocation is to be documented at all, document `fastlane cert revoke_expired` by name along with the fact that it selects on expiry. Verdict `Pending`: no pull request was opened in this plan, so the row is filed where the learning landed. |
| UL-020 | 2026-09-01 | 2 | `fastlane cert` on its default `force: false` reuses a certificate already present on the machine and never calls Apple's create endpoint, and the run it produces is indistinguishable at every surface from one that genuinely created: same exit `0`, same success banner, same `CER_CERTIFICATE_ID`. Any pipeline reading a green `cert` as evidence that its signing credentials are authorized to create certificates is relying on a check that cannot fail | No - in scope | Pending | — | Found by running the mint rather than reasoning about it, and then diffing the certificate list before and after: the lane succeeded and the set of certificate ids on the team was byte-identical, so no create request was ever made. The concrete ids, the team and the dated census live in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values belong; what is upstreamable is the shape. **The mechanism, read out of fastlane 2.238.0.** `Cert::Runner#run` sets `should_create = Cert.config[:force]` and, unless that is set, calls `find_existing_cert` and creates only when it returns nil. `find_existing_cert` has **two** reuse paths, not one. The first is a fingerprint match through `FastlaneCore::CertChecker.installed?`. The second is quieter and matters more: if no identity is installed but a cached `<id>.p12` is sitting in the `cert` output directory, it imports that key and certificate and reuses anyway. So clearing the keychain does not reliably compel a create either, which is worth stating because "delete the local copy and re-run" is the obvious thing a reader does when they want to test the real path. **`CertChecker.installed?` is the union of two different `security` queries, and they answer different questions.** Signing identities come from `security find-identity -v -p codesigning`; installer certificates come from `security find-certificate -c` against the third-party Mac installer and Developer ID installer names. Installer certificates never appear under the code-signing policy at all, so anyone predicting whether an installer-certificate mint will reuse, from the `find-identity` output alone, sees nothing and predicts a create. Note also that the two queries have different strengths: `find-identity -v` lists only certificates that have a usable private key, while `find-certificate` matches on name and fingerprint without requiring one. Read from the pinned source rather than observed, and flagged as such. **What is not wrong here.** The reuse behaviour itself is the right default and is exactly what stops `cert` burning a slot against a shared certificate limit on every run. Nothing about the default should change. The defect is narrower: the outcome is not reportable. `find_existing_cert` already knows which branch it took and already prints different sentences for reuse and for create, so the information exists inside the action and is simply not exposed anywhere a caller can assert on it. `run` returns nil either way, and `CER_CERTIFICATE_ID` is set identically on both paths. **Upstream shape, stated as the problem rather than as a prescription.** Make a reuse distinguishable from a create at the action's boundary, so a pipeline that needs to know which one happened can find out without diffing Apple's certificate list itself. Whether that is a new `CER_*` output, a return value, or documentation that says plainly the exit code carries no such information is upstream's call, and this row deliberately does not pick one. What should not stand is the current position, where the distinction is computed and then discarded. **Why it is more than bookkeeping.** The question "may this key create a distribution certificate" is one you need answered on a machine where no such certificate exists — a fresh runner, a new contributor, a rebuilt keychain. That is precisely the case in which the reuse path is unavailable, so a green mint on a developer machine is evidence about the only situation that never needed evidence. Related to but distinct from UL-019, which is about `--force` being documented as revoking something it does not touch; this row is about what happens when `--force` is correctly left alone. Verdict `Pending`: no pull request was opened in this plan, so the row is filed where the learning landed rather than held until there is one to point at. |
| UL-021 | 2026-09-01 | 2 | A document that publishes a pasteable re-check command per fact accumulates commands that have never been executed, and a re-check command nobody has run is a convention rather than a mechanism. Four of the commands published in this fork's account-state document were executed for the first time at phase close and did not run: two omitted the bundle prefix their own script requires and died in a `LoadError`; one named a command-line flag that the tool does not accept and could never have run whatever values were substituted; and one was a write that permanently consumes an unreclaimable resource, published in a column headed `Re-check command`. Nothing in the repository executed any of them, so all four had been sitting there looking runnable | No - in scope | Pending | — | Filed in the change that found it, not at a later close-out. **Why this is generic rather than this fork's own untidiness.** The template already ships several documents whose value is the commands in them — `docs/BOOTSTRAP.md`, `docs/APPLE-PREREQS.md`, `docs/CONTINUOUS-VALIDATION.md`, `docs/CONTRIBUTING-UPSTREAM.md` — and nothing anywhere runs a single one. The failure mode is silent by construction: prose rots loudly, because a reader notices it contradicts something; a stale command rots quietly, because it looks exactly like a fresh one until the day someone pastes it and gets a stack trace instead of an answer, which is generally the day they most needed the answer. **The three shapes observed here, because they want different fixes.** (1) A command whose environment is not the one it will be pasted into — the pinned-bundle case; the script runs in CI under `bundle exec` and the document dropped that half. (2) A command that could never have run at all — an invented flag; this one is only detectable by execution, since it is well-formed as text. (3) A command that runs but must not be run — a create, published in a re-check column; correctness here is not about whether it executes but about whether executing it is safe, which no linter can tell you. **Upstream shape, stated as a problem rather than a prescription.** Give a fork some way to assert that the commands its documents publish still execute, and some way to mark the ones that are writes so nobody re-checks with them. Whether that is a fenced-block extractor with a smoke job, a naming convention for read-only versus mutating commands, or documentation saying plainly that these commands are unverified, is upstream's call and this row deliberately does not pick one. What should not stand is the current position, where a document's most actionable content is the only part under no guarantee at all. **Related to but distinct from UL-010**, which is about a gate whose grep matched the prose describing the gate; this row is about commands that are never run rather than checks that pass vacuously. Verdict `Pending`: no pull request was opened in this plan. |
| UL-022 | 2026-09-01 | 2 | Reversing a recorded decision updates the documents the reversing change happens to touch, and leaves the decision's citations standing everywhere else. This fork reversed `D-05` mid-phase and updated the two documents that change edited; a third tracked document went on asserting the reversed decision by name for four plans, in a file whose sentinel-delimited sections are copied verbatim into what Apple's reviewers read | Yes - out of scope | Fork-only [a discipline learning about this fork's own decision log; the template ships no decision-citation surface for a fix to attach to] | — | Found at the phase close-out by grepping every tracked document for the reversed decision's id, which is the check that should have run inside the reversing change. Recorded rather than quietly fixed, because the class of error matters more than the instance. **What made it survive.** The reversal was correct, well-reasoned and thoroughly documented — in `docs/PRODUCT-IDENTITY.md` and `docs/APPLE-ACCOUNT-STATE.md`, both of which the reversing plan already had open. `docs/REVIEW-ARGUMENTS.md` cited the same decision twice and was not in that plan's file list, so nothing prompted anyone to look. The standing `DOC-01` exit criterion — every change updates its documentation in the same change — was satisfied on its face: documentation *was* updated in the same change. It just was not all of it, and 'the documentation this change touched' and 'the documentation this change invalidated' are different sets. **Why the stale text was more dangerous than it looked.** Both stale passages sat in the file whose `id=core` block is copied byte-for-byte into `Notes for Review`. Both happened to fall outside the sentinels, verified by grepping the generated `notes.txt` for the offending sentence — so nothing false was ever going to Apple. That was luck, not design: nothing about the file's structure keeps a stale claim on the safe side of a sentinel. **Why `Out-of-scope` rather than `Pending`.** The generic form of this — reversing a decision should trigger a citation sweep — has no artifact in apple-shipkit to attach to; the template has no decision log, no decision ids, and nothing that cites them. Sending upstream a discipline with no surface to enforce it on would be a process opinion dressed as a patch. The fix that does travel is narrower and is filed separately as `UL-021`. |
| UL-023 | 2026-09-01 | 3 | The template's Team-ID mechanism writes the real value into tracked build manifests on its success path. `bin/rename.sh` Step H substitutes `TEAM_ID_PLACEHOLDER` into `app/project.yml` and `app/Project.swift`, both committed; `bin/switch-to-xcodegen.sh` re-substitutes it into a restored manifest; and `docs/APPLE-PREREQS.md`'s setup checklist instructs a forker to do the same by hand. The template ships no gitignored per-clone signing configuration, so there is nowhere else for the value to go, and a forker who follows the documented path commits their Team ID on the first push | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Three call sites: `bin/rename.sh` Step H (the `sed` over the two manifests, lines 652-660), `bin/switch-to-xcodegen.sh` line 214 (re-applying fork identity to a restored `app/project.yml`), and the `docs/APPLE-PREREQS.md` checklist item "Substitute it for `TEAM_ID_PLACEHOLDER` in `app/project.yml`". The replacement, built in this fork's Phase 3: a tracked `app/Identity.xcconfig` holding the four identity variables and ending in an optional include (`#include? "Local.xcconfig"`) of a gitignored `app/Local.xcconfig` that holds `DEVELOPMENT_TEAM` alone. The include is silent when the file is absent, the `.gitignore` rule lands before the file is created so it is never untracked-and-visible, and the named failure comes from a preflight rather than from the build. Filed in the change that built the layer. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (squash of apple-shipkit #281, 39 files) — `gh pr view 281` reports `state: MERGED` with `mergeCommit.oid` equal to that hash, the merge commit read separately touches `app/Identity.xcconfig`, `.gitignore`, `bin/preflight-identity.rb`, `ci/check-identity.sh`, `bin/rename.sh`, `bin/switch-to-xcodegen.sh` and `docs/APPLE-PREREQS.md`, and all eight required contexts were green on the head (`enforce_admins` is on, so none were bypassed). All three call sites closed upstream: Step H writes gitignored `app/Local.xcconfig` instead of sed-ing the manifests, the `switch-to-xcodegen.sh` re-substitution block is gone, and the checklist item now names the xcconfig. Upstream's `Identity.xcconfig` carries placeholders only, per UP-05. The verdict moved to its terminal form in the change that observed the merge rather than at close-out, as UL-013 and UL-015 did. |
| UL-024 | 2026-09-02 | 3 | The template hardcodes its own application name into the scripts and configuration that check and build it. `ci/local-check.sh`, `ci/local-release-check.sh`, `fastlane/Snapfile`, `fastlane/MacSnapfile` and `.gitignore` all spell out `app/<AppName>.xcodeproj`, the `<AppName>-iOS` and `<AppName>-macOS` schemes, and `build/<AppName>-<version>.ipa` and `.pkg`, so every fork has to substitute a literal across the build system rather than set one value — which is the whole reason `bin/rename.sh` has to sed the build system at all | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Twenty-five occurrences across the five files here (6, 13, 2, 2 and 2). The fix is a generic project constant with nothing to resolve: the project is `app/App.xcodeproj`, the schemes are `App-iOS` and `App-macOS`, and the release artefacts are `build/App-<version>.ipa`, `build/App-<version>.pkg`, `App-iOS.xcarchive` and `App-macOS.xcarchive`. A constant rather than a name derived from `PRODUCT_NAME`, because a derived name reintroduces exactly the variable the constant removes, and the artefact name is build plumbing rather than product identity — the product name lives in the tracked xcconfig that UL-023 describes. This is the precondition for retiring the rename machinery entirely. Applied fork-side in this change as a forward-port. Upstream's copies of the same five files say a different name (`HelloApp`, in 45 files), so a patch generated here has every hunk rejected by `git am` on its context lines; the upstream change is authored by hand against upstream's own text, which [CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md) section 4 already allows. The same upstream change should drop the `vars.APP_NAME` indirection in the pull-request workflow, which is deliberately not forward-ported here. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281, hand-authored against `HelloApp` in a fresh clone as section 4 allows; `git am` was not attempted), eight required contexts green, `enforce_admins` on. Upstream's five files, its `Makefile`, `Brewfile`, `bin/switch-to-*.sh`, `bin/take-readme-screenshots.sh` and the Fastfile's scheme/artefact constants now spell the constant `App`, and this fork's 03-06 forward-port converges with upstream's text. The `vars.APP_NAME` indirection went with it — see UL-025. *Residue noted 2026-09-02 (03-REVIEW IN-07):* this fork's template-owned `Makefile:78` still advertises `Regenerate SmokeApp.xcodeproj from app/project.yml` under `make help` — the one `SmokeApp` project path a user is still told about. Upstream already says it right: `git show upstream/main:Makefile` (`c6c324f`) line 78 reads `Regenerate app/App.xcodeproj from app/project.yml`, so there is nothing to propose upstream; the line converges when the fork next takes the template's `Makefile` (a refork replaces it wholesale). Not edited here: the `Makefile` is template-owned and the fork adds nothing to it. |
| UL-025 | 2026-09-02 | 3 | The template's pull-request workflow resolves the Xcode project path and scheme from a repository variable, with the template's own application name as the hardcoded fallback operand, and it does so inside every required build context. A fork that renames and never sets the variable gets the template's name back silently, and a fork that renames again after setting it gets its previous name back, so the failure presents as six unmergeable pull-request checks rather than as a configuration error that names the variable | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Six interpolations in three `xcodebuild` steps of `.github/workflows/pr.yml` (lines 247-248, 268-269, 285-286 in this fork's copy), one per platform, multiplied across a two-generator matrix into six required status contexts; `release.yml` (line 105) and `canary-local-mode.yml` (line 109) read the same variable at their workflow-level `env:` with no fallback at all, so a rename that never sets the variable leaves those two workflows with an empty name rather than the wrong one. Nothing about the project's name needs a variable once it is a structural constant (UL-024): the fix is `app/App.xcodeproj` and `App-iOS` / `App-macOS` spelled in the workflow with no `vars.` lookup and no fallback, which also removes the only reason the bootstrap step has to set the variable at all. Fork-side, this repository's variable was set to the constant behind a blocking checkpoint and read back typed, because the workflow is template-owned and is not forward-ported here. Decided at that checkpoint on 2026-09-02: dropping the `vars.APP_NAME` indirection from the pull-request workflow is in scope for the upstream pull request that carries UL-024, authored by hand against upstream's own text. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281): `git show upstream/main:.github/workflows/pr.yml` now contains zero `vars.APP_NAME` references and spells `app/App.xcodeproj` / `App-iOS` / `App-macOS` in all six cells, with an `identity preflight` step in the `config` job. Scope kept to the pull-request workflow as decided: `release.yml` (line 105) and `canary-local-mode.yml` (line 109) were not in the 39 files and still read the variable at their workflow-level `env:` — a stated remainder for a follow-up, not a silent one; with UL-026 still open that is where the re-set can still bite. |
| UL-026 | 2026-09-02 | 3 | The fork-bootstrap and refork paths re-set the same repository variable from a config file during a routine operation, so a correct live value is silently reverted by the next `make bootstrap-fork`, and by a refork, with no warning, no diff and no gate — the reversion is invisible until the next pull request's required checks go red | No - in scope | Pending | — | Two call paths converging on one write. The write is `bin/lib/bootstrap.rb` line 595, `Sh.run!("gh", "variable", "set", key, "--body", val, "--repo", …)` inside `GHSecrets#do_it`, iterating `REQUIRED_VARIABLES = %w[APP_NAME BUNDLE_ID]` (line 546) from `.bootstrap.env`; `bin/bootstrap-fork.rb` is the entry point (`make bootstrap-fork`) and `docs/BOOTSTRAP.md` line 152 documents it as step 10 `GHSecrets`. The second path is `bin/refork-smoketest.sh`, which hardcodes `APP_NAME=SmokeApp` at line 101, writes it into the new clone's `.bootstrap.env` at line 239, and tells the operator to run `make bootstrap-fork` at line 273 — the refork script does not itself call `gh variable set`, which corrects the sentence at `CHANGELOG.md` line 161 that says it does. The comment above `bootstrap.rb`'s constant admits the mechanism: "PR CI also breaks silently without these". A step that silently overwrites live repository state on every re-run is the same shape as UL-020's success banner: exit 0 either way, no evidence the value is the one CI needs. With UL-025 landed the variable has no reader in the pull-request workflow and the re-set becomes harmless; until then the step should read the live value first and refuse, or at least say, when it is about to change it. |
| UL-027 | 2026-09-02 | 3 | XcodeGen derives a unit-test bundle's `TEST_HOST` (and `BUNDLE_LOADER`, which it sets to `$(TEST_HOST)`) from the host target's *name* at generation time, writing `$(BUILT_PRODUCTS_DIR)/<TargetName>.app/<TargetName>` into the pbxproj. The moment a manifest decouples the app's `PRODUCT_NAME` from its target name — by sourcing it from an xcconfig, which is what UL-023 and UL-024 propose — the executable is built at the product-name path, the test bundle is told to load the target-name path, and every `xcodebuild test` fails with `Could not find test host` while `xcodebuild build` stays green and the generator exits 0. Tuist derives the same path from `productName:` and does not break, so a two-generator matrix goes half red on a tree with no manifest diff between the generators to explain it | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Observed on this fork's pull request: two consecutive runs, the two XcodeGen test cells red on the same line (`Could not find test host for <UnitTests>: TEST_HOST evaluates to …/<AppTarget>.app/<AppTarget>` on iOS, `…/<AppTarget>.app/Contents/MacOS/<AppTarget>` on macOS, exit 70) while the device-build cell and all three Tuist cells were green; reproduced locally on Xcode 26.1.1 with XcodeGen 2.46.0 by `-showBuildSettings`: the unit-test targets' `TEST_HOST` ended in the target name while the app targets' `EXECUTABLE_PATH` ended in the product name. The raw pbxproj confirms XcodeGen writes the target name literally; the UI-test bundles are unaffected because they carry `TEST_TARGET_NAME` (a target name, correct as is) and no `TEST_HOST`. **The fix, two lines per unit-test target in `app/project.yml`:** `TEST_HOST` spelled from the product-name variable (`$(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(APP_PRODUCT_NAME)`, with `Contents/MacOS/` on macOS) and `BUNDLE_LOADER: $(TEST_HOST)` — the shape Tuist already emits, so the two manifests stay 1:1 at the resolved-settings level; `app/Project.swift` needed no change. **Why it survived the rename plan:** that plan built and listed but never ran `xcodebuild test`; the pre-push hook builds the iOS device target only; and the cross-generator parity tool compares bundle id and product name, not `TEST_HOST`, so parity read equal on the keys it reads. A parity check that means to catch generator drift should include `TEST_HOST` — recorded here for the phase's close-out audit rather than widened in the same change. **Upstream shape:** whenever upstream moves product identity into an xcconfig (UL-023), its `project.yml` needs these two lines on each unit-test target, and its `pr.yml` matrix is the only instrument that would notice their absence — a `build`-only local check cannot. The root cause is arguably XcodeGen's (deriving the host path from the target name rather than from a `PRODUCT_NAME` the manifest sets), and is worth an issue there, but the template can fix it in its own manifest today. **A local-proof pitfall met on the way, already documented by the template at README.md line 233:** under `CODE_SIGNING_ALLOWED=NO` the macOS UI-test runner is unsigned and this Mac's Gatekeeper kills it (`Test crashed with signal kill before establishing connection`) where GitHub's runners do not; local runs are therefore scoped with `-only-testing:<UnitTests>` and the pull-request run is the arbiter for the UI targets. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281, carried in the same pull request as UL-023/UL-024 because the xcconfig-sourced `PRODUCT_NAME` is what makes the two lines necessary): `git show upstream/main:app/project.yml` carries `TEST_HOST: $(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(APP_PRODUCT_NAME)` and `BUNDLE_LOADER: $(TEST_HOST)` on the iOS unit-test target and the `Contents/MacOS/` form on macOS, with a comment stating why; the fork-side source is commit `23c7124`. The XcodeGen-side issue named above is not filed — the template fixes it in its own manifest, which is the row's scope. |
| UL-028 | 2026-09-02 | 3 | A generation gate can be silently absent on both generators the template supports, for two different reasons, and neither absence produces an error. Tuist caches manifest evaluation keyed on the manifest's own content, so a guard embedded in `Project.swift` is skipped whenever only the guard's subject — the xcconfig it exists to check — changes: the one case the guard is for is the case it structurally cannot fire in. XcodeGen's `options.preGenCommand` does fire, but its own documentation states it does not run when the project is not regenerated under the cache flag, so that gate is one `--use-cache` away from bypass. In both absent states `generate` exits 0 and writes a project whose bundle identifier resolves to nothing | No - in scope | Pending | — | Observed on this fork 2026-09-02 with Tuist 4.205.0 and XcodeGen 2.46.0: with a required key removed from the identity xcconfig, `xcodegen generate` exited 3 naming the key and wrote no project; `tuist generate` on the same input exited 0, wrote the project, and `-showBuildSettings` on it printed no `PRODUCT_BUNDLE_IDENTIFIER` line at all (a setting that resolves to the empty string is omitted from the dump) while the pbxproj carried the unresolved reference twelve times. Earlier in the same phase a manifest-embedded guard was watched not running after only the xcconfig changed, and running only after a byte was appended to the manifest itself. **Mitigation, all three parts needed:** (1) the pre-generation hook on the generator that has one; (2) a CI step that runs the same preflight as plain text on every pull request regardless of which generator produced anything — on this fork the `review notes` job, which is why that step is the Tuist path's only coverage; (3) an assertion in the fork's test suite that no caller under `Makefile`, `ci/` or the workflows passes the cache flag, proven load-bearing by adding `--use-cache` to a Makefile line, watching the assertion fail naming the line, and restoring. UL-027 is the same asymmetry seen from the other side: the two generators are 1:1 on the keys a parity tool reads and different in what they do around generation. **Upstream shape:** the template's pull-request matrix runs both generators, so the text-level preflight belongs in a job that runs for both, and the cache-flag assertion belongs beside it; a manifest-embedded guard should be documented as an anti-pattern rather than offered as the Tuist equivalent. Verdict `Pending`, deliberately, after apple-shipkit #281 merged 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b`: parts (1) and (2) went upstream in that change — `options.preGenCommand: ruby ../bin/preflight-identity.rb` on the XcodeGen manifest and `ci/check-identity.sh` as the `identity preflight` step of `pr.yml`'s `config` job, which runs before every app cell on both generators — but part (3), the cache-flag assertion, was not carried because upstream has no fork-shaped test suite to hold it, and the anti-pattern note went in as a manifest comment rather than a documentation section. The row stays non-terminal for that half rather than being closed on two-thirds of a mitigation. |
| UL-029 | 2026-09-02 | 3 | The template's `.tool-versions` pin file drifts from every installed tool on a machine that installs through `Brewfile`, and nothing reports the drift, so behaviour observed locally is not the behaviour a pin-honouring runner would get. On this fork five of the six pins differ from what is installed: ruby 3.3.11 pinned, 3.3.12 installed; xcodegen 2.45.4 pinned, 2.46.0 installed; lefthook 2.1.6 pinned, 2.1.12 installed; swiftlint 0.63.2 pinned, 0.65.1 installed; swiftformat 0.61.1 pinned, 0.62.1 installed; only xcbeautify 3.2.1 matches. Tuist and Xcode are not pinned there at all. Every generator behaviour this fork recorded — the pre-generation hook, xcconfig attachment, generator parity, the built-plist values — was observed on the installed versions, not the pinned ones | No - in scope | Pending | — | Two remedies exist: verify against the pin (install the pinned versions and re-run the observations), or refresh the pin to what is installed and carry that change upstream, since `.tool-versions` is a template file. **This phase did neither in code.** It recorded the installed version beside every observation, in its evidence transcripts and in the identity document's measurement section, and left the pin as found: a pin edit is an upstream change, and the pin's own consumers (asdf, mise) are not what `make bootstrap` uses — `Brewfile` is, which is why the divergence is invisible to the daily workflow and why refreshing the pin locally would fix a file nothing here reads. The refresh is the upstream-shaped remedy and this row is its vehicle; the verify-against-pin run is deferred to the phase's close-out audit and is listed there as an accepted gap, not a silent one. **Upstream shape:** either drop `.tool-versions` or have `make bootstrap` (or `make doctor`) diff it against the installed versions and warn, so the two sources of truth cannot silently diverge; and pin the generator whose behaviour the template relies on, or state that it is unpinned by design. Verdict `Pending`: no pull request was opened in this change. |
| UL-030 | 2026-09-02 | 3 | On the template's iOS app target the copyright travels as the `INFOPLIST_KEY_NSHumanReadableCopyright` build setting while the target's Info.plist comes from the manifest's `info:` (XcodeGen) / `infoPlist:` (Tuist) block, and Xcode writes `INFOPLIST_KEY_*` settings into the bundle only when it generates the plist itself (`GENERATE_INFOPLIST_FILE = YES`). With a file-backed plist the setting resolves in `-showBuildSettings`, both generators exit 0, the build succeeds, and the built bundle carries no `NSHumanReadableCopyright` at all; the only instrument that shows it is `PlistBuddy` on the built artefact. The macOS target sets the plist key directly and is correct, so the two platforms silently disagree on where the copyright lives | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Observed on this fork 2026-09-02 with XcodeGen 2.46.0, Tuist 4.205.0 and Xcode 26.1.1 (17B100): an unsigned Release iOS Simulator build from the XcodeGen project answered `Print: Entry, ":NSHumanReadableCopyright", Does Not Exist` (exit 1) while the same build's `-showBuildSettings` printed the setting resolved to the full string and `GENERATE_INFOPLIST_FILE = NO`, `INFOPLIST_FILE = iOS/Generated-Info.plist`. **The fix, one line per manifest:** `NSHumanReadableCopyright` added to the iOS target's plist block of both manifests in the shape the macOS block already uses, and the inert `INFOPLIST_KEY_NSHumanReadableCopyright` line dropped from both — after it, the XcodeGen and the Tuist iOS builds both read the full string with the `©` as bytes `c2 a9`, the macOS build unchanged; removing the key again reproduced `Does Not Exist`, restoring it brought the value back. **Why it survived four plans:** the cross-generator parity tool compares build settings, and the setting was equal on both sides, so parity read green on a value that never reached the bundle; the pre-push hook and the pull-request matrix build and test but read no plist; and no iOS surface displays the key (the About box is macOS), so nothing user-visible was wrong. A parity check that means to catch this must read the built plist, or compare the xcconfig variable rather than the `INFOPLIST_KEY_` setting — recorded here for the close-out audit rather than widened in the same change; with the setting gone the tool's key list now has a second symmetric absence beside `DEVELOPMENT_TEAM`. **The same mechanism, recorded and not fixed here:** the iOS target also carries `INFOPLIST_KEY_LSApplicationCategoryType`, and the built iOS plist has no `LSApplicationCategoryType` either (observed in the same session). **Upstream shape:** the template's `app/project.yml` and `app/Project.swift` carry the same inert line on the iOS target and the plist key on macOS, so the fix is the same two edits there, and the template's `INFOPLIST_KEY_*` convention only holds for its test targets, which do set `GENERATE_INFOPLIST_FILE: YES`. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281): `git show upstream/main:app/project.yml` and `:app/Project.swift` each carry `NSHumanReadableCopyright` as `$(COPYRIGHT)` in the iOS plist block (lines 81 and 57) alongside the macOS one, and the inert `INFOPLIST_KEY_NSHumanReadableCopyright` line is gone from both; the fork-side source is commit `b8b1ac9`. `INFOPLIST_KEY_LSApplicationCategoryType` was left as found upstream and named in the pull request body. |
| UL-031 | 2026-09-02 | 3 | A presence gate over an xcconfig that decides "non-empty" with a bare non-space match after the `=` accepts a value that is only a `//` comment. In xcconfig `//` opens a comment at any position in the value, so `KEY = // disabled` resolves to the empty string in Xcode — the key is simply absent from `-showBuildSettings` — while the gate matches the first `/` and exits 0. That is the exact input the gate exists to refuse, reached by the most natural way of disabling a line, and a test that asserts the same property with the same regex passes with it: the gate and its guard fail together | No - in scope | Pending | — | Found by this fork's Phase 3 security audit (T-03-06 / F-01, also 03-REVIEW WR-01) and reproduced against a fixture: `BUNDLE_ID = // temporarily disabled` printed `identity preflight ok`, exit 0, under ruby 3.3.12 and 4.0.6. Xcode's behaviour was then observed rather than inferred — probe keys appended to the identity xcconfig and read back through `xcodebuild -showBuildSettings` on Xcode 26.1.1 (17B100), XcodeGen 2.46.0: `KEY = // x` and `KEY = //` resolve to "" (absent from the dump); `KEY = value // note` resolves to `value`; `KEY = https://example.com/x` resolves to `https:` because the comment opens inside the URL; a lone `/` resolves to `/` and `a/b` to `a/b`. **Fix, one predicate body in two files:** cut the value at the first `//`, then require a non-space character — and nothing tighter, because a single `/` is a real character and a copyright string may legitimately carry one. Driven red first on the unfixed code (three comment shapes exit 0), then green (exit 3 naming the variable; a real value with a trailing comment still exit 0), through the generator (`xcodegen generate` exit 3, no project written) and in the guard (red naming the file and the variable on the same input the unmodified guard had ticked). Filed in the same change as the fix. **Upstream carries the same predicate:** `git show upstream/main:bin/preflight-identity.rb` (`c6c324f`, apple-shipkit #281) has `def defines_non_empty?` at line 98 with `[ \t]*=[ \t]*\S` at line 99 — the preflight went upstream in #281 with this hole in it, and #281's `ci/check-identity.sh` runs it on every pull request. Upstream-bound as a follow-up pull request; not opened in this change, hence `Pending`. |
| UL-032 | 2026-09-02 | 3 | A release script that derives the bundle identifier by grepping the generator manifest's `PRODUCT_BUNDLE_IDENTIFIER:` line stops working the moment the manifest carries a `$(VAR)` reference instead of a literal — which is exactly the state the template's own identity move (UL-023) puts the manifest in. The grep then yields the unresolved reference `$(BUNDLE_ID)` as if it were a value, the `-z` emptiness guard beside it can never fire, and on the manual-signing path the string is written as the `provisioningProfiles` dict key of the ExportOptions plist, so `exportArchive` fails only after a full signed archive. The script's own documented direct invocation is exposed; the Fastfile's release lane is shielded only because it happens to pass `RELEASE_BUNDLE_ID` | No - in scope | Pending | — | Found by this fork's Phase 3 code review (03-REVIEW WR-02) and security audit (F-02) and reproduced on the real script with no signing flags: under `bash -x`, `ci/local-release-check.sh v0.0.0` assigned `BUNDLE_ID='$(BUNDLE_ID)'` and exited 0 — a check that structurally cannot fail, the shape this phase exists to eliminate; it also overwrote the value `.bootstrap.env` had just exported. **Fix, applied fork-side in this change as a forward-port:** read `BUNDLE_ID` from `app/Identity.xcconfig` as Xcode reads it (last assignment wins, `//` comment cut off), and reject an empty value or any `$(` reference with a named failure before any archive. Driven red first (the unresolved reference, exit 0), then green on the real value, then red on `RELEASE_BUNDLE_ID='$(BUNDLE_ID)'` and on a commented-out `BUNDLE_ID` (exit 1 naming the file, before the `xcodegen generate` step), each mutation restored. **Upstream carries the same line:** `git show upstream/main:ci/local-release-check.sh` (`c6c324f`, apple-shipkit #281) still greps `app/project.yml` at its line 145 while #281's own `app/project.yml` line 97 says `$(BUNDLE_ID)`, so the identity move went upstream with its release script still reading the manifest. `ci/` is template-owned here, so this is the D-46 reconciliation's forward-port half, paired with an upstream change authored against upstream's text; upstream-bound as a follow-up pull request, not opened in this change, hence `Pending`. |
| UL-033 | 2026-09-02 | 3 | When a template moves the app's identity out of the build manifests into one tracked xcconfig, the agent-facing instructions that tell a forker *where the value lives afterwards* are the citations most likely to be left standing: the change updates the passages it touches (entitlement paths, the direct test invocation, the project-path antipattern) and leaves the invariants-table row that says "afterward, the value is set in `.bootstrap.env`" — so an agent reading top-down gets two incompatible instruction sets in one file, the UL-022 shape again | No - in scope | Pending | — | Found by this fork's Phase 3 code review (03-REVIEW WR-06): five passages in `AGENTS.md` still named `<AppName>.entitlements`, a `<AppName>.xcworkspace` / `<AppName>-iOS` scan invocation and a `<APP_NAME>.xcodeproj` project path after the phase had made the project the constant `App` and put identity in `app/Identity.xcconfig`. Read against upstream rather than assumed: `git show upstream/main:AGENTS.md` (`c6c324f`, apple-shipkit #281) had already corrected four of the five — the entitlements row and the capability step (`app/iOS/App.entitlements`, "Constant paths; the rename does not touch them"), the unit-test invocation (`--project app/App.xcodeproj --scheme App-iOS`), the antipattern row ("Deriving the project path or scheme from `APP_NAME`") — and added an "App identity" row pointing at `app/Identity.xcconfig`. **Fork-side in this change:** those four passages and the new row aligned to upstream's wording byte for byte (convergence, not invention), and the fork-conventions section below the marker untouched. **What upstream did not correct, and this row carries:** the invariants-table row (`Do not sed-substitute … literals`) still ends "afterward, the value is set in `.bootstrap.env`" while `bin/rename.sh` upstream now writes `app/Identity.xcconfig`; corrected here to say identity is edited in `app/Identity.xcconfig` and only fastlane's `APP_NAME` / `BUNDLE_ID` handle stays in `.bootstrap.env`. Upstream-bound as a one-line follow-up alongside UL-031 / UL-032; not opened in this change, hence `Pending`. |
