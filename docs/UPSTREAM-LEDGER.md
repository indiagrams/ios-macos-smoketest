# Upstream learning ledger

The record of what this fork learned by shipping for real, what happened to each
learning, and — for the ones that will never go upstream — why not.

## Why this exists

This fork exists to walk the shipping path and find out where the template hurts. Every
generic shipping learning is proposed upstream to
[apple-shipkit](https://github.com/indiagrams/apple-shipkit) **when it is hit**, not
batched into a cleanup pass at the end of the project. Batching is how ledgers die: a row
written three phases after the fact is a reconstruction, not a record.

Every learning gets a row — **including the ones that will never go upstream**. A
learning that fails the [SCOPE.md](../SCOPE.md) test, or that is a fork concretion rather
than a generic capability, is still recorded, with the reason it was ruled out. That is
what makes the scope gate auditable instead of a matter of memory, and it is what makes
this project's close-out a mechanical check rather than an archaeology project.

**A row is added in the same change as the learning that produced it.** That rule, plus
the rule that documentation is updated in the same change, are standing exit criteria on
*every* phase of this project — not just the phase that created this file.

How a learning gets from here to upstream is [CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md).
Short version: there is no fork relationship and no cross-repo PR, so it travels by
`git format-patch` into a separate clone of apple-shipkit.

## Verdict vocabulary

Six values, closed set. Three are terminal. Adapted from the Yocto Project's
`Upstream-Status:` patch tag, which exists to answer exactly the question D-19 asks:
*why was this deliberately not sent upstream?*

- `Pending` — identified, not yet proposed. **Non-terminal.**
- `Submitted [#N]` — PR open upstream. **Non-terminal.** The PR number is required.
- `Merged [#N]` — landed in apple-shipkit. **Terminal.** The PR number is required.
- `Denied [reason]` — proposed, upstream declined. **Terminal.** Reason required in brackets.
- `Out-of-scope [reason]` — fails the SCOPE.md test. **Terminal.** Reason required in brackets.
- `Fork-only [reason]` — passes the SCOPE.md test but is a fork concretion; upstream gets
  the slot, never the value. **Terminal.** Reason required in brackets.

Do not invent a seventh value. A vocabulary that grows mid-project stops being greppable,
which is the only property this column has.

## Close-out contract

Closing this project out is defined as: **no row is left non-terminal.** That is one
command, not a review meeting:

```bash
grep -cE '^\| UL-[0-9]{3} \|.*\| (Pending|Submitted)' docs/UPSTREAM-LEDGER.md
```

It must read `0` at close-out. If it does not, every remaining non-terminal row must be
listed explicitly with the reason it is still open — an accepted, stated exception, never
a silent one.

## Row conventions

- **IDs are `UL-NNN`, monotonically increasing, and never reused.** A retired or mistaken
  row keeps its ID and gets a terminal verdict explaining what happened. Stable IDs mean a
  row can be cited from a commit message or an upstream PR body and still resolve later.
- **`SCOPE test`** holds the literal answer to SCOPE.md's question — `No - in scope` or
  `Yes - out of scope`. Cite the test's sentence, not its path literal; the path moves
  across template versions.
- **`PR`** is an em dash until a PR exists.
- **No fork concretion in any cell** — no Team ID, no contact address, no secrets. Values
  that belong to this fork live in this fork's own docs and are referenced from here.

## Hazard: this file can be deleted wholesale

`bin/refork-smoketest.sh` runs `gh repo delete` followed by `gh repo create --template`.
Running it **destroys this repository and recreates it from the template**, taking this
ledger with it — along with every other fork-owned file this project produced. Nothing in
this file survives that, because remotes, untracked state, and fork-owned docs alike are
all recreated from the template rather than preserved.

This is recorded as a risk, not fixed here; fixing it is outside this phase's scope. Two
consequences worth carrying forward:

- It is the real argument for upstreaming promptly. A fork-side tool that has been
  upstreamed survives a refork. One that has not, does not.
- "This ledger's content has been upstreamed or archived" is a close-out item, not an
  afterthought.

## Ledger

Rows UL-007 through UL-011 were added in the phase close-out rather than in the change that produced each learning, which is late by this project's own rule ([CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md) section 5). Each of them says so in its Notes cell and names the commit that carried the learning, rather than being back-dated to look timely. A late row that admits it is late is still a record; a back-dated one is not. From Phase 2 on, the row goes in the same change.

| ID | Date | Phase | Learning | SCOPE test | Verdict | PR | Notes |
|----|------|-------|----------|------------|---------|----|-------|
| UL-001 | 2026-08-31 | 1 | Review-notes generator plus its drift gate — `tools/gen-review-notes.rb`, `test/gen_review_notes_test.rb`, `test/docs_structure_test.rb`, `.github/workflows/review-notes.yml` | No - in scope | Pending | — | D-14. Tooling around the project, not app code. All four files now exist and the workflow's job name, `review notes`, is a required status check on `main`. The generator is dependency-free — zero `require` statements — so the upstream port needs no Gemfile change. Upstream shape: rename to `bin/gen-review-notes.rb`, add a `ci/check-review-notes.sh` wrapper matching the existing `ci/check-app-icon.sh` convention, and add a job to upstream's `pr.yml`. |
| UL-002 | 2026-08-31 | 1 | An upstream-learning-ledger template — this file's structure, vocabulary, and close-out contract | No - in scope | Pending | — | D-20. Upstream gets the structure; this fork's entries stay here. The schema is a generic capability, the rows are fork concretions. |
| UL-003 | 2026-08-31 | 1 | `bin/setup-github.sh` recomputes and PUTs the required-status-checks array from scratch, silently destroying any fork-added required check on the next run | No - in scope | Pending | — | It should preserve unknown checks, or read an extra-checks list from config, rather than assuming it is the only writer. Affects Phase 4's IDENT-11 gate as much as this phase's drift check. Now load-bearing rather than theoretical: 01-06 made `review notes` the ninth required check, so any `make setup-github` run silently returns protection to eight and the gate stops gating while still reporting. Recovery runbook, verified idempotent, is `CONTRIBUTING-UPSTREAM.md` section 6; verify by context count, not by looking at the Actions tab. **The fork-side fix landed 2026-09-02 (04-11)**: `bin/setup-github.sh` is read-append-assert with an extra-checks list (`SETUP_GITHUB_EXTRA_CHECKS`), pinned by `test/setup_github_test.rb`, whose static guard refuses to execute the script at all while an unconditional `PUT /protection` is present — the shape UL-009 says the naive repair must never take. Verdict stays `Pending` until the upstream pull request, and the workaround below it stays live for one caller: `bin/refork-smoketest.sh` recreates this repository from the template, so the copy of the script that runs there is still the destructive one. See UL-042 for the fix as offered upstream. |
| UL-004 | 2026-08-31 | 1 | `SCOPE.md`'s governing test hardcodes an app directory path that moves across template versions — `app/SmokeApp/` here, `app/HelloApp/` upstream — so the quoted test goes stale while the test itself has not changed | No - in scope | Pending | — | Would be durable phrased against "the app target". Interacts with DOC-03 in Phase 5. |
| UL-005 | 2026-08-31 | 1 | `AGENTS.md` documents a `git merge upstream/main` sync path that cannot work for a repository materialised by `gh repo create --template`, and its opening line links the words apple-shipkit to the smoketest URL | No - in scope | Pending | — | **The fork-side correction landed in this phase**, commit `c56bde9`: the opening paragraph now states the `gh repo create --template` relationship, and the sync section is a prohibition table with consequences plus the four fetch-and-compare uses of the remote. **Upstream still needs the same fix to its own copy** — upstream repaired the link but not the merge path, and a template that tells every forker to merge `upstream/main` is wrong for every template-materialised repo, not just this one. Interacts with DOC-03. |
| UL-006 | 2026-08-31 | 1 | The Shipkit Pipes product identity — App Store display name and both platform bundle IDs | No - in scope | Fork-only [fork concretion; upstream gets the slot, never the value] | — | Recorded in `docs/PRODUCT-IDENTITY.md`, which 01-04 created — the link was dangling when this row was written and now resolves. Upstream keeps its placeholders. This row exists to demonstrate the Fork-only verdict: the learning passes the SCOPE test, and is still deliberately not upstreamed. |
| UL-007 | 2026-08-31 | 1 | The template ships a single `fastlane/metadata/` tree while documenting dual-platform support, and one `review_information/` directory cannot hold two app records' metadata | No - in scope | Pending | — | Surfaced in 01-05, when the generated `id=macos` notes block turned out to have nowhere to be written. Blocks a per-platform notes generator, so splitting the tree is a prerequisite inside Phase 8 rather than optional cleanup; the generator already takes `--id` and `--dest`, only the directories are missing. **Late row** — the learning landed in commit `ac851da` and this row in the phase close-out. |
| UL-008 | 2026-08-31 | 1 | `read_review_field` resolves `ENV[APP_REVIEW_NOTES]` ahead of `notes.txt`, and `.bootstrap.env.example` actively recommends setting that variable — so the recommended path silently defeats any generated review-notes file | No - in scope | Pending | — | D-25. Upstream could invert the precedence, or warn at the recommendation that the variable overrides the tracked file that CI checks. Worked around fork-side by making `tools/gen-review-notes.rb --check` assert the variable is unset in both the process environment and `.bootstrap.env`, rather than merely diffing two files: a file-only comparison goes green while Apple receives text nobody reviewed. **Late row** — the learning landed in commit `ee3a86f` and this row in the phase close-out. |
| UL-009 | 2026-08-31 | 1 | GitHub replaces the entire `checks` array when `required_status_checks` is PATCHed with one, so the obvious command for adding a required check destroys every check already there | No - in scope | Pending | — | Caught in 01-06 before it ran; the shape as planned would have left `main` protected by a single context. The safe form reads the live array, appends the new entry, and PATCHes the result — additive by construction and idempotent. Distinct from UL-003: that row is `bin/setup-github.sh` dropping a fork-added check, this one is the naive repair doing worse damage in the other direction. Runbook in `CONTRIBUTING-UPSTREAM.md` section 6, including why the parent `/protection` object must never be PUT. **Late row** — the learning landed in commit `7286037` and this row in the phase close-out. |
| UL-010 | 2026-08-31 | 1 | A grep-based gate run over a repository that documents its own literals matches the prose explaining the gate; three separate near-misses in this phase alone | No - in scope | Pending | — | Occurrences: this file's close-out grep would have matched its own verdict vocabulary had that vocabulary been formatted as a table (01-03); a workflow comment naming the trigger it forbids defeated a grep for that trigger (01-06); and a note explaining a banned phrase would have tripped the ban on it (01-06). Upstream carries the same shape in `bin/verify-rename.sh`, which greps five literals repo-wide and maintains a hand-written seven-path exclusion list that has to grow every time a doc discusses one. Generic fix: extract the specific section or table, or strip comment regions, instead of growing an allowlist. **Late row** — the learnings landed in commits `48ee116`, `c31a7a7` and `2ca986e`, and this row in the phase close-out. |
| UL-011 | 2026-08-31 | 1 | A competitive-scan verdict was recorded from a store search result's name alone, and the wrong verdict stopped the phase before the listing was read | No - in scope | Fork-only [product-research learning about this fork's own App Review argument; the template has no slot for it] | — | 01-02 recorded `CyberChef Pro` as a CyberChef derivative on presence alone and escalated a blocker against D-24; commit `db936de` corrected both assumption verdicts to NO after the App Store listing showed an unrelated cooking app. The original error is preserved in `docs/REVIEW-ARGUMENTS.md`'s audit trail rather than rewritten away, because it is exactly the reasoning failure that document exists to guard against. Recorded here because D-19 requires a row for every learning, including the ones that will never go upstream. |
| UL-012 | 2026-08-31 | 1 | A tool that reads project files without pinning an encoding dies with a stack trace instead of a verdict whenever the locale is unset | No - in scope | Pending | — | `tools/gen-review-notes.rb` read `docs/REVIEW-ARGUMENTS.md`, `notes.txt`, and `.bootstrap.env` with `File.read`/`File.foreach`, inheriting `Encoding.default_external`. With `LANG` unset - cron, launchd, a bare container, `env -i` - that is US-ASCII, and any non-ASCII byte raises `ArgumentError` from `String#match?` (line 211) or `Encoding::CompatibilityError` from `String#rstrip` (line 132). Reachable input, not exotic: an App Review contact with a diacritic, or a curly apostrophe anywhere in the notes prose. CI never caught it because `.bootstrap.env` is gitignored and absent there, so the guard short-circuits. A drift gate that crashes cannot be distinguished by its caller from one that passed. Fixed fork-side by pinning UTF-8 at every read and write site plus a regression test that clears `LC_ALL`/`LANG`/`LC_CTYPE`; upstream needs the same once the generator lands there. |
| UL-013 | 2026-09-01 | 2 | `register_app_id` hardcodes `BundleIdPlatform::IOS`, and the comment above it defers macOS registration to a `match` code path that v1.6 deleted, so the lane cannot register a macOS bundle ID at all and nothing says so | No - in scope | Merged [#279] | [#279](https://github.com/indiagrams/apple-shipkit/pull/279) | D-33. The defect has four surfaces, and changing only the hardcoded constant would have left three of them teaching the wrong model: the comment deferred to match's per-platform profile path, which v1.6 removed along with match-based CI signing (apple-shipkit#158); it cited fastlane 2.230 against a 2.238.0 lockfile; and the success line interpolated a literal iOS platform regardless of what was sent, so the log would have become the next stale comment. The lane now takes a `platform:` option (`ios` default, `macos`), validated before authenticating so a typo costs no round trip. `BundleIdPlatform` exposes exactly `IOS` and `MAC_OS` - re-read 2026-09-01 in the locked 2.238.0 gem at `spaceship/lib/spaceship/connect_api.rb:126-130` - so `UNIVERSAL` is unreachable through spaceship even though Apple's own API defines it, and `BundleIdCreateRequest` requires the field, so a value must always be supplied. Delivered by `git format-patch` into a separate clone per D-34; nothing was pushed from this repository and the `upstream` remote's push URL is still the literal `no-push`. `docs/BOOTSTRAP.md`'s claim that the lane is idempotent because `BundleId.create` rescues `ALREADY_EXISTS` was corrected in the same pull request - no code does that, and the `BundleId.all(filter:)` lookup preceding it is what actually makes a re-run safe. Merged upstream 2026-09-01 as `4dbdaa8` with all eight required contexts green (`enforce_admins` is on, so none were bypassed); the verdict moved to its terminal form in the change that observed the merge rather than at close-out. D-35 gated every bundle ID registration in this phase on this row reaching a terminal verdict, and that gate is now open. |
| UL-014 | 2026-09-01 | 2 | The template documents `fastlane match` with a separate certs repo, where v1.6 replaced it with per-run mint-and-revoke, so a forker following the documented model supplies a `GH_CERTS_REPO` that no longer drives anything | No - in scope | Pending | — | C-01. `fastlane/Fastfile` records that match-based CI signing was dropped in v1.6 (apple-shipkit#158) because match needed a separate certs repo, two extra secrets, and broke for legacy WWDR-G3-issued certificates on macos-15-arm64; `.github/workflows/release.yml` states outright that there is no certs repo dependency. The configuration surface did not follow the code: `.bootstrap.env.example` and `bin/init-bootstrap-env.sh` still prompt for `GH_CERTS_REPO` and `MATCH_PASSWORD_FILE`, and `bin/refork-smoketest.sh` still reads them. That is the worst shape a dead config key can take - a forker supplies a value, nothing raises, and nothing happens - and it is how this project's own planning notes came to carry a signing model the code had already abandoned. Found by reading the signing path while preparing UL-013 rather than by being bitten, so it is recorded before it costs anyone a session. Upstream shape: either remove the dead keys, or annotate them at the point of prompting with the version that stopped reading them. |
| UL-015 | 2026-09-01 | 2 | The fix in UL-013 still misreports an App ID's platform, one indirection further along: `register_app_id`'s success line interpolates the **requested** platform rather than what Apple returned, and its new mismatch warning fires on every re-run against a `UNIVERSAL` record while advising a second, unreclaimable bundle identifier | No - in scope | Merged [#280] | [#280](https://github.com/indiagrams/apple-shipkit/pull/280) | R-10. Found by 02-06 actually registering the two App IDs rather than trusting the lane's output. Apple returns `UNIVERSAL` whatever platform was requested: all three App IDs on this fork's team read back that way from `GET /v1/bundleIds`, including the pre-existing smoketest App ID that predates this work (the identifiers themselves are recorded in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values live). Verified four ways before being written down — raw probe JSON, `BundleId.all` through spaceship's object mapping, an independent read, and the comparator discriminating (`identifier` passed while `platform` failed on the same record) — and `name` came back exactly as sent, so the request arrived intact. Two surfaces, both introduced by #279. The success line prints the caller's own input back at them, which is the same shape as the literal `(IOS)` that #279 removed and which #279's own commit message called out as "the next stale comment". Worse, the `already registered` branch now emits, verbatim on every re-run, *"is registered for UNIVERSAL, not the requested MAC_OS. An App ID's platform cannot be changed after creation; a separate bundle ID is needed for MAC_OS"* — a bundle identifier binds permanently to a team on first upload, so that advice spends something unreclaimable over a mismatch that does not exist. Fixed upstream by reporting Apple's answer next to the request and treating `UNIVERSAL` as covering the requested platform; the genuine `IOS`-when-`MAC_OS`-was-asked case keeps the old warning. **Deliberately unsettled and stated as such in the patch, the changelog and the PR body:** whether Apple ignores `platform` at create time or normalises the record afterwards. A read-back cannot tell them apart and settling it costs a third App ID that can never be reclaimed (T-02-32), so the code handles `UNIVERSAL` when Apple returns it and claims nothing about when Apple will. Delivered by `git format-patch` into the separate clone per D-34, from a throwaway branch that was deleted after the patch was produced — this fork's `fastlane/Fastfile` still carries only upstream's merged content, and the `upstream` remote's push URL is still the literal `no-push`. Fork-side read-back assertions were left failing rather than widened; that is UL-013's lesson applied to itself. Merged upstream 2026-09-01 as `212b4892` — `gh pr view 280` reports `state: MERGED` with `mergeCommit.oid` equal to that hash, seventeen checks green including all eight required contexts, and `enforce_admins` is on upstream so none were bypassed. The verdict moved to its terminal form in the change that observed the merge rather than at close-out, which is the same discipline UL-013 followed. The merged commit was then brought into this fork with `git cherry-pick -x` through the fetch-only remote, exactly as `4dbdaa8` was; the `register_app_id` lane body here diffs empty against `212b4892:fastlane/Fastfile`, and `git remote get-url --push upstream` still returns the literal `no-push`. The fork-side read-back assertions were still not widened — a lane that stops misreporting the platform does not make Apple stop storing `UNIVERSAL`, and those two are separate facts. |
| UL-016 | 2026-09-01 | 2 | `bootstrap_asc`'s not-found remediation walks the reader through creating a single App Store Connect record with both platforms ticked, which is the Universal Purchase shape, without naming it as such or saying that the shape is close to permanent — and in the same breath tells them the app name is free to choose, which it is not | No - in scope | Pending | — | C-10. The lane's `UI.user_error!` block prints a numbered recipe whose step 2 reads `Platforms: iOS + macOS`. Quoted here as the defective string, not as guidance: a forker who wants two independent records, one per platform, must not follow it. The text nowhere says that ticking both platforms selects Universal Purchase, that Universal Purchase makes both platforms share one bundle identifier, or that unwinding the choice later means deleting the record — which permanently burns its SKU, since a removed app's SKU cannot be reused in the same organization. A structural, near-irreversible choice is presented as a form-filling step. **Recorded honestly: this fork ended up in exactly the shape that text describes.** Universal Purchase was adopted mid-phase after Apple refused a second record, so the instruction is now accidentally right for this repository. That does not make it correct. It was never written against a decision — it names one option and gives the reader nothing to decide with — so it would still silently produce the wrong structure for the fork the next reader is building, and being coincidentally right for one fork is not a property worth preserving upstream. The second defect in the same block is the sharper one and was found by being bitten: step 2 annotates the name field `SmokeApp (or whatever; verify uses bundle_id, not name)`. The parenthetical is true about what the lane verifies and false about what Apple permits. App Store names are unique **per account**, not merely store-wide, so "or whatever" is not free — a name already used by another app in the same account is refused outright, and Apple's refusal is the only place that constraint surfaces because there is no availability-query endpoint to ask in advance. This phase hit that refusal creating its second record and had to reverse a Phase 1 decision to get past it; the record ids and identifiers involved are in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values live. Upstream shape: state which structure the recipe produces and what it costs to change, offer the per-platform alternative alongside it, and replace the "or whatever" with the account-uniqueness constraint plus the fact that it can only be discovered at creation. Distinct from UL-014, which is dead `match` configuration. It is also no longer paired with C-03 — that correction flagged `.bootstrap.env.example` and `fastlane/Appfile` for asserting a shared bundle identifier against a fork that had declined Universal Purchase, and adopting Universal Purchase dissolved the conflict rather than resolving it. The two were always separate defects: C-03 was a fork-versus-template disagreement that a fork decision could end, and this row is a template defect that no fork decision touches. Verdict `Pending`: no pull request was opened for it in this plan, so the row is filed at the point the learning landed rather than held back until there is a PR to point at. |
| UL-017 | 2026-09-01 | 2 | A shipping-guidance file states that EU Digital Services Act trader status is an account-level-only setting. Apple carries **two** independent surfaces — an account-level declaration under Business, and a per-app declaration under App Information, App Store Regulations and Permits — so a verification that reads only the account level reports green while an individual app record sits undeclared; and the same file names trader verification state labels that appear in no first-party Apple source | No - in scope | Pending | — | C-08 and R-06 as one row, because it is the same file wrong about the same subject in two ways. **The generic fix, in template terms:** a trader-status verification step must name *both* surfaces, and must not assert a UI label its author has not read off a screen. Both halves were tested against Apple on 2026-09-01 and both failed. The account-level status string is `Active` — neither of the two predicted state labels, and neither of those has a first-party source anywhere. The per-app surface is real, carries its own sentence, and is where an app's own declaration actually lives. A third instance of the same defect landed in the same session: Apple's own documentation describes the trader dialog's choice in wording that matches neither option label displayed, and the real one is a contraction — three documented-string-versus-reality failures inside one phase, which is what makes this a rule rather than an anecdote about one file. Two things a template should say outright rather than imply. First, **there is no App Store Connect API surface for trader status at any level** — an exhaustive scan of every path and every schema name in specification 4.4.1 found nothing — so this check cannot be automated and a template that leaves it looking automatable is worse than one that names it as human-only. Second, under Universal Purchase the per-app surface carries **no platform selector**, so a single declaration covers every platform version on a record; a template that assumed one declaration per platform would be wrong in the other direction, and that was observed rather than reasoned about. |
| UL-018 | 2026-09-01 | 2 | `POST /v1/reviewSubmissions` returns `201 Created` against an app record with no build and no version, and the created container can then be removed by **no API route at all** — the resource permits no `DELETE`, and the documented cancel is refused because a never-submitted submission is not in a cancellable state. Creating one is a one-way door whose only exit is submitting it to App Review | No - in scope | Pending | — | Found by 02-09 running the write it had planned as a permissions probe. The plan's premise — that a build-less, version-less record makes `POST /v1/reviewSubmissions` unable to succeed, so the probe creates no state — is simply wrong; Apple creates the container eagerly and defers every validation to the point where items are attached or the submission is submitted. Anyone reasoning "there is no build, so this write is safe" is reasoning from a model Apple does not implement. **The trap is the exit, not the entry.** `DELETE /v1/reviewSubmissions/{id}` returns `403 FORBIDDEN_ERROR` whose own `detail` reads *"The resource 'reviewSubmissions' does not allow 'DELETE'. Allowed operations are: CREATE, GET_COLLECTION, GET_INSTANCE, UPDATE"* — confirmed independently against Apple's OpenAPI specification 4.4.1, where `/v1/reviewSubmissions/{id}` carries exactly `GET` and `PATCH`. That leaves `UPDATE` as the only mutating verb, and in the locked fastlane 2.238.0 gem `UPDATE` is two different operations distinguished only by the attribute sent: `submit_for_review` sends `{ submitted: true }` (`spaceship/lib/spaceship/connect_api/models/review_submission.rb:55`) and `cancel_submission` sends `{ canceled: true }` (same file, line 62), both through `patch_review_submission` (`connect_api/tunes/tunes.rb:1181`). The cancel was issued with a body containing `canceled` and nothing else, and Apple refused it: `409 STATE_ERROR.ENTITY_STATE_INVALID`, *"Resource is not in cancellable state"*, against a container in `state: READY_FOR_REVIEW` with `submittedDate: null`. Cancellation is therefore only available to submissions that have already been **submitted** — so the sole route that would make an unwanted container removable is to send it to App Review first. Verified by read-back rather than by status code: the open-submission count on the affected record was `1` before the cancel and `1` after it. **Two distinct refusals both arrive as `403`/`409` on this endpoint family and mean unrelated things.** The `DELETE` `403` is a resource-capability refusal — the operation does not exist on the resource — and a Developer-role key, an Admin key and the Account Holder would all receive it identically. It says nothing whatever about the caller's privileges. Any probe that maps a bare status code to a permissions conclusion will read it as insufficiency and be wrong; that conflation is what makes this row worth filing rather than just the irreversibility. Note also the asymmetry that makes the shape easy to miss: the child `/v1/reviewSubmissionItems/{id}` **does** allow `DELETE`, so items can be detached from a container that itself cannot be removed. Upstream shape, in template terms: never use a `POST` against `reviewSubmissions` as a permissions or connectivity probe — Apple grants one open review submission per app and platform, so a speculative create strands that slot indefinitely — and document that `cancel_submission` is valid only after `submit_for_review`, because its name reads as a general undo and is not one. A tool offering `cancel` next to `submit` without that constraint invites exactly this dead end. Filed while the state it describes is still live on this fork's iOS record; the container remains open and this executor has no sanctioned route to retire it. |
| UL-019 | 2026-09-01 | 2 | Two files tell a reader that an at-capacity certificate mint with `--force` **revokes the oldest certificate by creation date**. No such code path exists on the mint path in the pinned fastlane, and the App Store Connect API exposes no creation date on `Certificate` to order by, so the stated behaviour is unimplementable as written as well as absent | No - in scope | Pending | — | C-07 / R-04. The defective strings are `.github/workflows/canary-local-mode.yml` (*"At-cap mint with --force revokes the OLDEST cert by creation date — could wipe out a production cert"*) and `docs/APPLE-PREREQS.md` under its per-team certificate quotas heading, which carries the same sentence. Read out of fastlane 2.238.0, the version this repository locks. **What `--force` actually does.** `cert/options.rb` defines `force` as *"Create a certificate even if an existing certificate exists"*, default `false`, env name `CERT_FORCE`. `Cert::Runner#run` uses it in exactly one place: `should_create = Cert.config[:force]`, and `unless should_create` it looks for a certificate already in the local login keychain and creates only when it finds none. So `--force` means *skip the local reuse check and always POST*, and nothing else. `create_certificate`, the only thing `run` calls afterwards, builds a signing request, calls `Certificate.create`, and writes the results to disk — there is no removal of anything anywhere on that path. **A correction to the correction, found by re-reading the source rather than trusting the note.** R-04 was originally written as *"no revocation code path exists in the cert action at all"*, and that is very slightly too strong. `Cert::Runner#revoke_expired_certs!` does exist in the same class and does call `certificate.delete!` — but it is reachable only through the `fastlane cert revoke_expired` sub-command declared in `cert/commands_generator.rb`, never from the `cert` action and never from `force`. It is worth stating precisely, because the precise version is the stronger refutation: the one revocation fastlane offers here selects certificates by `certificates.reject(&:valid?)`, which is **expired** ones, not oldest ones. So the repository is wrong twice over — about which flag revokes, and about the ordering the only real revocation uses. **The operational advice is unchanged and still correct: do not override the `--force` default.** Only the reason was wrong. `--force` still forces a create, and a create still consumes a slot against a shared certificate limit, so on a team shared with another shipping product the recommendation stands on its own feet. This distinction is the whole reason the row is worth filing rather than quietly editing: a reader who learns the false mechanism will reason from it later — they might conclude `--force` is *safe* once no certificate on the team is old, or that it is a legitimate way to recycle a slot, and both follow from the false premise while contradicting the true advice. The claim most plausibly migrated from `fastlane match --force`, which genuinely does regenerate certificates; match-based CI signing was removed from this template in v1.6, and the sentence outlived the tool it described. Not the whole comment block is wrong, and the row says so rather than inviting a blanket rewrite: `.github/workflows/release.yml` carries the same neighbouring comment but states only the safe half — at-cap without `--force` is non-destructive, and the default is no `--force`. That text is correct and should be left alone. Separately and not part of this learning: the quota figures printed beside the defective sentence in both workflow files were measured against a different Apple team than the one this fork uses, and nothing in either file except the parenthetical says so. That is filed as this fork's own re-measurement problem rather than an upstream row, because the numbers are concretions and the slot is what upstream would get. Upstream shape: delete the revocation sentence from both files, replace it with what the flag does — skips the local-keychain reuse check, always creates, therefore consumes a slot — and, if a revocation is to be documented at all, document `fastlane cert revoke_expired` by name along with the fact that it selects on expiry. Verdict `Pending`: no pull request was opened in this plan, so the row is filed where the learning landed. |
| UL-020 | 2026-09-01 | 2 | `fastlane cert` on its default `force: false` reuses a certificate already present on the machine and never calls Apple's create endpoint, and the run it produces is indistinguishable at every surface from one that genuinely created: same exit `0`, same success banner, same `CER_CERTIFICATE_ID`. Any pipeline reading a green `cert` as evidence that its signing credentials are authorized to create certificates is relying on a check that cannot fail | No - in scope | Pending | — | Found by running the mint rather than reasoning about it, and then diffing the certificate list before and after: the lane succeeded and the set of certificate ids on the team was byte-identical, so no create request was ever made. The concrete ids, the team and the dated census live in [APPLE-ACCOUNT-STATE.md](APPLE-ACCOUNT-STATE.md), which is where this fork's values belong; what is upstreamable is the shape. **The mechanism, read out of fastlane 2.238.0.** `Cert::Runner#run` sets `should_create = Cert.config[:force]` and, unless that is set, calls `find_existing_cert` and creates only when it returns nil. `find_existing_cert` has **two** reuse paths, not one. The first is a fingerprint match through `FastlaneCore::CertChecker.installed?`. The second is quieter and matters more: if no identity is installed but a cached `<id>.p12` is sitting in the `cert` output directory, it imports that key and certificate and reuses anyway. So clearing the keychain does not reliably compel a create either, which is worth stating because "delete the local copy and re-run" is the obvious thing a reader does when they want to test the real path. **`CertChecker.installed?` is the union of two different `security` queries, and they answer different questions.** Signing identities come from `security find-identity -v -p codesigning`; installer certificates come from `security find-certificate -c` against the third-party Mac installer and Developer ID installer names. Installer certificates never appear under the code-signing policy at all, so anyone predicting whether an installer-certificate mint will reuse, from the `find-identity` output alone, sees nothing and predicts a create. Note also that the two queries have different strengths: `find-identity -v` lists only certificates that have a usable private key, while `find-certificate` matches on name and fingerprint without requiring one. Read from the pinned source rather than observed, and flagged as such. **What is not wrong here.** The reuse behaviour itself is the right default and is exactly what stops `cert` burning a slot against a shared certificate limit on every run. Nothing about the default should change. The defect is narrower: the outcome is not reportable. `find_existing_cert` already knows which branch it took and already prints different sentences for reuse and for create, so the information exists inside the action and is simply not exposed anywhere a caller can assert on it. `run` returns nil either way, and `CER_CERTIFICATE_ID` is set identically on both paths. **Upstream shape, stated as the problem rather than as a prescription.** Make a reuse distinguishable from a create at the action's boundary, so a pipeline that needs to know which one happened can find out without diffing Apple's certificate list itself. Whether that is a new `CER_*` output, a return value, or documentation that says plainly the exit code carries no such information is upstream's call, and this row deliberately does not pick one. What should not stand is the current position, where the distinction is computed and then discarded. **Why it is more than bookkeeping.** The question "may this key create a distribution certificate" is one you need answered on a machine where no such certificate exists — a fresh runner, a new contributor, a rebuilt keychain. That is precisely the case in which the reuse path is unavailable, so a green mint on a developer machine is evidence about the only situation that never needed evidence. Related to but distinct from UL-019, which is about `--force` being documented as revoking something it does not touch; this row is about what happens when `--force` is correctly left alone. Verdict `Pending`: no pull request was opened in this plan, so the row is filed where the learning landed rather than held until there is one to point at. |
| UL-021 | 2026-09-01 | 2 | A document that publishes a pasteable re-check command per fact accumulates commands that have never been executed, and a re-check command nobody has run is a convention rather than a mechanism. Four of the commands published in this fork's account-state document were executed for the first time at phase close and did not run: two omitted the bundle prefix their own script requires and died in a `LoadError`; one named a command-line flag that the tool does not accept and could never have run whatever values were substituted; and one was a write that permanently consumes an unreclaimable resource, published in a column headed `Re-check command`. Nothing in the repository executed any of them, so all four had been sitting there looking runnable | No - in scope | Pending | — | Filed in the change that found it, not at a later close-out. **Why this is generic rather than this fork's own untidiness.** The template already ships several documents whose value is the commands in them — `docs/BOOTSTRAP.md`, `docs/APPLE-PREREQS.md`, `docs/CONTINUOUS-VALIDATION.md`, `docs/CONTRIBUTING-UPSTREAM.md` — and nothing anywhere runs a single one. The failure mode is silent by construction: prose rots loudly, because a reader notices it contradicts something; a stale command rots quietly, because it looks exactly like a fresh one until the day someone pastes it and gets a stack trace instead of an answer, which is generally the day they most needed the answer. **The three shapes observed here, because they want different fixes.** (1) A command whose environment is not the one it will be pasted into — the pinned-bundle case; the script runs in CI under `bundle exec` and the document dropped that half. (2) A command that could never have run at all — an invented flag; this one is only detectable by execution, since it is well-formed as text. (3) A command that runs but must not be run — a create, published in a re-check column; correctness here is not about whether it executes but about whether executing it is safe, which no linter can tell you. **Upstream shape, stated as a problem rather than a prescription.** Give a fork some way to assert that the commands its documents publish still execute, and some way to mark the ones that are writes so nobody re-checks with them. Whether that is a fenced-block extractor with a smoke job, a naming convention for read-only versus mutating commands, or documentation saying plainly that these commands are unverified, is upstream's call and this row deliberately does not pick one. What should not stand is the current position, where a document's most actionable content is the only part under no guarantee at all. **Related to but distinct from UL-010**, which is about a gate whose grep matched the prose describing the gate; this row is about commands that are never run rather than checks that pass vacuously. Verdict `Pending`: no pull request was opened in this plan. |
| UL-022 | 2026-09-01 | 2 | Reversing a recorded decision updates the documents the reversing change happens to touch, and leaves the decision's citations standing everywhere else. This fork reversed `D-05` mid-phase and updated the two documents that change edited; a third tracked document went on asserting the reversed decision by name for four plans, in a file whose sentinel-delimited sections are copied verbatim into what Apple's reviewers read | Yes - out of scope | Fork-only [a discipline learning about this fork's own decision log; the template ships no decision-citation surface for a fix to attach to] | — | Found at the phase close-out by grepping every tracked document for the reversed decision's id, which is the check that should have run inside the reversing change. Recorded rather than quietly fixed, because the class of error matters more than the instance. **What made it survive.** The reversal was correct, well-reasoned and thoroughly documented — in `docs/PRODUCT-IDENTITY.md` and `docs/APPLE-ACCOUNT-STATE.md`, both of which the reversing plan already had open. `docs/REVIEW-ARGUMENTS.md` cited the same decision twice and was not in that plan's file list, so nothing prompted anyone to look. The standing `DOC-01` exit criterion — every change updates its documentation in the same change — was satisfied on its face: documentation *was* updated in the same change. It just was not all of it, and 'the documentation this change touched' and 'the documentation this change invalidated' are different sets. **Why the stale text was more dangerous than it looked.** Both stale passages sat in the file whose `id=core` block is copied byte-for-byte into `Notes for Review`. Both happened to fall outside the sentinels, verified by grepping the generated `notes.txt` for the offending sentence — so nothing false was ever going to Apple. That was luck, not design: nothing about the file's structure keeps a stale claim on the safe side of a sentinel. **Why `Out-of-scope` rather than `Pending`.** The generic form of this — reversing a decision should trigger a citation sweep — has no artifact in apple-shipkit to attach to; the template has no decision log, no decision ids, and nothing that cites them. Sending upstream a discipline with no surface to enforce it on would be a process opinion dressed as a patch. The fix that does travel is narrower and is filed separately as `UL-021`. |
| UL-023 | 2026-09-01 | 3 | The template's Team-ID mechanism writes the real value into tracked build manifests on its success path. `bin/rename.sh` Step H substitutes `TEAM_ID_PLACEHOLDER` into `app/project.yml` and `app/Project.swift`, both committed; `bin/switch-to-xcodegen.sh` re-substitutes it into a restored manifest; and `docs/APPLE-PREREQS.md`'s setup checklist instructs a forker to do the same by hand. The template ships no gitignored per-clone signing configuration, so there is nowhere else for the value to go, and a forker who follows the documented path commits their Team ID on the first push | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Three call sites: `bin/rename.sh` Step H (the `sed` over the two manifests, lines 652-660), `bin/switch-to-xcodegen.sh` line 214 (re-applying fork identity to a restored `app/project.yml`), and the `docs/APPLE-PREREQS.md` checklist item "Substitute it for `TEAM_ID_PLACEHOLDER` in `app/project.yml`". The replacement, built in this fork's Phase 3: a tracked `app/Identity.xcconfig` holding the four identity variables and ending in an optional include (`#include? "Local.xcconfig"`) of a gitignored `app/Local.xcconfig` that holds `DEVELOPMENT_TEAM` alone. The include is silent when the file is absent, the `.gitignore` rule lands before the file is created so it is never untracked-and-visible, and the named failure comes from a preflight rather than from the build. Filed in the change that built the layer. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (squash of apple-shipkit #281, 39 files) — `gh pr view 281` reports `state: MERGED` with `mergeCommit.oid` equal to that hash, the merge commit read separately touches `app/Identity.xcconfig`, `.gitignore`, `bin/preflight-identity.rb`, `ci/check-identity.sh`, `bin/rename.sh`, `bin/switch-to-xcodegen.sh` and `docs/APPLE-PREREQS.md`, and all eight required contexts were green on the head (`enforce_admins` is on, so none were bypassed). All three call sites closed upstream: Step H writes gitignored `app/Local.xcconfig` instead of sed-ing the manifests, the `switch-to-xcodegen.sh` re-substitution block is gone, and the checklist item now names the xcconfig. Upstream's `Identity.xcconfig` carries placeholders only, per UP-05. The verdict moved to its terminal form in the change that observed the merge rather than at close-out, as UL-013 and UL-015 did. |
| UL-024 | 2026-09-02 | 3 | The template hardcodes its own application name into the scripts and configuration that check and build it. `ci/local-check.sh`, `ci/local-release-check.sh`, `fastlane/Snapfile`, `fastlane/MacSnapfile` and `.gitignore` all spell out `app/<AppName>.xcodeproj`, the `<AppName>-iOS` and `<AppName>-macOS` schemes, and `build/<AppName>-<version>.ipa` and `.pkg`, so every fork has to substitute a literal across the build system rather than set one value — which is the whole reason `bin/rename.sh` has to sed the build system at all | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Twenty-five occurrences across the five files here (6, 13, 2, 2 and 2). The fix is a generic project constant with nothing to resolve: the project is `app/App.xcodeproj`, the schemes are `App-iOS` and `App-macOS`, and the release artefacts are `build/App-<version>.ipa`, `build/App-<version>.pkg`, `App-iOS.xcarchive` and `App-macOS.xcarchive`. A constant rather than a name derived from `PRODUCT_NAME`, because a derived name reintroduces exactly the variable the constant removes, and the artefact name is build plumbing rather than product identity — the product name lives in the tracked xcconfig that UL-023 describes. This is the precondition for retiring the rename machinery entirely. Applied fork-side in this change as a forward-port. Upstream's copies of the same five files say a different name (`HelloApp`, in 45 files), so a patch generated here has every hunk rejected by `git am` on its context lines; the upstream change is authored by hand against upstream's own text, which [CONTRIBUTING-UPSTREAM.md](CONTRIBUTING-UPSTREAM.md) section 4 already allows. The same upstream change should drop the `vars.APP_NAME` indirection in the pull-request workflow, which is deliberately not forward-ported here. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281, hand-authored against `HelloApp` in a fresh clone as section 4 allows; `git am` was not attempted), eight required contexts green, `enforce_admins` on. Upstream's five files, its `Makefile`, `Brewfile`, `bin/switch-to-*.sh`, `bin/take-readme-screenshots.sh` and the Fastfile's scheme/artefact constants now spell the constant `App`, and this fork's 03-06 forward-port converges with upstream's text. The `vars.APP_NAME` indirection went with it — see UL-025. *Residue noted 2026-09-02 (03-REVIEW IN-07):* this fork's template-owned `Makefile:78` still advertises `Regenerate SmokeApp.xcodeproj from app/project.yml` under `make help` — the one `SmokeApp` project path a user is still told about. Upstream already says it right: `git show upstream/main:Makefile` (`c6c324f`) line 78 reads `Regenerate app/App.xcodeproj from app/project.yml`, so there is nothing to propose upstream; the line converges when the fork next takes the template's `Makefile` (a refork replaces it wholesale). Not edited here: the `Makefile` is template-owned and the fork adds nothing to it. |
| UL-025 | 2026-09-02 | 3 | The template's pull-request workflow resolves the Xcode project path and scheme from a repository variable, with the template's own application name as the hardcoded fallback operand, and it does so inside every required build context. A fork that renames and never sets the variable gets the template's name back silently, and a fork that renames again after setting it gets its previous name back, so the failure presents as six unmergeable pull-request checks rather than as a configuration error that names the variable | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Six interpolations in three `xcodebuild` steps of `.github/workflows/pr.yml` (lines 247-248, 268-269, 285-286 in this fork's copy), one per platform, multiplied across a two-generator matrix into six required status contexts; `release.yml` (line 105) and `canary-local-mode.yml` (line 109) read the same variable at their workflow-level `env:` with no fallback at all, so a rename that never sets the variable leaves those two workflows with an empty name rather than the wrong one. Nothing about the project's name needs a variable once it is a structural constant (UL-024): the fix is `app/App.xcodeproj` and `App-iOS` / `App-macOS` spelled in the workflow with no `vars.` lookup and no fallback, which also removes the only reason the bootstrap step has to set the variable at all. Fork-side, this repository's variable was set to the constant behind a blocking checkpoint and read back typed, because the workflow is template-owned and is not forward-ported here. Decided at that checkpoint on 2026-09-02: dropping the `vars.APP_NAME` indirection from the pull-request workflow is in scope for the upstream pull request that carries UL-024, authored by hand against upstream's own text. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281): `git show upstream/main:.github/workflows/pr.yml` now contains zero `vars.APP_NAME` references and spells `app/App.xcodeproj` / `App-iOS` / `App-macOS` in all six cells, with an `identity preflight` step in the `config` job. Scope kept to the pull-request workflow as decided: `release.yml` (line 105) and `canary-local-mode.yml` (line 109) were not in the 39 files and still read the variable at their workflow-level `env:` — a stated remainder for a follow-up, not a silent one; with UL-026 still open that is where the re-set can still bite. |
| UL-026 | 2026-09-02 | 3 | The fork-bootstrap and refork paths re-set the same repository variable from a config file during a routine operation, so a correct live value is silently reverted by the next `make bootstrap-fork`, and by a refork, with no warning, no diff and no gate — the reversion is invisible until the next pull request's required checks go red | No - in scope | Pending | — | Two call paths converging on one write. The write is `bin/lib/bootstrap.rb` line 595, `Sh.run!("gh", "variable", "set", key, "--body", val, "--repo", …)` inside `GHSecrets#do_it`, iterating `REQUIRED_VARIABLES = %w[APP_NAME BUNDLE_ID]` (line 546) from `.bootstrap.env`; `bin/bootstrap-fork.rb` is the entry point (`make bootstrap-fork`) and `docs/BOOTSTRAP.md` line 152 documents it as step 10 `GHSecrets`. The second path is `bin/refork-smoketest.sh`, which hardcodes `APP_NAME=SmokeApp` at line 101, writes it into the new clone's `.bootstrap.env` at line 239, and tells the operator to run `make bootstrap-fork` at line 273 — the refork script does not itself call `gh variable set`, which corrects the sentence at `CHANGELOG.md` line 161 that says it does. The comment above `bootstrap.rb`'s constant admits the mechanism: "PR CI also breaks silently without these". A step that silently overwrites live repository state on every re-run is the same shape as UL-020's success banner: exit 0 either way, no evidence the value is the one CI needs. With UL-025 landed the variable has no reader in the pull-request workflow and the re-set becomes harmless; until then the step should read the live value first and refuse, or at least say, when it is about to change it. **Amended 2026-09-02 (Phase 4, 04-06, D-56 as amended):** the mitigation this row proposed — have the step read the live value first and refuse, or announce, before changing it — is NOT what landed, and recording that matters because an upstream patch authored from this row's original text would send upstream a gentler write where the fork removed the write. `GHSecrets` no longer sets repository variables at all: `REQUIRED_VARIABLES`, the `gh variable set` loop in `do_it`, and the `gh variable list` probe in `check` are gone, and the step is now `Set 5 GH Secrets on app repo`. A read-then-refuse step would still have been a step that can write live repository state during a routine local command; deleting the write removes the class rather than narrowing it. The generic learning that replaces it is UL-037. This row's *observation* — two call paths converging on one silent write — is unchanged and still open upstream. |

| UL-027 | 2026-09-02 | 3 | XcodeGen derives a unit-test bundle's `TEST_HOST` (and `BUNDLE_LOADER`, which it sets to `$(TEST_HOST)`) from the host target's *name* at generation time, writing `$(BUILT_PRODUCTS_DIR)/<TargetName>.app/<TargetName>` into the pbxproj. The moment a manifest decouples the app's `PRODUCT_NAME` from its target name — by sourcing it from an xcconfig, which is what UL-023 and UL-024 propose — the executable is built at the product-name path, the test bundle is told to load the target-name path, and every `xcodebuild test` fails with `Could not find test host` while `xcodebuild build` stays green and the generator exits 0. Tuist derives the same path from `productName:` and does not break, so a two-generator matrix goes half red on a tree with no manifest diff between the generators to explain it | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Observed on this fork's pull request: two consecutive runs, the two XcodeGen test cells red on the same line (`Could not find test host for <UnitTests>: TEST_HOST evaluates to …/<AppTarget>.app/<AppTarget>` on iOS, `…/<AppTarget>.app/Contents/MacOS/<AppTarget>` on macOS, exit 70) while the device-build cell and all three Tuist cells were green; reproduced locally on Xcode 26.1.1 with XcodeGen 2.46.0 by `-showBuildSettings`: the unit-test targets' `TEST_HOST` ended in the target name while the app targets' `EXECUTABLE_PATH` ended in the product name. The raw pbxproj confirms XcodeGen writes the target name literally; the UI-test bundles are unaffected because they carry `TEST_TARGET_NAME` (a target name, correct as is) and no `TEST_HOST`. **The fix, two lines per unit-test target in `app/project.yml`:** `TEST_HOST` spelled from the product-name variable (`$(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(APP_PRODUCT_NAME)`, with `Contents/MacOS/` on macOS) and `BUNDLE_LOADER: $(TEST_HOST)` — the shape Tuist already emits, so the two manifests stay 1:1 at the resolved-settings level; `app/Project.swift` needed no change. **Why it survived the rename plan:** that plan built and listed but never ran `xcodebuild test`; the pre-push hook builds the iOS device target only; and the cross-generator parity tool compares bundle id and product name, not `TEST_HOST`, so parity read equal on the keys it reads. A parity check that means to catch generator drift should include `TEST_HOST` — recorded here for the phase's close-out audit rather than widened in the same change. **Upstream shape:** whenever upstream moves product identity into an xcconfig (UL-023), its `project.yml` needs these two lines on each unit-test target, and its `pr.yml` matrix is the only instrument that would notice their absence — a `build`-only local check cannot. The root cause is arguably XcodeGen's (deriving the host path from the target name rather than from a `PRODUCT_NAME` the manifest sets), and is worth an issue there, but the template can fix it in its own manifest today. **A local-proof pitfall met on the way, already documented by the template at README.md line 233:** under `CODE_SIGNING_ALLOWED=NO` the macOS UI-test runner is unsigned and this Mac's Gatekeeper kills it (`Test crashed with signal kill before establishing connection`) where GitHub's runners do not; local runs are therefore scoped with `-only-testing:<UnitTests>` and the pull-request run is the arbiter for the UI targets. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281, carried in the same pull request as UL-023/UL-024 because the xcconfig-sourced `PRODUCT_NAME` is what makes the two lines necessary): `git show upstream/main:app/project.yml` carries `TEST_HOST: $(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(APP_PRODUCT_NAME)` and `BUNDLE_LOADER: $(TEST_HOST)` on the iOS unit-test target and the `Contents/MacOS/` form on macOS, with a comment stating why; the fork-side source is commit `23c7124`. The XcodeGen-side issue named above is not filed — the template fixes it in its own manifest, which is the row's scope. **The instrument that missed this: parity widened 2026-09-02 (Phase 4, 04-12)** — `tools/identity-parity.rb` now compares each unit-test bundle's `TEST_HOST` against the HOST app target's own resolved `BUILT_PRODUCTS_DIR` + `EXECUTABLE_PATH` (a second `-target` dump taken with the same flags — never a literal), asserts `BUNDLE_LOADER == TEST_HOST`, and compares the two generators' `TEST_HOST` strings to each other. Driven red against this row's own manifest shape first: with `TEST_HOST: $(BUILT_PRODUCTS_DIR)/App-iOS.app/App-iOS` restored to `app/project.yml` the tool prints the diff and exits 1, and with the `$(TARGET_NAME)` spelling (which resolves in the TEST target, to `AppTests.app/AppTests`) it also exits 1 — evidence `.planning/…/evidence/04-12-T1-parity-testhost-controls.txt`. The point of the widening is that the instrument that missed this defect can no longer miss it; the point of the red control is that the claim was tested rather than asserted. It stays a LOCAL command with dated evidence (D-60): the diff needs Xcode, `review notes` runs on `ubuntu-latest`, and all three `macos-15` jobs live in template-owned `pr.yml`. See UL-043 for what the widening found on its first run. |
| UL-028 | 2026-09-02 | 3 | A generation gate can be silently absent on both generators the template supports, for two different reasons, and neither absence produces an error. Tuist caches manifest evaluation keyed on the manifest's own content, so a guard embedded in `Project.swift` is skipped whenever only the guard's subject — the xcconfig it exists to check — changes: the one case the guard is for is the case it structurally cannot fire in. XcodeGen's `options.preGenCommand` does fire, but its own documentation states it does not run when the project is not regenerated under the cache flag, so that gate is one `--use-cache` away from bypass. In both absent states `generate` exits 0 and writes a project whose bundle identifier resolves to nothing | No - in scope | Pending | — | Observed on this fork 2026-09-02 with Tuist 4.205.0 and XcodeGen 2.46.0: with a required key removed from the identity xcconfig, `xcodegen generate` exited 3 naming the key and wrote no project; `tuist generate` on the same input exited 0, wrote the project, and `-showBuildSettings` on it printed no `PRODUCT_BUNDLE_IDENTIFIER` line at all (a setting that resolves to the empty string is omitted from the dump) while the pbxproj carried the unresolved reference twelve times. Earlier in the same phase a manifest-embedded guard was watched not running after only the xcconfig changed, and running only after a byte was appended to the manifest itself. **Mitigation, all three parts needed:** (1) the pre-generation hook on the generator that has one; (2) a CI step that runs the same preflight as plain text on every pull request regardless of which generator produced anything — on this fork the `review notes` job, which is why that step is the Tuist path's only coverage; (3) an assertion in the fork's test suite that no caller under `Makefile`, `ci/` or the workflows passes the cache flag, proven load-bearing by adding `--use-cache` to a Makefile line, watching the assertion fail naming the line, and restoring. UL-027 is the same asymmetry seen from the other side: the two generators are 1:1 on the keys a parity tool reads and different in what they do around generation. **Upstream shape:** the template's pull-request matrix runs both generators, so the text-level preflight belongs in a job that runs for both, and the cache-flag assertion belongs beside it; a manifest-embedded guard should be documented as an anti-pattern rather than offered as the Tuist equivalent. Verdict `Pending`, deliberately, after apple-shipkit #281 merged 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b`: parts (1) and (2) went upstream in that change — `options.preGenCommand: ruby ../bin/preflight-identity.rb` on the XcodeGen manifest and `ci/check-identity.sh` as the `identity preflight` step of `pr.yml`'s `config` job, which runs before every app cell on both generators — but part (3), the cache-flag assertion, was not carried because upstream has no fork-shaped test suite to hold it, and the anti-pattern note went in as a manifest comment rather than a documentation section. The row stays non-terminal for that half rather than being closed on two-thirds of a mitigation. |
| UL-029 | 2026-09-02 | 3 | The template's `.tool-versions` pin file drifts from every installed tool on a machine that installs through `Brewfile`, and nothing reports the drift, so behaviour observed locally is not the behaviour a pin-honouring runner would get. On this fork five of the six pins differ from what is installed: ruby 3.3.11 pinned, 3.3.12 installed; xcodegen 2.45.4 pinned, 2.46.0 installed; lefthook 2.1.6 pinned, 2.1.12 installed; swiftlint 0.63.2 pinned, 0.65.1 installed; swiftformat 0.61.1 pinned, 0.62.1 installed; only xcbeautify 3.2.1 matches. Tuist and Xcode are not pinned there at all. Every generator behaviour this fork recorded — the pre-generation hook, xcconfig attachment, generator parity, the built-plist values — was observed on the installed versions, not the pinned ones | No - in scope | Pending | — | Two remedies exist: verify against the pin (install the pinned versions and re-run the observations), or refresh the pin to what is installed and carry that change upstream, since `.tool-versions` is a template file. **This phase did neither in code.** It recorded the installed version beside every observation, in its evidence transcripts and in the identity document's measurement section, and left the pin as found: a pin edit is an upstream change, and the pin's own consumers (asdf, mise) are not what `make bootstrap` uses — `Brewfile` is, which is why the divergence is invisible to the daily workflow and why refreshing the pin locally would fix a file nothing here reads. The refresh is the upstream-shaped remedy and this row is its vehicle; the verify-against-pin run is deferred to the phase's close-out audit and is listed there as an accepted gap, not a silent one. **Upstream shape:** either drop `.tool-versions` or have `make bootstrap` (or `make doctor`) diff it against the installed versions and warn, so the two sources of truth cannot silently diverge; and pin the generator whose behaviour the template relies on, or state that it is unpinned by design. Verdict `Pending`: no pull request was opened in this change. |
| UL-030 | 2026-09-02 | 3 | On the template's iOS app target the copyright travels as the `INFOPLIST_KEY_NSHumanReadableCopyright` build setting while the target's Info.plist comes from the manifest's `info:` (XcodeGen) / `infoPlist:` (Tuist) block, and Xcode writes `INFOPLIST_KEY_*` settings into the bundle only when it generates the plist itself (`GENERATE_INFOPLIST_FILE = YES`). With a file-backed plist the setting resolves in `-showBuildSettings`, both generators exit 0, the build succeeds, and the built bundle carries no `NSHumanReadableCopyright` at all; the only instrument that shows it is `PlistBuddy` on the built artefact. The macOS target sets the plist key directly and is correct, so the two platforms silently disagree on where the copyright lives | No - in scope | Merged [#281] | [#281](https://github.com/indiagrams/apple-shipkit/pull/281) | Observed on this fork 2026-09-02 with XcodeGen 2.46.0, Tuist 4.205.0 and Xcode 26.1.1 (17B100): an unsigned Release iOS Simulator build from the XcodeGen project answered `Print: Entry, ":NSHumanReadableCopyright", Does Not Exist` (exit 1) while the same build's `-showBuildSettings` printed the setting resolved to the full string and `GENERATE_INFOPLIST_FILE = NO`, `INFOPLIST_FILE = iOS/Generated-Info.plist`. **The fix, one line per manifest:** `NSHumanReadableCopyright` added to the iOS target's plist block of both manifests in the shape the macOS block already uses, and the inert `INFOPLIST_KEY_NSHumanReadableCopyright` line dropped from both — after it, the XcodeGen and the Tuist iOS builds both read the full string with the `©` as bytes `c2 a9`, the macOS build unchanged; removing the key again reproduced `Does Not Exist`, restoring it brought the value back. **Why it survived four plans:** the cross-generator parity tool compares build settings, and the setting was equal on both sides, so parity read green on a value that never reached the bundle; the pre-push hook and the pull-request matrix build and test but read no plist; and no iOS surface displays the key (the About box is macOS), so nothing user-visible was wrong. A parity check that means to catch this must read the built plist, or compare the xcconfig variable rather than the `INFOPLIST_KEY_` setting — recorded here for the close-out audit rather than widened in the same change; with the setting gone the tool's key list now has a second symmetric absence beside `DEVELOPMENT_TEAM`. **The same mechanism, recorded and not fixed here:** the iOS target also carries `INFOPLIST_KEY_LSApplicationCategoryType`, and the built iOS plist has no `LSApplicationCategoryType` either (observed in the same session). **Upstream shape:** the template's `app/project.yml` and `app/Project.swift` carry the same inert line on the iOS target and the plist key on macOS, so the fix is the same two edits there, and the template's `INFOPLIST_KEY_*` convention only holds for its test targets, which do set `GENERATE_INFOPLIST_FILE: YES`. Merged upstream 2026-09-02 as `c6c324f9f2470bac094ed49a37b8e29809130b2b` (apple-shipkit #281): `git show upstream/main:app/project.yml` and `:app/Project.swift` each carry `NSHumanReadableCopyright` as `$(COPYRIGHT)` in the iOS plist block (lines 81 and 57) alongside the macOS one, and the inert `INFOPLIST_KEY_NSHumanReadableCopyright` line is gone from both; the fork-side source is commit `b8b1ac9`. `INFOPLIST_KEY_LSApplicationCategoryType` was left as found upstream and named in the pull request body. **The instrument that missed this: parity widened 2026-09-02 (Phase 4, 04-12)** — `tools/identity-parity.rb` now BUILDS each app unsigned (`build` only, never the test action — an unsigned UI-test runner raises Gatekeeper) and reads the built bundle with `plutil -extract <Key> raw`, comparing `CFBundleIdentifier` / `CFBundleDisplayName` / `CFBundleName` / `NSHumanReadableCopyright` against `BUNDLE_ID` / `DISPLAY_NAME` / `APP_PRODUCT_NAME` / `COPYRIGHT` as `bin/lib/xcconfig.rb` resolves them, printing the `©` as bytes on every run. The bundle is located from `BUILT_PRODUCTS_DIR` + `INFOPLIST_PATH` of a dump taken with the same flags as the build, never by `find`. Driven red the only way this half can honestly be driven red — against the ARTEFACT rather than the manifest: with `DISPLAY_NAME = Negative Control` in the xcconfig and the bundle NOT rebuilt (`--reuse-build`), the tool reports the built `Shipkit Pipes` against the file's `Negative Control` and exits 1; rebuilt into a second derivedData tree the same bundle reads `Negative Control`, which is 03-09's observation re-made through the tool's own path — evidence `.planning/…/evidence/04-12-T2-parity-plist-controls.txt`. `--skip-plist` changes the verdict wording to `PARITY OK (build settings only; plist half skipped)`, because a skipped half that reads as full parity is the same defect one level up. This row's own fix is re-confirmed from the artefact by the widening: the built iOS plist now carries `NSHumanReadableCopyright` with the `©` as `c2 a9` on both generators. |
| UL-031 | 2026-09-02 | 3 | A presence gate over an xcconfig that decides "non-empty" with a bare non-space match after the `=` accepts a value that is only a `//` comment. In xcconfig `//` opens a comment at any position in the value, so `KEY = // disabled` resolves to the empty string in Xcode — the key is simply absent from `-showBuildSettings` — while the gate matches the first `/` and exits 0. That is the exact input the gate exists to refuse, reached by the most natural way of disabling a line, and a test that asserts the same property with the same regex passes with it: the gate and its guard fail together | No - in scope | Merged [#282] | [#282](https://github.com/indiagrams/apple-shipkit/pull/282) | Found by this fork's Phase 3 security audit (T-03-06 / F-01, also 03-REVIEW WR-01) and reproduced against a fixture: `BUNDLE_ID = // temporarily disabled` printed `identity preflight ok`, exit 0, under ruby 3.3.12 and 4.0.6. Xcode's behaviour was then observed rather than inferred — probe keys appended to the identity xcconfig and read back through `xcodebuild -showBuildSettings` on Xcode 26.1.1 (17B100), XcodeGen 2.46.0: `KEY = // x` and `KEY = //` resolve to "" (absent from the dump); `KEY = value // note` resolves to `value`; `KEY = https://example.com/x` resolves to `https:` because the comment opens inside the URL; a lone `/` resolves to `/` and `a/b` to `a/b`. **Fix, one predicate body in two files:** cut the value at the first `//`, then require a non-space character — and nothing tighter, because a single `/` is a real character and a copyright string may legitimately carry one. Driven red first on the unfixed code (three comment shapes exit 0), then green (exit 3 naming the variable; a real value with a trailing comment still exit 0), through the generator (`xcodegen generate` exit 3, no project written) and in the guard (red naming the file and the variable on the same input the unmodified guard had ticked). Filed in the same change as the fix. **Upstream carries the same predicate:** `git show upstream/main:bin/preflight-identity.rb` (`c6c324f`, apple-shipkit #281) has `def defines_non_empty?` at line 98 with `[ \t]*=[ \t]*\S` at line 99 — the preflight went upstream in #281 with this hole in it, and #281's `ci/check-identity.sh` runs it on every pull request. **Opened upstream 2026-09-02 (04-14) as [#282](https://github.com/indiagrams/apple-shipkit/pull/282)**, in one pull request with UL-032 because the same parser is the fix for both: the predicate was not tightened, it was deleted in favour of a shared `bin/lib/xcconfig.rb` whose rows were read back from `xcodebuild -showBuildSettings` rather than inferred, because a tighter predicate would still have left UL-032 needing a *value* and therefore keeping a second reader. The branch was hand-authored in a separate clone against upstream's own text at `c6c324f` (nothing pushed from this repository; its `upstream` push URL is `no-push`), the `//` hole reproduced RED against upstream's unmodified script under Ruby 3.3.12 and 4.0.6 before the fix went in, and the diff swept for this fork's identity values, all zero. Until it merges, the fork's `bin/preflight-identity.rb` stays divergent from the template's (UL-034). |
| UL-032 | 2026-09-02 | 3 | A release script that derives the bundle identifier by grepping the generator manifest's `PRODUCT_BUNDLE_IDENTIFIER:` line stops working the moment the manifest carries a `$(VAR)` reference instead of a literal — which is exactly the state the template's own identity move (UL-023) puts the manifest in. The grep then yields the unresolved reference `$(BUNDLE_ID)` as if it were a value, the `-z` emptiness guard beside it can never fire, and on the manual-signing path the string is written as the `provisioningProfiles` dict key of the ExportOptions plist, so `exportArchive` fails only after a full signed archive. The script's own documented direct invocation is exposed; the Fastfile's release lane is shielded only because it happens to pass `RELEASE_BUNDLE_ID` | No - in scope | Merged [#282] | [#282](https://github.com/indiagrams/apple-shipkit/pull/282) | Found by this fork's Phase 3 code review (03-REVIEW WR-02) and security audit (F-02) and reproduced on the real script with no signing flags: under `bash -x`, `ci/local-release-check.sh v0.0.0` assigned `BUNDLE_ID='$(BUNDLE_ID)'` and exited 0 — a check that structurally cannot fail, the shape this phase exists to eliminate; it also overwrote the value `.bootstrap.env` had just exported. **Fix, applied fork-side in this change as a forward-port:** read `BUNDLE_ID` from `app/Identity.xcconfig` as Xcode reads it (last assignment wins, `//` comment cut off), and reject an empty value or any `$(` reference with a named failure before any archive. Driven red first (the unresolved reference, exit 0), then green on the real value, then red on `RELEASE_BUNDLE_ID='$(BUNDLE_ID)'` and on a commented-out `BUNDLE_ID` (exit 1 naming the file, before the `xcodegen generate` step), each mutation restored. **Upstream carries the same line:** `git show upstream/main:ci/local-release-check.sh` (`c6c324f`, apple-shipkit #281) still greps `app/project.yml` at its line 145 while #281's own `app/project.yml` line 97 says `$(BUNDLE_ID)`, so the identity move went upstream with its release script still reading the manifest. `ci/` is template-owned here, so this is the D-46 reconciliation's forward-port half, paired with an upstream change authored against upstream's text; upstream-bound as a follow-up pull request, not opened in this change, hence `Pending`. **Amended 2026-09-02 (Phase 4, 04-03):** the `sed` reader this row describes as the fix has itself been replaced fork-side by a call to the shared parser `ruby bin/lib/xcconfig.rb` — it truncated at the first space or slash and was correct for `BUNDLE_ID` alone, which is UL-035. An upstream patch authored from this row's original text would carry that truncation upstream, so the upstream change owed here is the parser call, not the `sed`. The defect this row is about — reading the bundle id out of the generator manifest — is unchanged and still open upstream. **Opened upstream 2026-09-02 (04-14) as [#282](https://github.com/indiagrams/apple-shipkit/pull/282)**, in one pull request with UL-031 because the same parser is the fix for both — this half is why the fix is a parser and not a predicate: it needs the *value*, so a yes/no gate could not have served it and it would have kept a reader of its own. Upstream's grep was reproduced RED against upstream's own tree at `c6c324f` (it printed `$(BUNDLE_ID)`, so the `-z` guard below it could not fire) before the `ruby bin/lib/xcconfig.rb app/Identity.xcconfig BUNDLE_ID` call replaced it, and the new guard rejects an empty value or any unresolved `$(…)` reference — including one an operator supplies through `RELEASE_BUNDLE_ID` — before any archive. Until it merges, the fork's `ci/local-release-check.sh` stays divergent from the template's (UL-034). |
| UL-033 | 2026-09-02 | 3 | When a template moves the app's identity out of the build manifests into one tracked xcconfig, the agent-facing instructions that tell a forker *where the value lives afterwards* are the citations most likely to be left standing: the change updates the passages it touches (entitlement paths, the direct test invocation, the project-path antipattern) and leaves the invariants-table row that says "afterward, the value is set in `.bootstrap.env`" — so an agent reading top-down gets two incompatible instruction sets in one file, the UL-022 shape again | No - in scope | Merged [#288] | [#288](https://github.com/indiagrams/apple-shipkit/pull/288) @ `d74d89d` | Found by this fork's Phase 3 code review (03-REVIEW WR-06): five passages in `AGENTS.md` still named `<AppName>.entitlements`, a `<AppName>.xcworkspace` / `<AppName>-iOS` scan invocation and a `<APP_NAME>.xcodeproj` project path after the phase had made the project the constant `App` and put identity in `app/Identity.xcconfig`. Read against upstream rather than assumed: `git show upstream/main:AGENTS.md` (`c6c324f`, apple-shipkit #281) had already corrected four of the five — the entitlements row and the capability step (`app/iOS/App.entitlements`, "Constant paths; the rename does not touch them"), the unit-test invocation (`--project app/App.xcodeproj --scheme App-iOS`), the antipattern row ("Deriving the project path or scheme from `APP_NAME`") — and added an "App identity" row pointing at `app/Identity.xcconfig`. **Fork-side in this change:** those four passages and the new row aligned to upstream's wording byte for byte (convergence, not invention), and the fork-conventions section below the marker untouched. **What upstream did not correct, and this row carries:** the invariants-table row (`Do not sed-substitute … literals`) still ends "afterward, the value is set in `.bootstrap.env`" while `bin/rename.sh` upstream now writes `app/Identity.xcconfig`; corrected here to say identity is edited in `app/Identity.xcconfig` and only fastlane's `APP_NAME` / `BUNDLE_ID` handle stays in `.bootstrap.env`. Upstream-bound as a one-line follow-up alongside UL-031 / UL-032; not opened in this change, hence `Pending`. **Superseded 2026-09-02 (Phase 4, plan 04-04, D-58):** the fork's `AGENTS.md` row no longer says a fastlane `APP_NAME` / `BUNDLE_ID` handle stays in `.bootstrap.env`, because after 04-04 no release-path consumer reads those keys at all — fastlane resolves identity from `app/Identity.xcconfig` through `bin/lib/xcconfig.rb`. The correction this row proposes UPSTREAM must still say the handle stays, because upstream's `Fastfile` still reads `.bootstrap.env` and a patch telling upstream's forkers otherwise would be false against upstream's own code. The two texts therefore diverge BY DESIGN until the parser and the two-tier resolution go upstream together; the upstream one-liner is now a prerequisite of, not a substitute for, the UL-036 change. Recording this rather than silently rewriting the row is the point: a ledger row whose fork half and upstream half have drifted apart, with no note saying so, is how a later agent authors an upstream patch from stale fork text (UL-032's own amendment, filed 2026-09-02, is this repository's instance of exactly that). **Submitted 2026-09-03 as [#288](https://github.com/indiagrams/apple-shipkit/pull/288)**, opened against `658bd326609c4c8afa02dee90b42053beefd8faf` — the upstream head re-measured live at push time via `gh api repos/indiagrams/apple-shipkit/commits/main`, not carried from the plan, which predicted the stale `d714006`. One commit, three files, +13/-3. **The correction as sent upstream KEEPS upstream's `.bootstrap.env` handle language**, exactly as this row's 2026-09-02 amendment requires: upstream's `Fastfile` still reads those keys, so a patch telling upstream's forkers the handle is gone would be false against upstream's own code. The two texts therefore still diverge BY DESIGN, and this PR does not close that divergence — it makes the upstream half internally consistent. **Two things travelled with it that the original row did not name.** `SCOPE.md:11` named `app/HelloApp/`, measured absent at five upstream refs including the root commit `f9cd5a8` while `app/Shared/` is present at all five; corrected upstream so the fork's `:11` fix becomes convergence rather than divergence. And the template-owned row's "open an upstream issue at" pointer now says outright that the slug names the TEMPLATE, because `bin/rename.sh`'s Step D substitutes that slug in 15 tracked files — measured with the script's own `PATHSPEC_EXCLUSIONS` applied — `AGENTS.md` among them, which is how this fork's own copy came to name the fork. **`SCOPE.md:77` was deliberately NOT included**, per 05-14's reasoning re-checked here: after the retirement `bin/rename.sh` still personalizes prose, contact email and repo slug, so "finish the rename" names a step that is narrower rather than gone, and changing it would take `git diff upstream/main -- SCOPE.md` from one differing line to two. Verdict is `Submitted`, not `Merged`: the eight required contexts were still running when this row was written. **Merged 2026-09-03T21:36:38Z as `d74d89d`**, squash, three files: `AGENTS.md` (two rows rewritten), `SCOPE.md` (one line), `CHANGELOG.md` (+10). Verified live rather than recalled — `gh pr view 288` reports `mergedAt` non-null and `gh api repos/…/commits/main` returns `d74d89d`, and `git merge-base --is-ancestor 658bd32 d74d89d` exits 0, so the history is a clean fast-forward from the base this row was opened against. **The conflict this row's own Notes predicted with the parameterization branch was real and is now resolved:** both pull requests rewrote the `Do not sed-substitute` invariants row, both changed its Why cell to identical text, and only the parameterization one rewrote the Instead cell. The resolution keeps THIS row's sentence about `.bootstrap.env` retaining its own handle — which this row's 2026-09-02 amendment requires, because the Fastfile and the release workflows still read those keys — and replaces only the instruction to run a command the retirement makes refuse. The other row this pull request touched, the upstream-issue pointer, merged without conflict and is preserved intact. |
| UL-034 | 2026-09-02 | 4 | Adopting whole files from an upstream whose history is unrelated to the fork's: `git checkout upstream/main -- <path>` copies the exact blob **and** its mode (the `100755` bit survives), where `git show upstream/main:<path> > <path>` writes the bytes and silently drops the executable bit; `git format-patch` / `git am` fail on every hunk because the context lines differ, and `git merge` is unavailable with no merge base. Verify each adopted file with `git diff --quiet upstream/main -- <path>` (exit 0 = byte- and mode-identical), and cite the upstream commit SHAs in the fork's commit message, because the adopted blobs carry no author | No - in scope | Submitted [#289] | [#289](https://github.com/indiagrams/apple-shipkit/pull/289) | Phase 4's one-time sync with apple-shipkit `upstream/main` = `c6c324f` (D-53), landed as one attributed commit. **As-is, byte- and mode-identical (11):** `ci/check-identity.sh`, `ci/check-app-icon.sh`, `ci/check-embedded-floors.sh`, `.github/workflows/pr.yml`, `Makefile`, `ci/local-check.sh`, `.github/workflows/canary-local-mode.yml`, `.github/workflows/release.yml`, `bin/lib/bootstrap.rb`, `bin/ship.rb`, `test/sh_stream_test.rb`. No reference-forced additions: every path the synced `pr.yml`, `Makefile` and `ci/local-check.sh` invoke already existed. **Deliberately not taken verbatim (2):** `bin/preflight-identity.rb` — the fork's `tools/preflight-identity.rb` body moved to upstream's path (fork ahead on content: the value is cut at its first `//` before the non-empty test, UL-031; upstream's `\S` predicate would reopen the hole), divergent by 171 lines until UL-031 merges; `ci/local-release-check.sh` — upstream's two new steps (`App icon`, `App identity`) taken by hand, upstream's `PRODUCT_BUNDLE_IDENTIFIER:` grep of `project.yml` NOT taken (UL-032 regression; the fork's `Identity.xcconfig` reader stays). `fastlane/Fastfile`: the four identity constants (`App-iOS`, `App-macOS`, `App-%s.ipa`, `App-%s.pkg`) forward-ported; upstream's `HelloApp` / `com.example.helloapp` fallbacks not taken (D-58 replaces the fallback chain in 04-04). **Recorded remainder, still divergent by design (template-owned, `git diff --stat HEAD upstream/main` line counts):** `bin/rename.sh` 329, `ci/test-rename.sh` 107, `ci/test-rename-gates.sh` 59, `bin/switch-to-xcodegen.sh` 41, `bin/switch-to-tuist.sh` 4, `ci/test-switch-to-xcodegen.sh` 6, `ci/test-switch-to-tuist.sh` 6 (all Phase 5, IDENT-10 — not adoptable here); `bin/lib/version_resolver.rb` 61; `.github/workflows/bootstrap-doctor-matrix.yml` 40; `bin/take-readme-screenshots.sh` 24; `bin/refork-smoketest.sh` 10; `bin/adopt.rb` 4; `ci/bump-asc-version.rb` 4; `bin/preflight.sh` 2; `.github/workflows/canary-trigger.yml` 2; `fastlane/Appfile` 2; upstream-only `test/build_number_test.rb` 94 and `test/stubs/spaceship.rb` 10 (referenced by nothing synced). Fork-owned files (`app/Identity.xcconfig`, `app/project.yml`, `app/Project.swift`, `docs/`, `test/`, `tools/`, `AGENTS.md`, `CHANGELOG.md`, `README.md`, `fastlane/metadata/`) differ by design and are not divergence. **Attribution:** `c6c324f` (#281) preflight / `check-identity` / `pr.yml` / Fastfile constants; `9be3ad2` (#275) `check-app-icon`; `9c30afd` (#277) `check-embedded-floors`. **Inherited verbatim on purpose:** upstream's `pr.yml` `config` job now runs `ci/check-identity.sh` — a non-gating duplicate of the preflight the fork's required `review notes` job already runs; keeping the file byte-identical is worth more than the eleven seconds (CONTEXT "Claude's Discretion", resolved). **The two deliberately-not-taken files now have a closure path:** their upstream counterparts are under review as [#282](https://github.com/indiagrams/apple-shipkit/pull/282) (UL-031, UL-032), opened 2026-09-02 (04-14). When it merges, `bin/preflight-identity.rb` and `ci/local-release-check.sh` become adoptable as-is and this row's divergent-by-necessity pair drops to zero; until then the divergence is a stated debt with a number on it rather than an open-ended one. **Partially closed 2026-09-03 (Phase 5, 05-10) — and this cell's own closure prediction, two sentences up, is now measured FALSE.** `bin/rename.sh` was adopted from `upstream/main` `658bd32` (blob `a42a82c36b128ddb09142eda46506fd6d694f969`), with `git diff --quiet upstream/main -- bin/rename.sh` exit 0 and `git ls-tree HEAD` reporting `100755` after the commit, so the 329-line remainder recorded above — measured against `c6c324f` and left standing as the record it is — is 0 as of `658bd32`. `bin/verify-rename.sh` needed no adoption at all: blob `529910dba2e359784a3a7101f7bb599c5f918d3f` at `e773cfc`, `212b489`, `HEAD` and `upstream/main` alike, measured here rather than carried from the plan, and plan 05-12 deletes it regardless. **The other two do NOT close, and #282 merging did not make them adoptable.** #282 landed upstream as `d714006` and both of its fixes are in `upstream/main` (the preflight calls `Xcconfig.value`; the release check calls `ruby bin/lib/xcconfig.rb`), but the fork-side hardening AROUND those fixes did not travel with them, re-measured 2026-09-03 under ruby 3.3.12 and 4.0.6 rather than cited from research: `ruby bin/preflight-identity.rb --config app` gives the fork's named **exit 2** ("is a directory, not a file", naming the resolved path and the cwd), while upstream's copy lets `Errno::EISDIR` raised at `bin/lib/xcconfig.rb:164` escape uncaught through `preflight-identity.rb:183-184` — whose `rescue` catches only `Xcconfig::MissingInclude` — and exits **1**, a code that file's own contract at :32-48 already spends on "unknown or malformed argv" and which the fork was measured still spending there (a bogus flag and a `--config` with no PATH both exit 1). Adopting upstream's copy would therefore make "your config path is a directory" indistinguishable from "you passed a bad flag" — an IDENT-09 regression. `git show upstream/main:ci/local-release-check.sh \| grep -c IDENTITY_XCCONFIG` is **0** at `658bd32` and was 0 at `d714006` too, against 12 in the fork: upstream hardcodes `app/Identity.xcconfig` at its :176, so adopting deletes the announcing fixture knob (`docs/BOOTSTRAP.md:197`) that is how that gate's red controls are driven. Driven here rather than asserted, three arms: knob set announced `identity: reading <fixture> (IDENTITY_XCCONFIG override) — not a release configuration`; knob unset printed the announcement 0 times; and a fixture whose `BUNDLE_ID` is only a `//` comment failed AT the BUNDLE_ID step naming the fixture path, which is what proves the knob is honoured rather than merely announced. Both files therefore close only when the D-77(b) pull request merges, carrying the fork's hardening upstream as a third commit (plan 05-15) — dated and partial, not aspirational. **Submitted 2026-09-03 in [#289](https://github.com/indiagrams/apple-shipkit/pull/289).** Measured present in the patch rather than assumed from a plan: `git diff --name-only d74d89d..303245a` lists BOTH `bin/preflight-identity.rb` and `ci/local-release-check.sh`, which is this row's own amendment closing by upstreaming rather than by adopting. |
| UL-035 | 2026-09-02 | 4 | A template that reads its own identity config from several places grows one hand-rolled reader per consumer, and every one of them is correct about the value it was written against and wrong about the next. Four here, mutually incompatible, all reading the same xcconfig: a gate with a presence predicate and no value extractor; a `sed` capture of `([^[:space:]/]+)` in the release script; a `.env` parser in the Fastfile that would turn a `//` comment containing an `=` into a key; and a byte-identical copy of the gate's predicate inside the gate's own test, with a comment instructing the next reader to keep it identical. The last is the worst of the four: a guard that copies its subject's predicate goes green exactly when the subject is wrong in the same way, which is how the `//` hole (UL-031) passed a gate and its test together. The replacement for a bad reader is not a better reader — it is ONE parser, with a fixture set measured against the real toolchain rather than against any reader in the tree, and every consumer calling it, the shell ones by shelling out rather than growing a fifth | No - in scope | Pending | — | Measured on this fork's live `app/Identity.xcconfig` on 2026-09-02, not quoted: the release script's `sed` returned `BUNDLE_ID` and `APP_PRODUCT_NAME` correctly and truncated `DISPLAY_NAME` to `Shipkit` (from `Shipkit Pipes`) and `COPYRIGHT` to `Copyright` (from the full line with its `©`) — correct for one of four keys, and only because a bundle id happens to contain no spaces. **Landed fork-side across 04-02 and 04-03:** `bin/lib/xcconfig.rb`, a zero-`require` Ruby module (not even stdlib, so a `bundler-cache: false` required-context job stays honest and fastlane can `load` it) that is both a library and a CLI via `if $PROGRAM_NAME == __FILE__`, so bash calls it instead of reimplementing it; `test/xcconfig_test.rb`, 52 assertions whose expected values were OBSERVED with `xcodebuild -showBuildSettings` on Xcode 26.1.1 — that is where the independence comes from once the duplicate predicate is deleted, and it is why the fixture header forbids 'correcting' a row to match an implementation. Then the three in-tree readers repointed onto it: `bin/preflight-identity.rb`, `test/identity_test.rb`, `ci/local-release-check.sh`. **Two things a reimplementation must not lose.** (1) The parser distinguishes `nil` (never assigned) from `""` (assigned but empty or comment-only) and RAISES on a hard-`#include` miss rather than returning what it managed to read; a caller that collapses the raise into 'that key is empty' recreates the ambiguity the parser exists to remove. The preflight maps it to its file-unreadable code, the shell caller to a message that says the exit was NOT the empty-value code. (2) 'What would the toolchain resolve here' and 'what does THIS file's text say' are different questions, and a leak check asks the second: the identity config `#include?`s the gitignored local one, so a Team-ID-in-git warning wired to the resolving reader fires on every developer machine about a value that is not tracked — a warning that is wrong every time is one nobody reads the day it is right. `Xcconfig.own` is the second question asked of the same body, added test-first for exactly this. **Upstream state, read not assumed:** `git show upstream/main:bin/lib/` (`c6c324f`, apple-shipkit #281) contains only `bootstrap.rb` and `version_resolver.rb` — there is no parser — and upstream's `ci/local-release-check.sh:145` still greps `app/project.yml` for `PRODUCT_BUNDLE_IDENTIFIER:`. Upstream-bound with UL-031 and UL-032, which this supersedes the fix half of; not opened in this change, hence `Pending`. |
| UL-036 | 2026-09-02 | 4 | Inside a fastlane `Appfile`, a config guard cannot fail with `raise` or `UI.user_error!` — `CredentialsManager::AppfileConfig.try_fetch_value` evaluates the file inside `rescue => ex; puts(ex.to_s); return nil`, so the message prints and the caller receives `nil` as though the value were merely absent. A template whose Appfile validates anything therefore has a guard that reports and then lets the run continue with no identity, which is worse than no guard: it produces a reassuring log line on the way to an unidentified build. The construct that works is `abort` (SystemExit is not a StandardError, so the rescue does not catch it). The same file also cannot locate itself — it is `eval`'d, so `__FILE__` is `"(eval at …)"` and `__dir__` is `nil` — which quietly defeats the `__dir__`-relative path every other file in a template uses | No - in scope | Pending | — | Measured against the bundled fastlane 2.238.0 (pinned in `Gemfile.lock`) on 2026-09-02, first on scratch Appfiles during Phase 4 research and then re-measured on this repository's own shipping `fastlane/Appfile` as a red control in 04-04: with the guard written as `raise`, the probe printed the message and then `app_identifier=nil` at **exit 0**; with `abort`, the message went to stderr, **exit 1**, and no `app_identifier=` line was printed at all. Both transcripts are in `evidence/04-04-T2-fastlane-integration-controls.txt`, and the mutation was reverted with `git checkout --` and the control re-run to prove the revert. Two further measured facts a reimplementation needs: inside the Appfile eval `Dir.pwd` is the `fastlane/` directory (so the repo root is `File.expand_path("..", Dir.pwd)`) while in the `Fastfile`, which fastlane loads by file, `File.dirname(__FILE__)` works normally; and the Appfile is re-evaluated on EVERY `try_fetch_value` — three times in one observed lane run, and three override banners in one `fastlane lanes` run here — so anything it does must be cheap and side-effect free. **Upstream relevance:** upstream's template `Appfile` is a bare literal today, so it has no guard to get wrong yet; the moment it resolves anything (which is where the identity work is heading) it inherits this trap. Belongs in the same upstream change as UL-035's parser and UL-031's predicate, and is a prerequisite for UL-033's one-liner. |
| UL-037 | 2026-09-02 | 4 | A template's release workflows read the app's identity from GitHub Actions **repository variables** that each fork must set by hand, that live outside the tree, that no diff shows and no test reads — while the tracked xcconfig the BUILD already resolves carries the same two values. That is two sources for one fact, and the one CI trusts is the one nobody can see. Worse, the template's own bootstrap tooling re-sets both variables from a gitignored config file on every routine `make bootstrap-fork`, so a hand-corrected live value is silently reverted, and a deletion is not a decision but a temporary state. The fix is not a better variable: it is for every workflow to shell out to the same parser the rest of the tree uses, against the same tracked file the build reads, and for the bootstrap step to stop writing repository configuration entirely | No - in scope | Pending | — | **Measured on this fork 2026-09-02, not inferred.** `gh variable list --repo indiagrams/ios-macos-smoketest` returned `APP_NAME=App` (set 2026-09-02) and `BUNDLE_ID=com.indiagram.smokeapp` (set 2026-08-29). `com.indiagram.smokeapp` is the TEMPLATE's bundle id; this app's, in the tracked `app/Identity.xcconfig` that both generators and the Fastfile resolve, is `com.indiagram.shipkitpipes.ios`. So the CI release path was pointed at the wrong bundle id, and `release.yml:310` would have hard-errored `vars.BUNDLE_ID is not set on this repo` had anyone deleted it. Nothing had caught either fact because no build has ever been uploaded from this repo — the second source was wrong for four days in a repository whose whole subject is release correctness (04-CONTEXT C-16). **The re-creation half, which is what makes this a ledger row rather than a one-line fix.** `bin/lib/bootstrap.rb`'s `GHSecrets#do_it` iterated `REQUIRED_VARIABLES = %w[APP_NAME BUNDLE_ID]` and ran `Sh.run!("gh", "variable", "set", key, "--body", val, "--repo", config.repo_slug)` for each, sourced from the gitignored `.bootstrap.env`; `check` returned `:pending` until both existed, so `make doctor` would nag a correctly-configured fork into re-creating them. `bin/refork-smoketest.sh` ends by telling the operator to run `make bootstrap-fork`. That is UL-026's mechanism, and it means the obvious remedy — delete the variables — reverses itself on the next routine command with no diff, no warning and no gate. Same class as UL-003 (a script that silently empties the required-status-checks array) and the reason D-63 exists. **Landed fork-side in this change, in this order, because the order is load-bearing.** (1) The synced `pr.yml` (UL-034) had already stopped reading `${{ vars.APP_NAME \|\| 'SmokeApp' }}` in six places — deleting `APP_NAME` before that sync would have resolved the project path to `app/SmokeApp.xcodeproj`, which does not exist, and turned all six `app (…)` required contexts red. The sync was observed 9/9 green WITH the variables still present before anything was deleted. (2) `release.yml` and `canary-local-mode.yml` each lost the workflow-level `env:` block and gained a `Resolve app identity from app/Identity.xcconfig` step: two `ruby bin/lib/xcconfig.rb app/Identity.xcconfig <KEY>` calls exported through `>> "$GITHUB_ENV"`. `bin/lib/xcconfig.rb` has ZERO require lines, not even stdlib (asserted by `test/xcconfig_test.rb`, so it cannot rot), and was verified to run unchanged on `/usr/bin/ruby` 2.6.10, which is what makes "shell out to the shared parser" available to a workflow step with no Gemfile, no `bundle install` and no position requirement relative to the toolchain steps. `\|\| VAR=""` on each assignment is load-bearing: the parser exits 3 on an undefined-or-empty key and `set -e` would otherwise kill the step before the named `::error::` could print. (3) `GHSecrets` stopped writing variables, with `test/gh_secrets_test.rb` — stubbed `Sh`/`IO.popen`, real method bodies, argv as the subject — observed RED first on the unedited code (7 of 10 assertions failing, the first naming both `gh variable set` argv arrays) and green after, under ruby 3.3.12 and 4.0.6. Its three secret-half assertions passed in BOTH runs, which is what shows the harness exercises the real body rather than asserting nothing. (4) Only then were the variables deleted, bracketed by `gh variable list` captures and preceded by a `PHASE4_REVERT_PROBE` set/delete round trip that demonstrated the mechanism's reversibility rather than asserting it. **Two things a reimplementation must not lose.** The guard shape survives relocation: each workflow keeps a `[ -z "${BUNDLE_ID:-}" ]` check at the point of USE, not only in the resolving step, so that an edit which reorders, conditions or drops the resolving step cannot reach an archive with no bundle id — a single guard co-located with the thing it guards is a guard that disappears with it. And the deletion and the tooling edit must be ONE change: either alone is a state, not a decision. **Upstream state, read not assumed:** `git show upstream/main:.github/workflows/release.yml` (`c6c324f`, apple-shipkit #281) still carries the `${{ vars.APP_NAME }}` / `${{ vars.BUNDLE_ID }}` env block and the `gh variable set` guidance in its header, and upstream's `bin/lib/bootstrap.rb` still carries `REQUIRED_VARIABLES` and the `gh variable set` loop, so upstream forks inherit both halves. This extends UL-026, whose narrower remedy it supersedes, and rides with UL-035's parser: the workflow change is not authorable upstream until `bin/lib/xcconfig.rb` exists there. The fork's `release.yml` and `canary-local-mode.yml` therefore diverge from upstream on this until it goes upstream (D-53's recorded remainder). Not opened in this change, hence `Pending`. **Filed in the same row, because it is the same file and the same change:** `.github/workflows/bootstrap-doctor-matrix.yml:194` wrote a real address on the org's own mail domain (local part `smoketest`, deliberately not spelled in full here — this file is tracked and public, and D-67 does not allowlist that domain) into a synthesized `.bootstrap.env` in a public workflow; it is now an address on the RFC 2606 reserved `.invalid` TLD, the pattern `canary-local-mode.yml:557` already used (IDENT-06). Neither the old nor the new literal is spelled out in this file: the five fork-owned documents are swept for anything email-shaped by `test/docs_structure_test.rb`, which has no domain allowlist until D-67's lands, and a ledger row that had to be exempted from the gate it is describing would be its own kind of joke. D-67 deliberately does not allowlist the org address, so a re-add fails a gate rather than depending on someone remembering. **Extended 2026-09-02 (Phase 4, 04-08): both halves of that sentence are now machinery.** The PII half is `tools/domain-allowlist.txt`, read by `tools/check-contamination.rb`: six dated, reasoned rows of `domain<TAB>local-part<TAB>date<TAB>reason`, and every address-shaped string in every tracked file fails unless its domain has a row whose local part admits it. Fail-CLOSED is the whole design — a deny-list of addresses somebody already thought of is green for every address nobody has thought about yet — and the org domain carries exactly ONE row, for the published contact, so the two role addresses this fork had grown (local parts `smoketest` and `refork`, deliberately not spelled in full here for the reason this row already gives) fail by the ABSENCE of a row rather than by anyone's memory. The list is itself a gate in the same five ways the identity allowlist is: a wildcard or non-hostname domain cell, an undated or malformed row, a duplicate, and a row matching no address in the tree each FAIL. **The false-positive class the existing sweep hides.** `test/docs_structure_test.rb:671`'s `EMAIL` regex covers five fork-owned documents; run tree-wide the identical regex matches the macOS icon filenames — `icon_<size>@2x.png` parses as local-part @ domain . tld — measured 2026-09-02 as twelve occurrences in four tracked files. That class is excluded in CODE, by one anchored expression evaluated before the address regex, not by a path exemption (which would blind the gate to a real address in the same file) and not by a `.png` domain row (which would admit any local part on a pretend TLD). **The exclusion was proven load-bearing rather than assumed harmless:** commented out in a scratch copy of the gate, the run reported fourteen red lines — the thirteen icon filenames plus the gate header's own prose example — while a `@2x.io` lookalike is still reported with the exclusion in place, which is a contract-test case. **The `vars.*` half is the construction proof of criterion 2 (D-68).** `VARS_ALLOW` freezes one file and one number — `.github/workflows/dependabot-automerge.yml`, three lines — and every other `vars.` read under `.github/workflows/` fails with its file and line; a fourth read in the allowlisted file fails too, so a new dependency cannot be added under its cover, and a tree that has workflows but has lost that file reports `found 0` rather than `0 reads`. Scope is workflow files only: `vars.` in a doc is prose about workflows, and a gate that flagged prose would be trained away inside a phase. **Where the two role addresses went, since template-owned files are involved (UL-034 records `bin/refork-smoketest.sh` as divergent by 10 lines, now more):** `CODE_OF_CONDUCT.md` and `SECURITY.md` now publish the maintainers contact — the one this list does allowlist — rather than the repository-named role address; `bin/refork-smoketest.sh` writes that same contact as the `APP_EMAIL` it seeds a recreated repository with, and its throwaway orphan-commit git author is now on the RFC 2606 `.invalid` TLD. The `.invalid` treatment was deliberately NOT applied to the two governance documents, for the reason recorded as UL-039. **Measured after the change, on the live tree:** 147 files scanned, 62 address occurrences seen, 0 unallowed, 3 `vars.` reads, 0 unallowed; five live red controls plus the scratch-copy proof in `.planning/…/evidence/04-08-T3-pii-vars-live-controls.txt`. |
| UL-038 | 2026-09-02 | 4 | A template that ships a completeness check named `ci/check-identity.sh` — is the fork's own identity present and non-empty everywhere the build needs it? — and a fork that needs a **contamination** check — is the TEMPLATE's identity absent everywhere it is not explicitly permitted? — will end up with two files of the same name asking opposite questions, and a green run of either says nothing about the other. The generic capability is the second one: a whole-tree scan of `git ls-files` against a frozen literal set covering every identity the template has ever shipped, gated by an allowlist whose every row carries path + expected COUNT + ISO date + reason, and where a stale row, a short count, an undated or malformed row, a duplicate row and an out-of-policy wildcard each FAIL. The count is the load-bearing part: a path-level exemption fully exempts the path, so a real regression standing next to a legitimate guard literal passes — and guard literals are unavoidable, because a check that rejects a placeholder must spell the placeholder it rejects | No - in scope | Pending | — | **IDENT-11's path is amended in place with dated provenance (D-69, 2026-09-02):** the requirement's literal wording named `bin/check-identity.sh`, and the gate was built at `tools/check-contamination.rb` instead. `bin/` and `ci/` are template-owned and `bin/refork-smoketest.sh` deletes this repository and recreates it from the template, so a fork-authored file at a template-owned path is removed wholesale on the next refork — which would silently delete this phase's main gate. That is the UL-003 failure class, and D-63 exists to close it. **Upstream proposal, to accompany UL-031's pull request:** rename the template's script `ci/check-identity-complete.sh`, then take the contamination gate and its allowlist grammar as new files. **The `config`-job call site was inherited verbatim** (04-CONTEXT "Claude's Discretion", resolved): upstream's synced `pr.yml` runs `ci/check-identity.sh` from a job that is not a required context here, so it is a non-gating duplicate of the preflight the required `review notes` job already runs — keeping the file byte-identical is worth more than the eleven seconds. **Measured on this tree, 2026-09-02:** 146 files scanned, 15 skipped as binary, 41 allowlisted, 0 unallowed; rows by class 17 template doc / 7 not-synced template file / 7 fixture / 4 historical record / 4 guard / 2 self; zero rows under `app/` and zero rows for configuration anything reads — the three that remained (a placeholder bundle-id fallback in `ci/bump-asc-version.rb` that would have bumped the template's own App Store record, a comment in `review-notes.yml`, a stale usage example in `tools/identity-parity.rb`) were fixed at the source in `081d2b8` rather than permitted. Seven live red controls recorded in `.planning/…/evidence/04-07-T3-contamination-live-controls.txt` before the gate was trusted. |
| UL-039 | 2026-09-02 | 4 | A template's rename substitutes the maintainer contact address into the fork's `SECURITY.md` (the disclosure channel) and `CODE_OF_CONDUCT.md` (the enforcement contact), so whatever value a forker passes once at fork creation becomes the repository's PUBLISHED contact — and the same value is hardcoded a second time, as an `APP_EMAIL` constant, in the script that recreates the repository from the template. Two sources for one fact again, and the copy in the script is the one no reader of the published documents can see. This matters the moment the fork adds a tree-wide personal-information rule: the address the rule wants gone may be the one the repository publishes for security reports, and replacing it with an RFC 2606 `.invalid` address — the correct answer for synthetic fixtures and throwaway git authors — leaves a public repository advertising an undeliverable vulnerability-disclosure channel. A privacy rule that silently disables a security-reporting path has traded a smaller harm for a larger one | No - in scope | Pending | — | Found in Phase 4 04-08 while making D-67's fail-closed domain allowlist green on HEAD. The phase's own decision record had described the published contact as the maintainers address and named `SECURITY.md` as one of its sites; measured on the tree, `SECURITY.md:11` and `CODE_OF_CONDUCT.md:40` in fact carried a role address whose local part is this repository's own name — the value `bin/refork-smoketest.sh:104` seeds a recreated repo with — and the maintainers address appeared only inside the rename machinery (`bin/rename.sh`, `bin/verify-rename.sh`, `ci/test-rename.sh`) as the FROM side of the substitution. The decision was intact and its inventory was wrong about which file held which, which is the ordinary outcome of a fact recorded once and not re-measured. **Resolved by making the tree match the decision rather than by widening the allowlist:** the two governance documents now publish the maintainers contact, which is the one local part the org domain's single row admits, so the policy keeps a deliverable channel; the throwaway git author identity in the refork script took the `.invalid` treatment, because nothing delivers mail to an orphan commit's author. `bin/rename.sh` has no guard against `--email` equal to the template's own value, so the substitution is a no-op and the tree is now a fixed point of its own refork — which it was not before. Adding a second row on the org domain was the alternative and was refused: it would silently re-admit the addresses IDENT-06 removed, and `test/contamination_test.rb` now asserts there is exactly one such row and asserts which local part it names. **Upstream shape:** the template's `bin/rename.sh` already substitutes the contact into both documents, so upstream inherits the whole mechanism; the change worth proposing is that the refork/bootstrap path stop carrying its own copy of the address and read the one the documents publish, the same one-source-per-fact move as UL-035 and UL-037. Not opened in this change, hence `Pending`. **No address is spelled in this row** — this document is one of the five swept by `test/docs_structure_test.rb` and is now also covered tree-wide by the gate this row is about. |
| UL-040 | 2026-09-02 | 4 | A secret scan added to a template has five ways of being green while seeing nothing, and four of them are invisible in a passing log. (1) **The action is not free for the fork.** `gitleaks`' GitHub Action requires a commercial licence for organisation-owned repositories while the binary it wraps is MIT, so a template that ships the Action either costs its org-owned forks money or gets silently deleted from their workflows; the pinned, digest-verified binary costs nothing and is auditable. (2) **A working-tree scan is not a history scan.** `gitleaks dir` misses a credential that was committed and then removed in a later commit — measured, both directions, on the same clone — and removing a credential in a follow-up commit does not unpublish it. Scan git history, not the checkout. (3) **A shallow checkout makes a history scan vacuous.** On a `--depth 1` clone the same verified binary with the same flags reported `1 commits scanned … no leaks found` and exited 0 with the token sitting in history. `fetch-depth: 0` fixes it and is NOT the gate: it is one YAML line anybody can delete with nothing going red, so the scanner's wrapper must assert `git rev-parse --is-shallow-repository` = false in code and refuse otherwise. The same class of silent pass exists for a target that is not a repository at all (`scanned ~0 bytes … no leaks found`, exit 0). (4) **The default rules flag documentation, so the gate cannot pass on a clean HEAD without an allowlist** — ten findings on this repository's history, every one a placeholder or an `ENV.fetch` example — and a gate that is red on a clean tree is a gate somebody disables within a week. But an allowlist is where the hole gets drilled: one entry with a match-everything regex was observed turning an injected token from exit 42 into exit 0 with the scan still printing "no leaks found", so the allowlist needs its own test (no catch-all or trivially short regex, no repo-wide line-target entry, dated descriptions, no `paths` row that matches nothing, no key the scanner ignores silently). (5) **The red control itself can be a fake.** `ghp_` followed by 36 identical characters is not detected, and neither is AWS's own documented example key: a control built from either would either "prove" the gate broken or, worse, be trusted after never having run red. Generate the fake from `/dev/urandom`, in a scratch clone, never on a real branch. Also: `--exit-code 42` is what separates "findings" from "the scanner failed" — the default exit for findings is 1, which is also the exit for a malformed config | No - in scope | Pending | — | Phase 4 04-09, IDENT-12 / D-62. Pinned gitleaks **8.30.1** (release published 2026-03-21T02:17:58Z); both digests copied from that release's own `gitleaks_8.30.1_checksums.txt` (999 bytes, re-fetched 2026-09-02 and byte-identical to the copy fetched during research) rather than computed from a downloaded tarball, which would verify the download against itself: `linux_x64` `551f6fc8…70eb` is what the runner executes, `darwin_arm64` `b40ab0ae…b6a5` is what was exercised locally. Implemented as fork-owned `tools/gitleaks.rb` (D-69: `bin/` and `ci/` are template-owned and `bin/refork-smoketest.sh` recreates this repository from the template, so a fork-authored file at a template-owned path is deleted wholesale on the next refork — the UL-003 failure class), Ruby stdlib only so `review-notes.yml` keeps `bundler-cache: false`, wired as a separate named step inside the already-required `review notes` job rather than as a new job — a tenth status context would run, report and block nothing. Exit contract 0 clean / 42 findings / 1 scanner error / 2 CANNOT RUN, the `ci/check-embedded-floors.sh` no-verdict idiom. Every claim above was observed before it was trusted: tampered digest → exit 2 printing both digests; depth-1 clone → wrapper exit 2 while the raw binary on the same clone exits 0; a `/dev/urandom` token committed then `git rm`'d → exit 42 with a redacted report naming file, line and commit, while `dir` on the same clone exits 0; a catch-all allowlist entry → exit 0, the config test rejecting that file, and exit 42 again once removed. Recorded in `.planning/…/evidence/04-09-T2-gitleaks-controls.txt`; the token's value appears nowhere and both scratch clones were deleted. **Upstream shape (SCOPE.md:31 puts secret scanners in scope):** the wrapper, the `.gitleaks.toml` skeleton and the config test generalise unchanged; what does not is this fork's three allowlist entries, which are about its own documentation. **What the scan cannot see, stated rather than implied:** `--log-opts=HEAD` covers the checked-out ancestry only, so commit `33f8e08` — a real given name in a fixture, off `main` via squash and reachable only by SHA — is NOT covered by it and remains a separate non-blocking human action. |
| UL-041 | 2026-09-02 | 4 | A doctor that verifies PRESENCE reports `done` on a placeholder, and the report is more dangerous than no report at all — the operator now has a green check saying the thing was verified. This template shipped that exact defect: `Icon1024` compared the icon's bytes against `ICON_1024_PATH`, so a fork that pointed `ICON_1024_PATH` at the placeholder was told `done`; the asset was a valid 1024x1024 PNG, every CI cell was green, App Store Connect's upload validation accepted it, and the first thing to notice was a human reviewer — Guideline 2.3.8, one full review cycle (upstream's own `ci/check-app-icon.sh` header records it). The generic capability is a doctor that reports DEPTH OF VERIFICATION per step and cannot report more depth than it reached: **tier 1** the thing exists, **tier 2** its content is not the template's, **tier 3** the value the build or Apple actually resolves is not the template's. Three parts make it hold. (1) A failure names the TIER and the OFFENDING VALUE, not a generic "identity check failed". (2) Unreachable depth is an explicit `verified to tier 2; tier 3 not reached: <reason>` and a tool that fails outright is **no verdict**, never a pass — a depth that could not be measured must never render as a depth that was. (3) A step declares its minimum meaningful depth and **THE RUNNER, NOT THE STEP**, downgrades a `done` from below it to blocked; that placement is the whole learning, because a step's own discipline is precisely what failed in the icon story and a fix that relied on the next step author being careful would have the same shape as the bug | No - in scope | Pending | — | Implemented here as `Bootstrap::Step::MIN_TIER` + `attr_reader :tier_reached` + `Step#detail`, `Bootstrap::IdentityAdopted` (registered immediately after `RenameStub`), `Bootstrap::Runner#render_result`, and `Icon1024`'s tier-2 content check shelling to `ci/check-app-icon.sh` — the script that exists because of this step — with `MIN_TIER = 2`. `IdentityAdopted` resolves through `bin/lib/xcconfig.rb`, the one reader (D-57), so the `//` cut is the same one every other consumer gets. **Two things upstream should take with it.** First, upstream's post-#281 `RenameStub#check` is a tier-2-ish check written as a bare regex (`/^\s*APP_PRODUCT_NAME\s*=\s*NAME\s*$/`) with **no `//` cut**, so `APP_PRODUCT_NAME = // disabled` satisfies it — the UL-031 hole in a second file, and a natural consumer of the shared parser once UL-031's pull request lands. Second, `TEMPLATE_VALUES` must list every identity the template has EVER shipped, not the current one: #281 replaced the old placeholders with new ones, so the literal changed while the fallback-placeholder shape did not, and a tier-2 check that knew only about the newest pair would pass a fork still carrying the older. **Measured on this tree, 2026-09-02, under bundler:** baseline exit 0 with `verified to tier 3: com.indiagram.shipkitpipes.ios / ShipkitPipes`; a fixture carrying the template bundle id → `tier 2 failed: BUNDLE_ID = <template> (template identity)`, `— needs fix`, exit 2; a fixture disagreeing with `.bootstrap.env` → the D-59 disagreement message with both values, exit 2; `ICON_1024_PATH` pointed at a byte-identical copy of the placeholder (so the old hash equality passed) → `tier 2 failed: icon is the template placeholder — ci/check-app-icon.sh: FAIL iOS 1024 icon: looks like a placeholder — flat colour or bare gradient (spread 0, need >= 40).`, exit 2; the generated project moved aside → `verified to tier 2; tier 3 not reached`, exit unchanged from baseline. **Two fixture knobs, each loud on stderr** (`IDENTITY_XCCONFIG`, `BOOTSTRAP_ENV`): a doctor run against a fixture that could pass for a real one would be a worse defect than the one the fixture tests for, so every overridden run announces itself. No tracked file was mutated to produce any of the reds. **Not done here, and named rather than rounded up:** the tier vocabulary was applied to the two identity steps only — the other nineteen still report status without depth, and `Config::REQUIRED_ALWAYS` is deliberately untouched (D-58's amendment), because removing the `.bootstrap.env` identity keys would break `make doctor`, which is this decision's own deliverable. Phase 5 owns that. **One claim in this cell was measured FALSE on 2026-09-03 and is left standing rather than edited, because a ledger row records what was believed and measured THEN; editing it would falsify the record it exists to be.** The cell states that upstream's post-#281 presence check accepts a value which is only a comment. Run against `git show upstream/main:bin/lib/bootstrap.rb` under both pinned interpreters it does not — it reports not-done. The hole belongs to the anchored non-space presence predicate (UL-031), closed upstream by #282. **UL-046 carries the corrected measurement**, including the inverse defect the real predicate did have: a correctly set value carrying a trailing comment ALSO reported not-done, which in an idempotent driver means re-running the mutation over an already-correct fork. Flagged by plan 05-07 and closed here by plan 05-14, the first plan after it to touch this file. |
| UL-042 | 2026-09-02 | 4 | A configuration script that OWNS a list it does not own is a silent gate-remover. `bin/setup-github.sh` recomputed the required-status-check array from its own platform matrix and PUT the whole `/protection` object, on the assumption that it was the only writer of that array. Two distinct defects follow from the one assumption. **Wholesale replacement**: any required context the script did not author is dropped on the next run, and the drop is invisible — the de-required job keeps running and keeps reporting, so the Actions tab looks identical and only the CONTEXT COUNT changes. **PUT rather than the narrowest endpoint**: a PUT rebuilds the entire protection object from whatever the caller supplies, so a caller that omits `enforce_admins`, `required_linear_history` or the approving-review count weakens the branch while appearing to add a check, and a rebuilt `checks` array of bare `{context}` objects silently rebinds every check's `app_id`. The generic shape of the fix is read-append-assert against the NARROWEST endpoint that can express the change: read the live array, compute the union with what this caller knows plus an explicit extra list, write only the difference through an endpoint that is additive by construction, re-read, and assert. The assertion is the part that is easy to leave out and the part that makes the rest checkable — and it has to be observed FAILING, because an assertion on a silent defect that has never gone red is indistinguishable from no assertion | No - in scope | Pending | — | Implemented as `gh_write` (one choke point for every write), `protection_status` (an `-i` status read that separates 404-create from 403-plan-limit, so the free-plan advisory no longer needs a failed write to detect itself), a two-call `contexts_get` whose read ordinal is an argument, a `jq` union, a POST of the difference only, and a sorted-equality assert printing all three lists. The full PUT is confined to the 404 branch, where there is no protection object to preserve. **Measured on this tree 2026-09-02.** Live before-state: nine contexts, `strict`, `enforce_admins` and `required_linear_history` all true, `app_id` 15368 on seven checks and `null` on the two iOS-Simulator cells — which is what a rebuilt array would have changed. Controls, all under a `gh` shim that logs its argv and exits 1, with an empty log after every one: a before-list short by one produced a POST body of exactly the one missing context and not the whole array; a post-write read stubbed short by one produced `protection assert failed: before=… after=… want=…` and exit 1; an already-correct array was a no-op that still asserted, nine in and nine out; and a before-list carrying a context the script has never heard of kept it. **Two implementation notes worth carrying upstream.** The read ordinal is an argument rather than a counter, because both call sites are command substitutions and a counter incremented in that subshell is discarded on return — which would make the post-write read answer as the pre-write one and leave the assertion unable to fail, the same class of defect in the fix as in the bug. And the test's static guard reads the script as TEXT and refuses to execute it while the unconditional PUT is present, because the only way to observe the old behaviour is to perform it. **Not fixed by this row:** `bin/refork-smoketest.sh` recreates this repository from the template and runs the template's copy, so the destructive write survives on that path until this lands upstream. **Observed live 2026-09-02, twice, on this repository's real protection** (the controls above are stubs; this is the endpoint): both runs exited 0 with `required contexts: 9 (expected 9)` and issued no `POST` and no `PUT`, the two runs' output was byte-identical, and the full `/protection` object diffed empty before versus after — nine contexts and the same `null`/`null`/7-x-`15368` `app_id` binding. That last clause is the point of capturing the whole object rather than the count: a rebuilt `checks` array keeps the count at nine while rebinding the two `null` cells, so `--jq length` would have reported success on precisely the regression |
| UL-043 | 2026-09-02 | 4 | The two project generators a template ships spell the same unit-test `TEST_HOST` differently, and nothing notices until something compares them byte for byte. XcodeGen takes the manifest's own per-platform spelling verbatim (`$(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(APP_PRODUCT_NAME)`, with `Contents/MacOS/` inserted by hand on macOS); Tuist derives ONE platform-free spelling for both from `productName:`, `$(BUILT_PRODUCTS_DIR)/$(APP_PRODUCT_NAME).app/$(BUNDLE_EXECUTABLE_FOLDER_PATH)/$(APP_PRODUCT_NAME)` — and `BUNDLE_EXECUTABLE_FOLDER_PATH` resolves to the EMPTY STRING on iOS, so Tuist's resolved iOS `TEST_HOST` carries a double slash where XcodeGen's has one. The two strings name the same file, POSIX collapses the run, and both generators' iOS unit-test cells are green — so this is a spelling difference and not a defect, but any cross-generator gate that compares the resolved value as text is red on it forever, and a gate that is red for a reason nobody can fix is a gate that gets deleted rather than fixed | No - in scope | Pending | — | Found 2026-09-02 (Phase 4, 04-12) on the FIRST run of the D-60 widening of `tools/identity-parity.rb` — the widening's own first finding, before either red control was staged, which is the strongest evidence available that half 2 reads something half 1 could not. Measured, not inferred: `grep TEST_HOST app/App.xcodeproj/project.pbxproj` after `tuist generate` shows the `$(BUNDLE_EXECUTABLE_FOLDER_PATH)` form on all four test targets and after `xcodegen generate` shows zero occurrences of that variable; the resolved iOS values are `…/ShipkitPipes.app//ShipkitPipes` (Tuist) versus `…/ShipkitPipes.app/ShipkitPipes` (XcodeGen), macOS identical on both. **The fork-side resolution is to normalise, loudly:** the tool compares POSIX-normalised paths and prints a `NOTE: equal only after POSIX slash normalisation` line naming both raw strings whenever normalisation is what made them equal — silent equalisation would be the same class of defect as the gate it was added to close. **Upstream shape:** Tuist's spelling is the more durable of the two (one line, no platform branch, no `Contents/MacOS/` to forget), so the template's `project.yml` could adopt `$(BUNDLE_EXECUTABLE_FOLDER_PATH)` for both platforms and the two manifests would agree as text as well as in effect — which is the real fix, and it belongs upstream beside UL-027's two lines rather than in a normalising comparison. Evidence: `.planning/…/evidence/04-12-T1-parity-testhost-controls.txt` |
| UL-044 | 2026-09-02 | 4 | When a template moves the app's display name out of the build manifests into one tracked config the build resolves, `fastlane/metadata/en-US/name.txt` is left behind as a SECOND, unwired home for the same value — and it is the App Store LISTING name, the first string App Review reads and the one `deliver` uploads. Nothing compares the two. The template's rename script writes `name.txt` once at fork time and never again, so after the config move a fork can edit the config, watch both platforms' `CFBundleDisplayName` change in the built product, and still ship a listing named after whatever the display name was on the day the fork was created. The failure is silent in both directions: no gate can see it, because a listing name is spaced prose and carries none of the identifier-shaped literals a contamination gate is keyed on, and the App Store Connect record does not disagree either, because the record's name is set once at creation and `deliver` only overwrites it at the next submit | No - in scope | Pending | — | Found 2026-09-02 (Phase 4, 04-15) by reading the artefacts App Review sees during the clause-by-clause close-out audit, three plans after the identity gates went green. This fork's `name.txt` still held its pre-Phase-3 display name — a string containing the words *Smoke App*, which is guideline 2.2's exact prohibition — while `app/Identity.xcconfig`'s `DISPLAY_NAME`, both built plists and the App Store Connect record all carried the real product name. Every gate this phase built was green over it: `tools/check-contamination.rb` is keyed on identifier-shaped literals and the offending string contains none, which is `C-21`'s point restated at a second site, and `test/docs_structure_test.rb` asserts nothing about `fastlane/metadata/`. Had `make submit` run, it would have RENAMED the live listing to the stale value. **Fork-side resolution:** the value was corrected at its source rather than allowlisted or deferred. **Upstream shape:** two candidates, and the second is the durable one — (a) a doctor tier-2 clause comparing `fastlane/metadata/en-US/name.txt` against the resolved `DISPLAY_NAME` and reporting disagreement, which is the same shape as the `.bootstrap.env` agreement check `D-59` already added and needs no new machinery; or (b) `deliver` reading the listing name from the identity config so there is one source rather than two, which is `D-57`'s argument applied to store metadata. (a) is a check and (b) is a fix; the template should carry both, because (b) does not help a fork that already has a stale `name.txt` on disk. Related: `UL-035` (one hand-rolled reader per consumer) and `UL-041` (a doctor that verifies presence reports `done` on a placeholder). Evidence: `.planning/…/evidence/04-15-T2-full-suite-both-interpreters.txt` § FINDING AND FIX |
| UL-045 | 2026-09-02 | 4 | A template that ships an identity contamination gate will key it on the identifier-shaped forms of its own name — the CamelCase product token and the reverse-DNS bundle id — because those are what the build files contain and what a rename script substitutes. Store metadata is not built from those forms. A listing name, a subtitle, a description and review notes are ordinary sentences, so the same words that the gate refuses inside a bundle id pass it as prose with spaces between them. The gate is then green on exactly the strings App Review reads first, and green for a structural reason rather than a fixable bug: a scan over contiguous stems cannot see a spaced variant, and widening it to catch the words in any arrangement collides with the many legitimate uses of those same words in a template's own documentation. App Store guideline 2.2 is written about prose, so an identity gate that can only see identifiers does not defend against the guideline it exists for | No - in scope | Pending | — | Filed 2026-09-02 at the close of Phase 4, from that phase's own `passed_with_concerns` verification rather than from a failure in the field. Reproduced twice on this tree and restored both times: appending the product name spelled with spaces, plus the words demo / beta / trial / smoke test as a sentence, to a tracked file leaves `ruby tools/check-contamination.rb` at exit 0, `0 unallowed`, while the contiguous stem in the same file exits 1. UL-044 is the instance this class explains — `fastlane/metadata/en-US/name.txt` carried the pre-Phase-3 display name through three phases of green gates and was found by reading the artefact, not by a check; it was fixed at that one site in `68c489c` and the class was deliberately left open rather than claimed closed. Tracked fork-side as IDENT-15 against Phase 6, which already owns the visual-pass criterion. Upstream shape not proposed here: the honest options (a curated prose deny-list per fork, or comparing store metadata against the tracked identity config rather than scanning it for words) differ enough in cost that the choice belongs with whoever implements it |
| UL-046 | 2026-09-03 | 5 | A template that keeps app identity in BOTH a gitignored per-fork dotenv and a tracked build-config file has two sources of truth with a human or a generator between them, and only the tracked one is what the build actually reads. Making the tracked file authoritative and deleting the dotenv keys is the durable shape — but the deletion is only safe if it lands TOGETHER WITH the readers. A config loader whose `[]` returns the empty string for an absent key turns every surviving reader into a BLANK rather than a failure, and a blank identity is indistinguishable from a correct one at every file-level gate — in the shipped artefact name as much as in a doctor line. Three parts make the flip hold. (1) ONE resolver, reading through the same parser every other consumer uses, that raises naming the key AND the file; no fallback-default tier, because a default is how a fork ships under someone else’s identity. (2) A TRIPWIRE on the config loader that refuses a retired key only when it is READ **and** ABSENT — a fork still carrying the key in its own dotenv is unaffected, while a fork scaffolded from the new example gets a named refusal pointing at the source of truth. Without it, the window between removing the key and moving the last reader is silent, and that window spans plans or releases. (3) The presence step and the content step read through the SAME parser: a hand-rolled regex over an xcconfig does not cut at `//`, so `KEY = // disabled` satisfies an anchored non-space presence check while Xcode resolves the value to nothing (UL-031). **And measure which predicate actually has the hole before citing it.** The step replaced here compared the file text against the dotenv value with an anchored equality regex; run on 2026-09-03 it did NOT accept `= // disabled` (it reported not-done), and its real defect was the inverse — a correctly-set value carrying a trailing `// note` also reported not-done, which in an idempotent bootstrap driver means re-running the mutation over an already-correct fork. One parser fixes both directions; a comment repeated from memory fixed neither. | No - in scope | Pending | — | Measured on this tree 2026-09-03. The dotenv example file was **byte-identical to upstream** before this change (`git diff --stat upstream/main -- .bootstrap.env.example` empty), so the edit CREATES divergence and is recorded here rather than left silent — the reconciled-forward-port shape Phase 3 established, not the UL-022 shape. Three files diverge as one unit and travel together: `.bootstrap.env.example` (the three identity keys removed; the lead-in corrected, since it claimed the values become the Xcode scheme and the project file name and both have been constants since #281; `ASC_APP_NAME` re-described for its new generator role), `bin/init-bootstrap-env.sh` and `bin/preflight.sh` (both told a forker to fill two fields the example no longer ships). Fork-side implementation: `Bootstrap.identity!` / `.identity_xcconfig_path` / `.identity_for_display`, `Bootstrap::Config::RETIRED_IDENTITY_KEYS` plus the `Config#[]` tripwire, and `Bootstrap::IdentityPresent` replacing the rename step in `Runner::PIPELINE` — all in `bin/lib/bootstrap.rb`. The tripwire was driven BOTH ways rather than asserted: retired key absent → named refusal at exit 1; the same key present → returned unchanged at exit 0. Fifteen in-file reads moved in the same commit; nine more across four release-path scripts are a later plan’s, and the tripwire is what covers them meanwhile. Verdict stays `Pending` — bare, per this file’s own vocabulary, which reserves the bracket for the other five values — until the D-77(b) parameterization pull request carries it. **Extended 2026-09-03 with the rest of the divergence this learning creates.** Plans 05-08 and 05-09 each recorded that their template-owned changes had no ledger row and that the row had no owner; it belongs here, because a row belongs to a LEARNING and this is that learning — the "nine more across four release-path scripts" this cell already anticipated. They landed: `bin/compute-release-tag.rb`, `bin/ship.rb`, `bin/submit.rb` and `bin/verify-testflight.rb` moved all nine reads onto `Bootstrap.identity!`, closing the tripwire window this cell predicted would span plans, and `.github/workflows/canary-local-mode.yml` had its account of which fields it needs corrected in the same change (05-08); `bin/lib/bootstrap.rb` and `docs/BOOTSTRAP.md` gained the `StoreMetadataGenerated` step that gives `fastlane/metadata/en-US/name.txt` and `fastlane/metadata/copyright.txt` a writer after the retirement removed their only one (05-09, D-74/A-07). Total divergence from `upstream/main` at `658bd32`, measured 2026-09-03 with `git diff --numstat` and stated as CUMULATIVE across phases rather than attributed to one plan: `bin/lib/bootstrap.rb` 753/101, `docs/BOOTSTRAP.md` 103/32, `.github/workflows/canary-local-mode.yml` 73/20, `bin/compute-release-tag.rb` 36/17, `bin/ship.rb` 19/9, `bin/submit.rb` 17/143, `bin/verify-testflight.rb` 16/5. `.github/workflows/review-notes.yml` and `test/doctor_identity_test.rb` are NOT divergence — `git cat-file -e upstream/main:<path>` fails for both, so they are fork-only files whose own upstreaming is UL-001's. **One measurement from that work corrects a claim made elsewhere, not in this cell:** `AGENTS.md` said the dotenv's `BUNDLE_ID` had had no release-path consumer since Phase 4, and `bin/compute-release-tag.rb:42` was one — it fed the tag the release workflow pushes — so the window this cell warned about was live rather than theoretical. Closed by `71774cf`, and the sentence corrected in place by plan 05-14 rather than softened. The retirement's own files do NOT travel with this row; they are the second of D-77's two pull requests. **Still `Pending` after [#289](https://github.com/indiagrams/apple-shipkit/pull/289) opened, and that is a MEASUREMENT rather than an oversight.** An earlier summary said this row closes through the parameterization pull request; it does not. That request keeps the dotenv's three identity keys — counting the three identity assignments in `.bootstrap.env.example` on the branch returns 3 — because the docs pull request's own amendment requires it, upstream's Fastfile and release workflows still reading them. This row's learning IS the removal of those keys, so the flip has not travelled. It needs its own change. |
| UL-047 | 2026-09-03 | 5 | Extracting a script out of another ref to compare its behaviour against the in-tree copy — `git show <ref>:<path> > /tmp/copy.rb` — silently changes what the copy can load. A script whose only dependency is `require_relative "lib/<x>"` dies at load time in the new location with a `LoadError`, before it parses argv, so it exits non-zero for EVERY invocation including `--help`. If the comparison expected the other copy to fail, that expectation is confirmed by a crash that has nothing to do with the property under test | No - in scope | Pending | — | Found 2026-09-03 (Phase 5, 05-10) while re-measuring A-06's two do-not-adopt regressions. The plan prescribed the extraction verbatim (`git show upstream/main:bin/preflight-identity.rb > <scratch>/preflight-upstream.rb`, then run `--config app` under both pinned interpreters, expecting exit 1). It produced exit 1 under ruby 3.3.12 and 4.0.6 — the plan's predicted headline number — for entirely the wrong reason: `LoadError: cannot load such file -- <scratch>/lib/xcconfig`, raised by `require_relative` at the file's line 78. **The discriminator that caught it:** re-running the same scratchpad copy with `--config app`, no argv at all, `--help`, and a bogus flag gave exit 1 and the identical first line every time. A measurement that cannot distinguish `--help` from the input it is about measures nothing. This is the project's "crash-as-detection" anti-pattern (a real verdict standing behind an `Errno`), reached this time through the harness rather than the subject, and it is a live hazard for anyone diffing forks of this template because both copies of `bin/preflight-identity.rb` carry exactly one `require`-family line by design. **The honest method, and what it then showed:** reconstruct the script's own relative layout before running it — `bin/preflight-identity.rb` plus `bin/lib/xcconfig.rb` plus an `app/` directory, all from the same ref — and run from that root. Upstream then failed at real code, `Errno::EISDIR` from `bin/lib/xcconfig.rb:164` escaping through `preflight-identity.rb:183-184`, still exit 1 but now for the reason the comparison was about. The verdict was unchanged; the evidence for it was worthless until re-taken. Generalisable rule for this ledger's own method: an extracted copy must be run in a layout that resolves its `require_relative` targets, and every cross-copy comparison needs one invocation that is EXPECTED to differ, run alongside the one under test. |
| UL-048 | 2026-09-03 | 5 | A template's own bootstrap reads its tracked dotenv with an INHERITED encoding, so on any machine where `LANG` and `LC_ALL` are unset `Encoding.default_external` is US-ASCII and the first `String#strip` raises `Encoding::CompatibilityError: invalid byte sequence in US-ASCII` — before any pipeline step runs, and before the comment-skip that would otherwise have thrown the offending line away, so commenting the line out does not help either. This is not a fork-specific accident: the example file the template instructs every forker to copy carries the non-ASCII itself, so the shipped configuration is the trigger, and both the doctor and the fork-bootstrap entry points die with a backtrace on a fresh machine. The tests said nothing because every CI runner sets a locale. Generic fix: pass the encoding explicitly on every read of tracked text rather than inheriting it from the environment, and SWEEP for the reads that do not, instead of fixing one site per report | No - in scope | Submitted [#290] | [#290](https://github.com/indiagrams/apple-shipkit/pull/290) | **Second instance of one class in one repository, which is the signal rather than either bug.** The first was `tools/gen-review-notes.rb` (UL-012), fixed in `3b1efb9`; this one is `Bootstrap::Config.parse` at `bin/lib/bootstrap.rb:216`, which read through `Pathname#each_line` with no encoding pin. Found and fixed by plan 05-09 in `3ba092b`, and **driven red with BOTH discriminators** rather than one: the crash needs the non-ASCII bytes AND the missing locale, and either alone is green, so a control that varied only one would have proved nothing (`evidence/05-09-metadata-controls.txt`). **The reachability measurement is what makes this worth upstreaming on its own merits**, separately from this phase's parameterization work: `.bootstrap.env.example` carries 52 lines with non-ASCII bytes and `bin/init-bootstrap-env.sh:36` copies it verbatim, so every forker starts from a file that trips it — a template whose bootstrap cannot run for the population it exists to serve is broken regardless of what its suites report. Sites already correct before anyone looked: `bin/lib/xcconfig.rb:155`, `test/identity_test.rb`'s `read_utf8`, and `test/docs_structure_test.rb`'s `read_doc`, whose own comment records the same observation independently. **The rest of the tree has NOT been swept.** The check is `git grep -nE 'File\.read\|each_line\|readlines\|IO\.read'` followed by reading each hit for an explicit encoding argument; that sweep has no owner and needs an allocation at phase verification or in Phase 6. Verdict `Pending`: the fix is fork-side today and travels with the D-77(b) pull request, or as its own smaller change, whichever opens first — it depends on none of the parameterization work **Appended 2026-09-03 (plan 05-17), because two code comments in this repository stated the WRONG HALF of this class and a third was about to.** Both said an UNPINNED WRITE of a non-ASCII string raises `Encoding::UndefinedConversionError` under a cleared locale. Measured under 3.3.12 and 4.0.6 with `LANG`, `LC_ALL` and `LC_CTYPE` all unset: it does not. Every write form tested — `File.write`, and an explicit-mode `File.open` used with `#write` or with `#puts` — emits the String's own bytes and raises nothing, because Ruby does not transcode a String to `Encoding.default_external` on the way out. This row's own generic fix already said READ, and it was right; the comments citing it were not. Corrected in place at both sites rather than softened. The falsifiable form is the READ: an unpinned `File.read` of non-ASCII bytes under a cleared locale, followed by a `strip`, a concatenation or a regex, raises `Encoding::CompatibilityError`. Driven both ways with both discriminators — the bytes AND the missing locale, either alone green — as `RESULT control=team-id-read-locale exit_pinned=0 exit_unpinned=7 exit_unpinned_with_locale=0 restored=ok`. The upstream fix does not change; the reason written beside it does. **Still `Pending` after [#289](https://github.com/indiagrams/apple-shipkit/pull/289) opened — measured, not assumed.** The branch's `Config.parse` still reads through an unpinned `each_line`; the count of the pinned form in the patch is ZERO. That matches the deliberate decision to give this its own small pull request rather than bury a one-line fix that breaks every forker whose locale is unset inside an eight-thousand-line change. Recorded here because an earlier summary said all three rows close through that request, and only one of the three does. **Appended 2026-09-04 (plan 05-18, gap closure for GAP-05-01). This is the FOURTH instance, the sweep this row asked for finally has an owner, and it has a result.** UAT found `make doctor` still dying on a fresh clone with the locale cleared -- at the metadata scan in `bin/lib/bootstrap.rb`, which 05-09's fix EXPOSED rather than caused, because execution only then got far enough to reach it. A second unswept site sat in `bin/adopt.rb`, the exact `each_line`-then-`strip` shape 05-09 had just fixed, in a different file. Three one-site fixes did not hold, so this row's own instruction -- SWEEP, do not fix one site per report -- was carried out. Derivation re-run at execution time rather than carried from the plan: `git grep -nE 'File\.read\(\|File\.readlines\(\|IO\.read\(\|File\.foreach\(\|\.each_line' -- '*.rb' 'fastlane/Fastfile'` filtered for an explicit encoding argument yields 31 sites, every one of which now carries a verdict: **17 pinned, 5 binread, 9 exempt with a written and measured reason**. Confirmed crashing, each reproduced rather than inferred: the doctor metadata scan, `bin/adopt.rb`'s dotenv parse, `bin/lib/version_resolver.rb` and `bin/submit.rb` reading the two build manifests, and -- driven through REAL fastlane with the locale cleared -- `extract_changelog_section`, `project_marketing_version` and `build_asc_metadata_args` in the Fastfile, which the plan had only listed as crash-ELIGIBLE. The five App Store Connect private-key reads get the BINARY form, not a UTF-8 pin, after measuring against a throwaway P-256 key that `Spaceship::ConnectAPI::Token.create` accepts an `ASCII-8BIT` string and that `JSON.dump` round-trips those bytes identically. Measured and recorded rather than assumed: fastlane does NOT protect these reads -- it sets the default external encoding to UTF-8 only inside its own `sh` helper and restores it afterwards, and in the same cleared-locale run it printed "WARNING: fastlane requires your locale to be set to UTF-8" and then read the file anyway. The guard against a FIFTH instance is `test/encoding_test.rb`, now in the REQUIRED `review notes` context: it demands a verdict for every candidate, keys exemptions by content anchor so a stale one FAILS instead of silently widening, and spells no call form in its own source so it needs no self-exclusion. A non-required locale-cleared `make doctor` job in `bootstrap-doctor-matrix.yml` covers the execution half; it cannot be required because it needs a `.bootstrap.env`, which is gitignored. Template-owned files carried by this work, listed here rather than in a new row because this row already covers the class and a duplicate would record work that does not exist: `bin/adopt.rb`, `bin/lib/bootstrap.rb`, `bin/lib/version_resolver.rb`, `bin/submit.rb`, `bin/compute-release-tag.rb`, `fastlane/Fastfile`, `.github/workflows/review-notes.yml` and `.github/workflows/bootstrap-doctor-matrix.yml`. **Still `Pending`: the fix is fork-side only.** Whether it travels upstream now, folds into [#289](https://github.com/indiagrams/apple-shipkit/pull/289), or is handed to Phase 6 is a decision presented at plan 05-18's blocking checkpoint and NOT taken by this plan -- opening a pull request against a repository the user does not own needs explicit approval, as #288 and #289 both did. **Appended 2026-09-04: the upstream branch is PREPARED and UNPUSHED, and the upstream sites are NOT the fork's.** Branch `fix/pin-encoding-on-reads-of-tracked-text` in `/Users/prakash/code/apple-shipkit`, three commits, head `21c382b`, based on `origin/main` measured at fetch time as `42a1abbb2a2e7f2273592ec6f2d6f54b9bc8b99d` (`42a1abb`) -- re-measured rather than carried, because this ref moved four times during the phase. `git merge-base --is-ancestor origin/main HEAD` exits 0, so it is a clean fast-forward. **The classification was re-derived against upstream's own bytes, not ported**: upstream has **26** candidate sites to the fork's 31, split 17 pin / 5 binread / 4 exempt. The five exemptions the fork carries for `tools/check-contamination.rb`, `tools/gitleaks.rb` and `tools/identity-parity.rb` do NOT exist upstream -- those files are fork-owned -- and porting them would have failed the gate's own stale-exemption rule, which is the rule working. Upstream carries three sites the fork does not: `Config.parse` itself (still unpinned at `bin/lib/bootstrap.rb:116`, confirming 05-17's measurement), `fastlane/Fastfile:78`'s `_fork_config` dotenv read, and `test/tag_prefix_test.rb:105`. Upstream's own numbers, measured: `.bootstrap.env.example` **53** non-ASCII lines (this row said 52 -- it drifted), `CHANGELOG.md` 300, `app/project.yml` 21, `app/Project.swift` 11, `bin/lib/bootstrap.rb` 120. **Reproduced on upstream's own bytes with both discriminators** on 3.3.12 and 4.0.6: the shipped example file with the locale cleared raises `Encoding::CompatibilityError`; with `LANG` set it parses 39 keys; a pure-ASCII copy with the locale cleared parses. And upstream's crash fires EARLIER than the fork's -- `make doctor` on the branch's parent, with a fixture config and the locale cleared, printed **zero** status lines and died at `bin/lib/bootstrap.rb:117`; on the branch it prints 21 and a verdict. **The doctor execution regression was deliberately NOT ported, and that is a measurement, not a preference**: because upstream dies at parse time before any step, an execution job there would assert exactly what the gate's own dynamic discriminator already asserts -- the real parser, on the real shipped example file, with the locale cleared -- while costing macos-15, installed gems, a twelve-key fixture dotenv and Apple API traffic with invalid credentials from a public template's CI. The fork needs its doctor job only because 05-09 had already pinned `Config.parse` fork-side, leaving its remaining exposure deeper in the pipeline. Upstream instead gets `encoding-regression` in `bootstrap-doctor-matrix.yml`, modelled on the existing `parser-regression`: ubuntu-latest, stdlib-only, `bundler-cache: false`. Note also that upstream has no `review-notes.yml`, so the fork's required-context wiring has no upstream home; whether the new job becomes a required check is upstream's call, not this branch's. The gate caught a defect in this very patch before any human saw it: the Fastfile commit had introduced an unpinned stream read, and E1 named it. Fork-value grep over the 696-line patch and over the full content of every changed file: `shipkitpipes` 0, `Shipkit Pipes` 0, `G5H628C6WR` 0, `com.indiagram.shipkitpipes` 0, each recorded with `grep_raw=1` (no-match, not error), and a poisoned copy of the patch returned 1 for all four, so the zeros mean something. **Verdict stays `Pending`: NOT pushed** (`git ls-remote --heads origin fix/pin-encoding-on-reads-of-tracked-text` is empty), no pull request opened, and the PR column stays `—` until a number exists. The user approves opening it separately, as they did for #288 and #289. Submitted upstream 2026-09-04 as apple-shipkit#290 (9 files, +461/-22, base 42a1abb). Measured before submitting rather than assumed: upstream's own Config.parse at bin/lib/bootstrap.rb:116 is still unpinned, so upstream dies at PARSE time with zero doctor status lines, where this fork only ever reached the later metadata-scan crash because 05-09 had already pinned that site locally. The upstream classification was re-derived against upstream's bytes (26 sites: 17 pin, 5 binread, 4 exempt) rather than ported from the fork's 31 — porting would have left five exemptions matching zero sites and failed the gate's own stale-exemption rule. |
| UL-049 | 2026-09-03 | 5 | A template that keeps its scheme and target names CONSTANT while letting the product name VARY grows tooling that composes a built-product PATH out of a scheme or target NAME. The composition is correct only for a fork that never changed its product name, and it fails silently in three different costumes: a `find` that matches nothing, a unit-test host pointing at a bundle that was never built, and a release artefact looked for under a name nothing writes. None of the three is visible to the manifests, to a file-level test, or to a parity tool, because the name each of them reads is genuinely there — it is the DERIVATION that is wrong. Three instances in one repository is a class, not a coincidence. Generic fix: ask the build for the path — `FULL_PRODUCT_NAME` and `BUILT_PRODUCTS_DIR` out of `xcodebuild -showBuildSettings`, pinned to a destination — instead of composing one from a name, and never assert on a manifest for something that must be true of the shipped artefact | No - in scope | Pending | — | **The three instances, in the order they were found.** (1) UL-043 — the two generator manifests spelled the same unit-test `TEST_HOST` differently, and nothing noticed because every assertion in eleven plans was file-level; the generator bakes the host TARGET name in at generation time while `PRODUCT_NAME` resolves at build time, so the two can never agree for a fork that renamed its product. (2) Plan 05-04 carried the same defect into the migrated fixture, and it was caught only by reading resolved build settings rather than the manifest that produced them. (3) `bin/take-readme-screenshots.sh:174,207` searches DerivedData for `"$SCHEME_IOS.app"` — the SCHEME name — while the built bundle is `$(APP_PRODUCT_NAME).app`, so the `find` matches nothing. **The third is LIVE in this repository at HEAD**, not only in a migrated fork: at HEAD the iOS scheme is the constant and the product name is the fork's own, which is precisely the case the derivation cannot handle. Found 2026-09-03 by plan 05-16 while measuring something else; that plan's scope fence excluded this repository's own files, so it was logged rather than fixed. `fastlane/Fastfile`'s artefact-name patterns are the same shape and are named here rather than counted as a fourth, because Phase 4 already reconciled them onto the one parser. **NOT fixed in this phase, deliberately.** `bin/take-readme-screenshots.sh` is template-owned (10 insertions / 14 deletions divergent from `upstream/main` at `658bd32`, measured 2026-09-03), it is in no Phase 5 plan's `files_modified`, and the change cannot be validated without a real screenshot run on both platforms — an unvalidated edit to the script that produces the README's own images is worse than a search that returns nothing loudly. **Severity LOW** for that reason: the failure is visible (no screenshots) rather than silent-and-wrong. Owner unassigned; needs an explicit allocation at phase verification or in Phase 6, which already owns the gate-blindness class (IDENT-15 / UL-045) |
| UL-050 | 2026-09-03 | 5 | A template whose tracked build config `#include?`s a GITIGNORED per-clone file, and whose dotenv separately carries that same value under a different key, has a WRITER between them that nobody owns. Retire the personalization step that used to write it — a reasonable thing to do, since a Team ID is per-clone signing configuration rather than personalization — and the value silently acquires ZERO writers. Nothing goes red: the tracked config still parses, every CI runner is green because the gitignored file is structurally absent there anyway, and the only person who meets the gap is a fresh forker at their first signed build. Two properties make it worth a row rather than a bug report. First, the failure is ASYMMETRIC by platform: an UNDEFINED value fails iOS signing by name, while an EMPTY one lets a macOS build SUCCEED with an ad-hoc signature and say nothing — so a writer that defaults to blank is strictly worse than no writer at all. Second, the absence is invisible to grep unless the predicate is run with a POSITIVE CONTROL: the same expression that returns zero here returns double digits in the files that do name the path. Generic fix: give the value a pipeline step that writes it from the dotenv, refuses BY NAME on an absent or malformed value rather than writing a blank, refuses to OVERWRITE a differing value already there (which makes it a drift check between the two consumers as well as a writer), and reads back through the project's one config parser so `done` means the value RESOLVES rather than that a file exists | No - in scope | Submitted [#289] | [#289](https://github.com/indiagrams/apple-shipkit/pull/289) | Measured on this tree 2026-09-03. The gap was surfaced by plan 05-11's enumeration of every file-write call site in the bootstrap library — zero named the gitignored path, `grep_raw=1`, against 14 / 13 / 4 hits for the same predicate in the migration command, the identity preflight and the personalization script — and re-measured with the same controls by 05-17 before being closed. It fails loudly today rather than silently: the preflight's `--require-team` mode already exits 4 naming the file and printing the exact line to create (that is why the severity is LOW and why the row is about the MISSING WRITER, not about a leak). Fork-side implementation: `Bootstrap::LocalSigningTeam`, inserted into the pipeline immediately after the store-metadata generator so identity is verified before anything is generated from it, with `bin/lib/xcconfig.rb` as the read-back parser. The RESOLVED half was measured rather than asserted, with `xcodebuild -showBuildSettings -target` (a TARGET, not a scheme: a target reads its own SDKROOT and does not go through destination resolution): with the file absent, BOTH targets report no `DEVELOPMENT_TEAM` line at all and macOS reports `_DEVELOPMENT_TEAM_IS_EMPTY = YES`; after the step runs, both report the value and macOS flips that flag to `NO`. The written file was confirmed untracked and invisible to `git status` in the same pass. **Verdict stays `Pending`:** this closes the fork's copy, and the same gap exists upstream — the retirement is what removes the last writer there too — so it travels with the D-77(b) parameterization pull request, which is prepared and held rather than open **Submitted 2026-09-03 in [#289](https://github.com/indiagrams/apple-shipkit/pull/289)**, as its own `feat(identity)` commit rather than folded into the retirement — burying a change to what `make bootstrap-fork` does inside a `refactor` commit is how a reviewer misses the one thing that affects every forker. Measured present: `class LocalSigningTeam < Step` is in the patch. The four documentation sites that said no script writes the file were corrected in the same commit, because that commit is what makes them false. |
| UL-051 | 2026-09-04 | 5 | A driver that shells straight through to a live App Store Connect lane, with NO argument parsing of any kind, turns every invocation into a production API call -- including the `--help` a reader reaches for first. `bin/adopt.rb` exists to stop a fork clobbering a live App Store listing; it parses no `ARGV`, has no `--help`, and its last statement is `exec("bundle", "exec", "fastlane", "adopt_existing_app")`, so anything typed after the script name is ignored and the lane runs. Generic fix: a driver whose failure mode is a network call against production state must parse its arguments before it does anything, answer `--help` and an unrecognised flag by refusing, and support a dry run **Generalised 2026-09-04 (plan 05-20, gap GAP-05-03) from one driver to the class, after three more were audited read-only.** The class: a driver whose last act launches a subprocess that writes to production must parse its argument vector BEFORE it does anything, answer `--help` by printing usage rather than by acting, and refuse an unrecognised argument BY NAME -- because the argument most likely to be typed by somebody who does not yet know what the command does is `--help`. Two further shapes make this a class rather than three separate bugs. (1) **`ARGV.include?` is sniffing, not parsing.** It matches exactly or not at all, so `bin/ship.rb` honoured `--dry-run` and `--force` and silently ignored `--dryrun`, `--dry_run`, `--DRY-RUN`, `--help` and every other spelling, each of which then shipped for real. The script had a dry-run mode and NO WAY TO DISCOVER IT, which is worse than having none: the reader who types `--help` to find the flag triggers the exact thing the flag exists to prevent. (2) **`bin/submit.rb`'s safety was a config default the script itself tells you to remove.** `auto_submit` reads `SUBMIT_FOR_REVIEW` and never argv; that key is unset in most forks, so the lane resolved to the reversible staging one -- and the closing banner says, in as many words, to set it true on future runs. After which `ruby bin/submit.rb --help` submits the app for App Store review. That is the same incidental-guard shape as the encoding crash recorded earlier in this row: safety nobody chose is safety that leaves without notice, and it grows sharper as the codebase gets healthier. | No - in scope | Pending | — | **Measured 2026-09-03, by accident, during 05-18 planning, and recorded so nobody repeats it.** A `ruby bin/adopt.rb --help` with the locale set ran to the `exec` and drove the real `adopt_existing_app` lane against the LIVE App Store Connect record, overwriting **13 tracked files** under `fastlane/metadata/` and creating **10 untracked** ones. It also raised a `fastlane_tmp_keychain` unlock prompt on the operator's desktop. It is a download, not an upload, so **no Apple-side state changed**; the tree was restored with `git checkout -- fastlane/metadata/` plus a SCOPED `git clean -fd -- fastlane/metadata/`, and the restore was verified by an empty `git status --porcelain` and by `.bootstrap.env`'s sha256 being unchanged. The diff is preserved at `scratchpad/accidental-adopt-diff.patch`. **The sharp edge, and the reason this is filed the same day it was made safe:** until 05-18, an unset locale made `bin/adopt.rb` crash at its dotenv parse BEFORE the `exec`, so the obvious probe `env -u LANG -u LC_ALL ruby bin/adopt.rb --help` was safe -- and safe ONLY because of that defect. UL-048's fix removed the crash, so that exact command became a live App Store Connect call the moment the pin landed. A probe whose safety depends on the defect it probes for arms itself on success, and this one did. Confirmed still true at the fork's HEAD after 05-18: a case-sensitive count over `bin/adopt.rb` of `ARGV`, `OptionParser`, `optparse` and the help flag returns **0** for all four and the file's last line is still the `exec`. Every control in 05-18 therefore ran in a COPY of the tree, with a fixture dotenv deliberately missing a required key so the script refuses by name at its `Missing required env vars` check, and with stub `bundle` and `fastlane` executables ahead of the real ones on `PATH` as belt and braces; `stub_invoked=no` on all four runs proves the `exec` was never reached. Both the fork and upstream carry the file unchanged in this respect, so the defect is upstream's too. Fork-side gap plan 05-19 owns the fix; this row owns the upstream half **Appended 2026-09-04 (plan 05-19, gap closure for GAP-05-02). The fork's front door is fixed and guarded; the upstream half is re-measured and still open.** `bin/adopt.rb` now parses its argument vector BEFORE the dotenv read and before anything that can open a connection. `-h` and `--help` print usage naming what the command DOES -- that it contacts App Store Connect and OVERWRITES `fastlane/metadata` and `fastlane/screenshots` -- and exit 0. `--dry-run` resolves the target from the dotenv, reports the app name, bundle id, team and what would be overwritten, and exits 0 without connecting, passing nothing through to the lane. Anything else is refused BY NAME at exit 2, which is deliberately distinct from the preflight's exit 1; the refusal covers arguments that are not flag-shaped, because a parser inspecting only tokens that begin with a dash would ignore a bare positional and fall through to the lane. The exit codes are documented in a header table, in `bin/preflight-identity.rb`'s shape. **Additive by construction, and measured as such rather than asserted:** 110 insertions and ZERO deletions; the ten-line clean-tree gate and the `status =` line above it are byte-identical to their pre-change bytes, diffed explicitly, and no removal line appears anywhere in the diff. `--dry-run` stops after identity validation and BEFORE that gate, so it reports uncommitted work instead of refusing over it -- a dry run overwrites nothing, so the gate has nothing to protect there. **The guard is an OBSERVATION, not a reading of the source.** `test/adopt_argv_test.rb`, now a step in the REQUIRED `review notes` context, copies the script into a temp directory beside a synthetic fixture dotenv and puts a directory first on PATH holding a `bundle` that records its argv and exits 0; reaching that shim is the failure condition. It never runs the script in place, for the same reason `test/setup_github_test.rb` never runs its own subject against a live repository. **The shim count is asserted NUMERICALLY, and the exit code is asserted in order to show that it discriminates nothing:** `--help` exited 0 before the fix by running the lane and exits 0 after it by printing usage, so a gate built on the status alone would have been green on the defective script -- the exit-code-blind half of the crash-as-detection class, real on this subject rather than theoretical. Three controls, each driven red rather than reasoned about. (1) The positive control is load-bearing: with the shim directory dropped from PATH the harness became incapable of observing an exec at all, and FIVE `the shim log is empty` assertions passed VACUOUSLY while only the bare-invocation control noticed. (2) Neutering the unknown-argument refusal put both offending arguments back to one shim invocation each -- while the assertion that the refusal NAMES the argument stayed GREEN, because the warning still printed, which is exactly how a message-only gate would pass a script that refuses out loud and then runs the lane anyway. (3) Neutering `--dry-run` left the count at zero and was caught by the content assertions instead, the mirror image of (2), which is why both kinds are asserted. **CI eligibility was verified in a CLONE, and the clone found a real defect.** With `GEM_HOME`, `GEM_PATH`, `BUNDLE_GEMFILE`, `RUBYOPT`, `LANG` and `LC_ALL` cleared and PATH cut to `/usr/bin:/bin`, the first run raised `ArgumentError: invalid byte sequence in US-ASCII` after 30 of 42 checks -- UL-048's class arriving through PROCESS OUTPUT rather than a file read, which is the residual 05-18 logged to `deferred-items.md`, met for real. `Open3.capture3` tags what it captures with `Encoding.default_external`, and the new usage text carries an em dash, so the first concatenation raised; there is no encoding argument to give a captured stream, so both are re-tagged once at the boundary. The suite's own crash guard reported the raise as a named failure instead of letting an unfinished run read as a pass -- which matters more here than usual, because the subject IS an unwanted execution. Green afterwards in the clone under 3.3.12 and 4.0.6, locale cleared and locale set. **Upstream re-measured 2026-09-04 rather than carried, at `origin/main` = `42a1abb`:** upstream's `bin/adopt.rb` is 96 lines, its case-sensitive count of the argument vector, the stdlib option parser and that parser's require name is 0 for all three, it spells no help flag anywhere, and its last statement is the same `exec`. Fork and upstream diverge on this file by 19 lines, every one of them 05-18's encoding pin, that plan's comment correction, or the two template-placeholder lines. The defect is therefore upstream's unchanged, and the fix above is generic rather than a fork concretion. **Verdict stays `Pending`: fork-side only, and NOT pushed.** No branch was prepared for this one and no pull request opened -- opening one against a repository the user does not own needs explicit approval, as #288 and #289 both did. Whether it travels as its own small change or rides with the prepared `fix/pin-encoding-on-reads-of-tracked-text` branch is a decision for phase verification, and not one this plan takes. **Appended 2026-09-04 (plan 05-20, gap closure for GAP-05-03). Two more fork-side front doors, one generalised gate over five drivers, and two drivers named measured-safe WITH their reasons.** **The audit, measured read-only on 2026-09-04, no driver executed:** `bin/adopt.rb` fixed by 05-19; `bin/submit.rb` parsed ZERO arguments and its last act is a shell-out to the fastlane stage or submit lane; `bin/ship.rb` sniffed exactly two arguments and parsed none; `bin/mint-local-certs.rb` is refused by a `RELEASE_MODE=ci` guard that sits before the keychain work, and this fork is ci mode; `bin/verify-testflight.rb` makes only ASC queries. **None of the five confirms interactively** -- they print a banner and proceed. **The fix.** Both drivers now parse before the config read and before anything that can open a connection: `-h` and `--help` print usage and exit 0, `--dry-run` on `bin/submit.rb` reports the resolved app, bundle id, platforms and lane and contacts nothing, and an unrecognised argument -- flag-shaped or not -- is refused by name at **exit 3**. Exit 3 rather than `bin/adopt.rb`'s exit 2 because 2 already meant `the lane or the run concluded badly` in both files before this front door existed; redefining a live exit code to match a sibling would be a behaviour change wearing a consistency costume, and both header tables now say so. `bin/submit.rb`'s usage names which lane a bare run resolves to and why, which was previously discoverable only from the closing banner AFTER the write. `bin/ship.rb` keeps `--dry-run` and `--force` spelled exactly as they were: those spellings are the contract, and the defect was never that they were wrong but that everything else was ignored. **Additive where it could be:** every pre-existing guard is byte-identical to `eac2f2e`, diffed rather than argued -- `bin/submit.rb`'s PLATFORMS, metadata, screenshots-dir and per-platform screenshot gates and `bin/ship.rb`'s in-progress and already-tagged block all produce no diff output. The ten deleted lines across both files are five superseded header comment lines each, plus `bin/ship.rb`'s two sniff lines, which ARE the defect. **The guard is one suite over a table, not five copies.** `test/driver_argv_test.rb` (152 checks, stdlib only) replaces `test/adopt_argv_test.rb` in the REQUIRED `review notes` context; the old file is ABSORBED rather than kept, because two suites sharing a harness drift, and every one of its five groups is covered with the same needles. The step count in that job is unchanged, so branch protection's nine contexts are untouched. Every observation runs a COPY inside `Dir.mktmpdir` beside synthetic fixtures, with capturing `bundle`, `fastlane` and `gh` shims first on PATH; reaching a shim from a non-consenting argument is the failure condition. **RED, both pinned interpreters: exit 1, 39 FAIL lines, all naming `bin/submit.rb` or `bin/ship.rb`.** Every non-consenting argument reached the lane: submit at 1 invocation each, ship at 6 each including all three near-miss spellings. `bin/adopt.rb` passed every case, so 05-19's front door holds. **GREEN, both interpreters: exit 0, 152 checks, 0 failures**, with A1 -- a bare configured invocation -- still reaching a shim for all three executed drivers, so nothing went green by becoming unobservable. **Six red controls, each mutation verified landed by a moved sha256 AND a re-parse before the red was trusted.** Neutering `bin/submit.rb`'s refusal exit gave 6 FAIL with both offenders back to one invocation -- **while the assertion that the refusal NAMES the argument stayed GREEN**, because the warning still printed; a message-only gate would pass a script that refuses out loud and then runs the lane anyway, which is 05-19's finding reproduced on a new subject. Restoring `bin/ship.rb`'s sniffing gave 25 FAIL with every near miss back to 6 invocations, a real ship, while A1 stayed green. Pasting a lane literal into the suite turned A0 red, which is the gate-collision hazard that bit six consecutive plans, caught by MEASURING zero candidates in the suite's own source rather than by a path self-exclusion a later paste defeats. Removing `bin/mint-local-certs.rb`'s guard gave 5 FAIL and the shim recorded a real `fastlane mint_local_certs` handoff -- that guard is the only thing between a bare run and a cert lane, which is exactly why it is ASSERTED and not commented. Adding a write and a lane handoff to `bin/verify-testflight.rb` gave 3 FAIL. Dropping the shim directory from PATH gave 11 FAIL and **18 emptiness assertions passing VACUOUSLY**, caught only by A1, by the dry-run log-content assertions and by the `--force` acceptance check; 05-19 measured five such vacuous passes on one driver, and a table of five has eighteen. **Two corrections to the plan's own audit table, measured rather than inherited.** First, `bin/ship.rb --dry-run` does NOT mean `contact nothing`: it dispatches release.yml with `dry_run=true`, a real run of the pipeline with the upload skipped. Asserting an empty shim log there would have required silently redefining the flag, so the suite asserts the flag is HONOURED and that the honouring is visible in the launched argv, and reserves the emptiness property for the near misses, which is where the defect actually lived. Second, `bin/verify-testflight.rb` is NOT launch-free: it shells out to `git fetch --tags --quiet origin` at line 83. Still a read, so the safe disposition stands -- but a gate whose whole point is that a safe driver is named safe WITH its reason cannot carry a false sentence, so it asserts the property that is true (no launch site hands off to a lane, and every launch site names git) rather than the tidier one that is not. **CI eligibility verified in a CLONE** -- no `.bootstrap.env`, no generated project, `GEM_HOME`, `GEM_PATH`, `BUNDLE_GEMFILE`, `RUBYOPT`, `LANG` and `LC_ALL` cleared and PATH cut to `/usr/bin:/bin` -- green under 3.3.12 and 4.0.6, because 05-08 established that unsetting environment variables does not simulate a runner's filesystem. **Verdict stays `Pending`: fork-side only, NOT pushed.** No branch prepared and no pull request opened; opening one against a repository the user does not own needs explicit approval, as #288 and #289 both did. Upstream was NOT re-measured for these two drivers by this plan, and that limit is recorded rather than papered over: the upstream half of this row still rests on the `bin/adopt.rb` measurement above. **Scope limit, unchanged and restated:** five drivers audited. Any other `bin/` script ending in a shell-out has the same question open, and `ci/*.sh` was not examined. **Appended 2026-09-04 (plan 05-21, gap closure for GAP-05-04). The scope limit immediately above is now a RUN-TIME ENUMERATION rather than a sentence, and closing it reframed the class.** That limit -- "five drivers audited, any other script has the same question open" -- was the defect one level up: a hand-maintained list of AUDITED scripts goes stale exactly the way a hand-maintained list of PARSED ARGUMENTS does, and it proved that inside a day. The inventory that scoped 05-21 was built with a grep for `$1`, which does NOT match `${1:-}`, the form every shell script in `bin/` actually uses; it therefore reported `bin/init-bootstrap-env.sh` as parsing zero arguments and put it on the FIX list. Reading the file moved it to the reference set instead. A table built by eye was wrong within a day and pointed at unnecessary work on correct code. **So the population is now PARSED OUT OF THE MAKEFILE at run time** -- every script some recipe launches -- and every member must carry one of three dispositions with its reason ASSERTED rather than commented. Completeness is asserted in BOTH directions, because a one-directional check rots in the other: a member with no entry fails NAMING ITS PATH, and an entry naming a script no recipe launches fails AS STALE. **The generic, upstreamable shape is therefore not "add front doors to the scripts we found" but MEMBERSHIP WITHOUT CLASSIFICATION IS A BUILD FAILURE**, so the script somebody adds six months from now cannot land silently. **Measured: 15 members, not the 14 the audit listed.** The delta is `ci/gen-macos-icons.swift`, missed because the audit looked only at `bin/`; it is measured-safe, refusing on arity before its first write. **THE CONVENTION ALREADY EXISTED, AND THAT IS THE FINDING THAT REFRAMES THIS ENTIRE ROW.** `bin/clean-revoked-certs.rb` and `bin/revoke-orphan-certs.rb` -- the two most destructive scripts in the tree, both of which revoke Apple certificates against a per-team quota -- have had `--help`, `--dry-run` and a refusal that names the unknown flag SINCE THE DAY THEY WERE WRITTEN. `adopt.rb`, `submit.rb` and `ship.rb` did not arrive broken; they DRIFTED from a convention two siblings already followed, and nothing noticed for as long as those scripts existed, because the convention lived in code rather than in a check. Fixing three more by hand would have left the identical hole open for the fourth. **Six of fifteen are already_correct, five measured_safe, four front_door_required -- and only ONE needed work.** `bin/bootstrap-fork.rb`, which pushes straight to main, writes repository secrets and applies branch protection, parsed nothing at all, so `--help` ran it. It now parses before the library is even loaded, prints usage naming what it acts on, and refuses anything else BY NAME at exit 2. **It gets NO `--dry-run`, deliberately:** this repository already has one and it is spelled `make doctor`, because `Runner#doctor` walks the SAME steps and asks each only for its reporting half -- and shipping a second flag that did something subtly different is precisely the `ship.rb --dry-run` trap recorded earlier in this row. **`bin/init-bootstrap-env.sh`, measured either way as promised: it CAN overwrite an existing `.bootstrap.env` -- but only when `--force` is passed explicitly, and that gate sits BEFORE the copy.** Disposition `already_correct`; `git diff` on it, and on both cert scripts, is empty at the end of the plan. **Three corrections to the plan's own audit table, measured rather than inherited.** (1) `clean-revoked-certs.rb` refuses an unknown argument at exit **1**, not the exit 2 the table claimed, because it routes through the shared `UI.fail!`. RECORDED rather than "fixed": rewriting a live cert-revocation path to renumber an exit code is not a trade worth making, so the gate asserts the exit code that is TRUE. (2) Three of the four scripts the table called "partial parsing" -- `bin/phase-runbook.sh`, `ci/local-check.sh`, `ci/take-screenshots.sh` -- have real front doors on reading. (3) `bin/setup-github.sh` needs none: its first argument is a repository slug, and any argument failing the `owner/name` shape is refused BY NAME before the first write, so the `--help` probe this row is about cannot reach the branch-protection PUT. It does not HONOUR a help flag, which is a usage gap rather than a hazard, and closing it would mean editing the script that performs UL-003 / UL-042's destructive-array write for no measured gain. **PROSE ABOUT CODE IS NOT CODE, measured twice in OPPOSITE directions while building this.** `Runner#doctor` MENTIONS the acting half in a comment while never calling it, so a naive count called the one read-only driver in the enumeration a mutating one and would have sent this plan to add a front door to a script that needs none; and `bin/adopt.rb`'s header says in prose that it ends by handing off to its lane, ABOVE the parse it gained in 05-19, so a naive offset comparison reported a CORRECT front door as parsing too late -- a false regression report about the very file this class was discovered through. Comments are blanked before any ordering is measured. **A RED CONTROL FOUND A DEFECT IN THE GATE, AND THE GATE WAS FIXED RATHER THAN THE CONTROL.** The probe for doctor's safety was first written as the receiver-qualified call both entry points happen to use; the control reached the same method through a DIFFERENT receiver and the suite STAYED GREEN. A receiver-specific needle exempts every other way of calling the same thing, which checks a coding style rather than a behaviour. Anchored on the call instead, the identical mutation trips it. **Four red controls in all**, each mutation verified by a moved sha256 and each restore by the sha256 returning: an entry removed (missing=1, stale=0), an entry added for a script with no make target (missing=0, stale=1), the acting half added to `Runner#doctor`, and -- the THIRD subject in this row to show it -- `exit 2` removed from the new refusal while every word of it was left intact, which produced 6 failures with all three arguments walking on into the config layer WHILE all three "the refusal NAMES the argument" assertions stayed GREEN. **`bin/bootstrap-fork.rb` is observed in a second sandbox tree that has NO `.bootstrap.env`**, because its consenting path creates repositories and no shim directory makes a bare run safe to attempt. The observable boundary moves EARLIER, to the config layer, whose refusal is named and deliberate: a bare run MUST reach it, which proves the front door does not swallow a consenting invocation and that the process really executed, while `--help` and an unknown argument MUST NOT. That containment holds whether the front door works or not, which is what makes running the driver at all safe. **279 checks, 0 failures under 3.3.12 and 4.0.6, with CI eligibility verified in a CLONE** -- no `.bootstrap.env`, no generated project, gem paths and `RUBYOPT` cleared, `PATH` cut to `/usr/bin:/bin`. **Verdict stays `Pending`: fork-side only, and NOT pushed.** No branch was prepared and no pull request opened; opening one against a repository the user does not own needs explicit approval, as #288 and #289 both did. Upstream was NOT re-measured by this plan, and that limit is recorded rather than papered over. **Scope limit, restated and now far narrower:** the population is every script a `make` recipe launches. The seven `bin/` scripts with no make target remain outside it by that definition -- but if one gains a target it enters the population automatically and fails the gate until somebody classifies it, which is the property this plan exists to add. |
| UL-052 | 2026-09-04 | 5 | Enumerating an audit population from the build file is right and is still not enough: it defines the population as "what `make` reaches", so a script a HUMAN types and no recipe names sits outside the gate by construction. `ci/bump-asc-version.sh` is that script -- no make target, no workflow job, documented only in a Fastfile comment as the last step of a release. Its help guard read `$1` for exactly two spellings and its `case` of known flags only ever saw `"${@:2}"`, so every other probe-shaped word in position 1 became the version TAG. Measured in a sandbox with the capture step shimmed and bundler inert: `--dry-run` and `--no-capture` -- the second a REAL flag of that same script -- both captured screenshots and then reached the App Store Connect bump. The payload `ci/bump-asc-version.rb` had no shape check at all, so `--help` PATCHed the edit-state version string to the literal `--help` on both platforms, measured offline against a stub. Fixing only the wrapper would not have closed it: the payload documents direct invocation in its own header. The structural fix is to stop treating one entry-point registry as the population and union three enumerated sources -- recipe lines, git’s executable bit read from the INDEX rather than the filesystem, and interpreter launches by an existing member -- each asserted separately, because a union that has stopped drawing from one source looks identical to one that never did. | No - in scope | Pending | — | Fork: `b8cfbcc` (RED, 346 checks / 8 failures, every failure naming one of the two files) and `df866ab` (GREEN, 346 / 0 under 3.3.12 and 4.0.6). Population 15 -> 25; the ten new members are the ci/ set, each classified with its reason as an ASSERTION rather than a comment. **Upstream applicability, and its limit:** `ci/bump-asc-version.sh` and `ci/bump-asc-version.rb` are template-owned and the fork was 4 lines divergent on the .rb at the last sync (UL-034), so both fixes look upstreamable -- but **upstream was NOT re-measured by this work**, and that is recorded rather than assumed. The enumeration widening is the more valuable half and is fully template-owned. **Scope limit, stated rather than left to be found:** source 2 is scoped to `ci/`. Nine tracked executables under `bin/` that no recipe launches are still outside the population; the count is PRINTED in the suite’s transcript (`unenumerated_bin=9`) so it moves when somebody adds one, and closing that half is this row’s own follow-up. Widening it now would add nine unmeasured members at once, and a disposition written without a measurement behind it is the assumption this whole group exists to refuse. |
| UL-053 | 2026-09-05 | 6 | A template-owned script that greps an APP SOURCE FILE for a literal, and guards its own patch step with an `[ -n "$VAR" ]` test that returns 0 when the variable is empty, becomes a silent no-op the moment a fork replaces the file it greps -- which every fork does, because replacing the placeholder screen is the entire point of the template. `bin/take-readme-screenshots.sh` captures the placeholder view's subtitle into `ORIGINAL_SUBTITLE`, and when the grep finds nothing that variable is the empty string, so `patch_subtitle` and `restore_subtitle` both return early without patching anything. The script still exits green and still produces screenshots; they are simply no longer per-platform. Nothing in the repository says so, because the guard was written to make the step OPTIONAL rather than to report that it did not run. | No - in scope | Pending | — | Fork: this phase deleted the placeholder root view under D-66 / PRIV-06, so the degradation is live here as of `d71be99`. NOT FIXED HERE, deliberately: `bin/` is template-owned and `bin/refork-smoketest.sh` deletes and recreates this repository from the template, so an edit there is replaced wholesale (AGENTS.md). Consequence recorded rather than hidden: the README and getting-started screenshots still show the deleted placeholder UI and now have no automated refresh path. **Upstream applicability:** the fix is generic and small -- either drop the subtitle-patching feature, or make an empty capture an explicit warning naming the file the literal was not found in, so a silent no-op becomes a stated one. A guard that makes a step optional should still say when it opted out. |
| UL-054 | 2026-09-05 | 6 | A retired-flag error message that names a template SOURCE PATH hands the reader an instruction that stops being executable as soon as a fork does the thing the template exists for. `bin/rename.sh` refuses `--platforms` with advice to edit the placeholder view directly; the file it names is exactly the one a fork is expected to replace, so the advice resolves to nothing for any fork past its first hour. A dangling path inside a `fail` string is invisible to every gate -- it is a string, not a reference, so no compiler, linter or test sees it go stale. | No - in scope | Pending | — | Fork: same commit and same reason as UL-053 above -- template-owned, not edited here. Found by searching the whole tracked tree for every string the deleted view rendered rather than by following imports, which is how a reference living only inside a message gets found at all. **Upstream applicability:** name the concept rather than the path -- "the app's root view" -- or point at a directory that survives. Generic to any template whose scripts advise editing files the template expects forks to replace. |
